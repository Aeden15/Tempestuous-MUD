<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE MudletPackage>
<MudletPackage version="1.001">
  <TriggerPackage>
      <TriggerGroup isActive="yes" isFolder="yes" isTempTrigger="no" isMultiline="no" isPerlSlashGOption="no" isColorizerTrigger="no" isFilterTrigger="no" isSoundTrigger="no" isColorTrigger="no" isColorTriggerFg="no" isColorTriggerBg="no">
        <name>Tempest Combat</name>
        <script></script>
        <triggerType>0</triggerType>
        <conditonLineDelta>0</conditonLineDelta>
        <mStayOpen>0</mStayOpen>
        <mCommand></mCommand>
        <packageName></packageName>
        <mFgColor>transparent</mFgColor>
        <mBgColor>transparent</mBgColor>
        <mSoundFile></mSoundFile>
        <colorTriggerFgColor>#ff0000</colorTriggerFgColor>
        <colorTriggerBgColor>#ffff00</colorTriggerBgColor>
        <regexCodeList>
        </regexCodeList>
        <regexCodePropertyList>
        </regexCodePropertyList>
      <Trigger isActive="yes" isFolder="no" isTempTrigger="no" isMultiline="no" isPerlSlashGOption="no" isColorizerTrigger="no" isFilterTrigger="no" isSoundTrigger="no" isColorTrigger="no" isColorTriggerFg="no" isColorTriggerBg="no">
        <name>Posture line</name>
        <script>Tempest.update_posture_from_line(matches[1] or &quot;&quot;)</script>
        <triggerType>0</triggerType>
        <conditonLineDelta>0</conditonLineDelta>
        <mStayOpen>0</mStayOpen>
        <mCommand></mCommand>
        <packageName></packageName>
        <mFgColor>transparent</mFgColor>
        <mBgColor>transparent</mBgColor>
        <mSoundFile></mSoundFile>
        <colorTriggerFgColor>#ff0000</colorTriggerFgColor>
        <colorTriggerBgColor>#ffff00</colorTriggerBgColor>
        <regexCodeList>
          <string>^\s*(.*Position.*)\s*$</string>
        </regexCodeList>
        <regexCodePropertyList>
          <integer>1</integer>
        </regexCodePropertyList>
      </Trigger>
      <Trigger isActive="yes" isFolder="no" isTempTrigger="no" isMultiline="no" isPerlSlashGOption="no" isColorizerTrigger="no" isFilterTrigger="no" isSoundTrigger="no" isColorTrigger="no" isColorTriggerFg="no" isColorTriggerBg="no">
        <name>Knockdown stand</name>
        <script>Tempest.on_knockdown()</script>
        <triggerType>0</triggerType>
        <conditonLineDelta>0</conditonLineDelta>
        <mStayOpen>0</mStayOpen>
        <mCommand></mCommand>
        <packageName></packageName>
        <mFgColor>transparent</mFgColor>
        <mBgColor>transparent</mBgColor>
        <mSoundFile></mSoundFile>
        <colorTriggerFgColor>#ff0000</colorTriggerFgColor>
        <colorTriggerBgColor>#ffff00</colorTriggerBgColor>
        <regexCodeList>
          <string>^You are knocked down from the force of the blow!$</string>
        </regexCodeList>
        <regexCodePropertyList>
          <integer>1</integer>
        </regexCodePropertyList>
      </Trigger>
      <Trigger isActive="yes" isFolder="no" isTempTrigger="no" isMultiline="no" isPerlSlashGOption="no" isColorizerTrigger="no" isFilterTrigger="no" isSoundTrigger="no" isColorTrigger="no" isColorTriggerFg="no" isColorTriggerBg="no">
        <name>Knockdown stand alt</name>
        <script>Tempest.on_knockdown()</script>
        <triggerType>0</triggerType>
        <conditonLineDelta>0</conditonLineDelta>
        <mStayOpen>0</mStayOpen>
        <mCommand></mCommand>
        <packageName></packageName>
        <mFgColor>transparent</mFgColor>
        <mBgColor>transparent</mBgColor>
        <mSoundFile></mSoundFile>
        <colorTriggerFgColor>#ff0000</colorTriggerFgColor>
        <colorTriggerBgColor>#ffff00</colorTriggerBgColor>
        <regexCodeList>
          <string>^You have been knocked down!$</string>
        </regexCodeList>
        <regexCodePropertyList>
          <integer>1</integer>
        </regexCodePropertyList>
      </Trigger>
      <Trigger isActive="yes" isFolder="no" isTempTrigger="no" isMultiline="no" isPerlSlashGOption="no" isColorizerTrigger="no" isFilterTrigger="no" isSoundTrigger="no" isColorTrigger="no" isColorTriggerFg="no" isColorTriggerBg="no">
        <name>Knockdown message loose</name>
        <script>Tempest.on_knockdown()</script>
        <triggerType>0</triggerType>
        <conditonLineDelta>0</conditonLineDelta>
        <mStayOpen>0</mStayOpen>
        <mCommand></mCommand>
        <packageName></packageName>
        <mFgColor>transparent</mFgColor>
        <mBgColor>transparent</mBgColor>
        <mSoundFile></mSoundFile>
        <colorTriggerFgColor>#ff0000</colorTriggerFgColor>
        <colorTriggerBgColor>#ffff00</colorTriggerBgColor>
        <regexCodeList>
          <string>(?i).*knocked down.*</string>
        </regexCodeList>
        <regexCodePropertyList>
          <integer>1</integer>
        </regexCodePropertyList>
      </Trigger>
      <Trigger isActive="yes" isFolder="no" isTempTrigger="no" isMultiline="no" isPerlSlashGOption="no" isColorizerTrigger="no" isFilterTrigger="no" isSoundTrigger="no" isColorTrigger="no" isColorTriggerFg="no" isColorTriggerBg="no">
        <name>Knockdown retreat stumble</name>
        <script>Tempest.on_knockdown()</script>
        <triggerType>0</triggerType>
        <conditonLineDelta>0</conditonLineDelta>
        <mStayOpen>0</mStayOpen>
        <mCommand></mCommand>
        <packageName></packageName>
        <mFgColor>transparent</mFgColor>
        <mBgColor>transparent</mBgColor>
        <mSoundFile></mSoundFile>
        <colorTriggerFgColor>#ff0000</colorTriggerFgColor>
        <colorTriggerBgColor>#ffff00</colorTriggerBgColor>
        <regexCodeList>
          <string>^As you retreat backwards, you loose your footing and stumble to the ground!$</string>
          <string>^As you retreat backwards, you lose your footing and stumble to the ground!$</string>
        </regexCodeList>
        <regexCodePropertyList>
          <integer>1</integer>
          <integer>1</integer>
        </regexCodePropertyList>
      </Trigger>
      <Trigger isActive="yes" isFolder="no" isTempTrigger="no" isMultiline="no" isPerlSlashGOption="no" isColorizerTrigger="no" isFilterTrigger="no" isSoundTrigger="no" isColorTrigger="no" isColorTriggerFg="no" isColorTriggerBg="no">
        <name>Cannot command while laying</name>
        <script>Tempest.on_knockdown()</script>
        <triggerType>0</triggerType>
        <conditonLineDelta>0</conditonLineDelta>
        <mStayOpen>0</mStayOpen>
        <mCommand></mCommand>
        <packageName></packageName>
        <mFgColor>transparent</mFgColor>
        <mBgColor>transparent</mBgColor>
        <mSoundFile></mSoundFile>
        <colorTriggerFgColor>#ff0000</colorTriggerFgColor>
        <colorTriggerBgColor>#ffff00</colorTriggerBgColor>
        <regexCodeList>
          <string>^You can't use this command while laying\.$</string>
        </regexCodeList>
        <regexCodePropertyList>
          <integer>1</integer>
        </regexCodePropertyList>
      </Trigger>
      <Trigger isActive="yes" isFolder="no" isTempTrigger="no" isMultiline="no" isPerlSlashGOption="no" isColorizerTrigger="no" isFilterTrigger="no" isSoundTrigger="no" isColorTrigger="no" isColorTriggerFg="no" isColorTriggerBg="no">
        <name>Stood up</name>
        <script>Tempest.note_stood_up()</script>
        <triggerType>0</triggerType>
        <conditonLineDelta>0</conditonLineDelta>
        <mStayOpen>0</mStayOpen>
        <mCommand></mCommand>
        <packageName></packageName>
        <mFgColor>transparent</mFgColor>
        <mBgColor>transparent</mBgColor>
        <mSoundFile></mSoundFile>
        <colorTriggerFgColor>#ff0000</colorTriggerFgColor>
        <colorTriggerBgColor>#ffff00</colorTriggerBgColor>
        <regexCodeList>
          <string>^You stand up\.$</string>
        </regexCodeList>
        <regexCodePropertyList>
          <integer>1</integer>
        </regexCodePropertyList>
      </Trigger>
      <Trigger isActive="yes" isFolder="no" isTempTrigger="no" isMultiline="no" isPerlSlashGOption="no" isColorizerTrigger="no" isFilterTrigger="no" isSoundTrigger="no" isColorTrigger="no" isColorTriggerFg="no" isColorTriggerBg="no">
        <name>Move capability ft units</name>
        <script>Tempest.note_move_capability(matches[2], matches[3])</script>
        <triggerType>0</triggerType>
        <conditonLineDelta>0</conditonLineDelta>
        <mStayOpen>0</mStayOpen>
        <mCommand></mCommand>
        <packageName></packageName>
        <mFgColor>transparent</mFgColor>
        <mBgColor>transparent</mBgColor>
        <mSoundFile></mSoundFile>
        <colorTriggerFgColor>#ff0000</colorTriggerFgColor>
        <colorTriggerBgColor>#ffff00</colorTriggerBgColor>
        <regexCodeList>
          <string>^\s*You can move (\d+)ft\((\d+)units\)!\s*$</string>
        </regexCodeList>
        <regexCodePropertyList>
          <integer>1</integer>
        </regexCodePropertyList>
      </Trigger>
      <Trigger isActive="yes" isFolder="no" isTempTrigger="no" isMultiline="no" isPerlSlashGOption="no" isColorizerTrigger="no" isFilterTrigger="no" isSoundTrigger="no" isColorTrigger="no" isColorTriggerFg="no" isColorTriggerBg="no">
        <name>Invalid target stop auto</name>
        <script>Tempest.on_invalid_target()</script>
        <triggerType>0</triggerType>
        <conditonLineDelta>0</conditonLineDelta>
        <mStayOpen>0</mStayOpen>
        <mCommand></mCommand>
        <packageName></packageName>
        <mFgColor>transparent</mFgColor>
        <mBgColor>transparent</mBgColor>
        <mSoundFile></mSoundFile>
        <colorTriggerFgColor>#ff0000</colorTriggerFgColor>
        <colorTriggerBgColor>#ffff00</colorTriggerBgColor>
        <regexCodeList>
          <string>^That target does not exist\.$</string>
        </regexCodeList>
        <regexCodePropertyList>
          <integer>1</integer>
        </regexCodePropertyList>
      </Trigger>
      <Trigger isActive="yes" isFolder="no" isTempTrigger="no" isMultiline="no" isPerlSlashGOption="no" isColorizerTrigger="no" isFilterTrigger="no" isSoundTrigger="no" isColorTrigger="no" isColorTriggerFg="no" isColorTriggerBg="no">
        <name>Melee unusable stop auto</name>
        <script>Tempest.on_melee_unusable(&quot;weapon type&quot;)</script>
        <triggerType>0</triggerType>
        <conditonLineDelta>0</conditonLineDelta>
        <mStayOpen>0</mStayOpen>
        <mCommand></mCommand>
        <packageName></packageName>
        <mFgColor>transparent</mFgColor>
        <mBgColor>transparent</mBgColor>
        <mSoundFile></mSoundFile>
        <colorTriggerFgColor>#ff0000</colorTriggerFgColor>
        <colorTriggerBgColor>#ffff00</colorTriggerBgColor>
        <regexCodeList>
          <string>^You cannot use this type of weapon to perform a melee attack!$</string>
        </regexCodeList>
        <regexCodePropertyList>
          <integer>1</integer>
        </regexCodePropertyList>
      </Trigger>
      <Trigger isActive="yes" isFolder="no" isTempTrigger="no" isMultiline="no" isPerlSlashGOption="no" isColorizerTrigger="no" isFilterTrigger="no" isSoundTrigger="no" isColorTrigger="no" isColorTriggerFg="no" isColorTriggerBg="no">
        <name>Weapon allows slashing</name>
        <script>Tempest.note_slashing_weapon(matches[1])</script>
        <triggerType>0</triggerType>
        <conditonLineDelta>0</conditonLineDelta>
        <mStayOpen>0</mStayOpen>
        <mCommand></mCommand>
        <packageName></packageName>
        <mFgColor>transparent</mFgColor>
        <mBgColor>transparent</mBgColor>
        <mSoundFile></mSoundFile>
        <colorTriggerFgColor>#ff0000</colorTriggerFgColor>
        <colorTriggerBgColor>#ffff00</colorTriggerBgColor>
        <regexCodeList>
          <string>^\s*This\s+is\s+a\s+slashing\s+weapon\s*\.?\s*$</string>
          <string>^\s*This-is-a-slashing-weapon\s*\.?\s*$</string>
        </regexCodeList>
        <regexCodePropertyList>
          <integer>1</integer>
          <integer>1</integer>
        </regexCodePropertyList>
      </Trigger>
      <Trigger isActive="yes" isFolder="no" isTempTrigger="no" isMultiline="no" isPerlSlashGOption="no" isColorizerTrigger="no" isFilterTrigger="no" isSoundTrigger="no" isColorTrigger="no" isColorTriggerFg="no" isColorTriggerBg="no">
        <name>Weapon allows blunt</name>
        <script>Tempest.note_blunt_weapon(matches[1])</script>
        <triggerType>0</triggerType>
        <conditonLineDelta>0</conditonLineDelta>
        <mStayOpen>0</mStayOpen>
        <mCommand></mCommand>
        <packageName></packageName>
        <mFgColor>transparent</mFgColor>
        <mBgColor>transparent</mBgColor>
        <mSoundFile></mSoundFile>
        <colorTriggerFgColor>#ff0000</colorTriggerFgColor>
        <colorTriggerBgColor>#ffff00</colorTriggerBgColor>
        <regexCodeList>
          <string>^\s*This\s+is\s+a\s+blunt\s+weapon\s*\.?\s*$</string>
          <string>^\s*This-is-a-blunt-weapon\s*\.?\s*$</string>
        </regexCodeList>
        <regexCodePropertyList>
          <integer>1</integer>
          <integer>1</integer>
        </regexCodePropertyList>
      </Trigger>
    </TriggerGroup>
  </TriggerPackage>
  <TimerPackage />
  <AliasPackage>
    <AliasGroup isActive="yes" isFolder="yes">
      <name>Tempest Combat</name>
      <script></script>
      <command></command>
      <packageName></packageName>
      <regex></regex>
      <Alias isActive="yes" isFolder="no">
        <name>Auto melee off</name>
        <script>Tempest.auto_melee_stop()</script>
        <command></command>
        <packageName></packageName>
        <regex>^acoff$</regex>
      </Alias>
      <Alias isActive="yes" isFolder="no">
        <name>Auto melee on</name>
        <script>Tempest.auto_melee_start()</script>
        <command></command>
        <packageName></packageName>
        <regex>^acon$</regex>
      </Alias>
      <Alias isActive="yes" isFolder="no">
        <name>Weapon sharp</name>
        <script>Tempest.set_weapon_line(&quot;sharp&quot;)</script>
        <command></command>
        <packageName></packageName>
        <regex>^wsharp$</regex>
      </Alias>
      <Alias isActive="yes" isFolder="no">
        <name>Weapon blunt</name>
        <script>Tempest.set_weapon_line(&quot;blunt&quot;)</script>
        <command></command>
        <packageName></packageName>
        <regex>^wblunt$</regex>
      </Alias>
      <Alias isActive="yes" isFolder="no">
        <name>Weapon styles reset</name>
        <script>Tempest.reset_weapon_capabilities()</script>
        <command></command>
        <packageName></packageName>
        <regex>^wreset$</regex>
      </Alias>
      <Alias isActive="yes" isFolder="no">
        <name>Set character name</name>
        <script>Tempest.set_character_name(matches[2])</script>
        <command></command>
        <packageName></packageName>
        <regex>^setname (\w+)$</regex>
      </Alias>
      <Alias isActive="yes" isFolder="no">
        <name>Fire rapid</name>
        <script>Tempest.send_ranged(&quot;safe&quot;, matches[2])</script>
        <command></command>
        <packageName></packageName>
        <regex>^frap (.+)$</regex>
      </Alias>
      <Alias isActive="yes" isFolder="no">
        <name>Fire deftly</name>
        <script>Tempest.send_ranged(&quot;mid&quot;, matches[2])</script>
        <command></command>
        <packageName></packageName>
        <regex>^fdeft (.+)$</regex>
      </Alias>
      <Alias isActive="yes" isFolder="no">
        <name>Fire precisely</name>
        <script>Tempest.send_ranged(&quot;heavy&quot;, matches[2])</script>
        <command></command>
        <packageName></packageName>
        <regex>^fprec (.+)$</regex>
      </Alias>
      <Alias isActive="yes" isFolder="no">
        <name>Fire auto risk</name>
        <script>Tempest.send_ranged(&quot;auto&quot;, matches[2])</script>
        <command></command>
        <packageName></packageName>
        <regex>^fauto (.+)$</regex>
      </Alias>
      <Alias isActive="yes" isFolder="no">
        <name>Move by units</name>
        <script>Tempest.move_by_units(tonumber(matches[2]))</script>
        <command></command>
        <packageName></packageName>
        <regex>^mv ([+-]?\d+)$</regex>
      </Alias>
      <Alias isActive="yes" isFolder="no">
        <name>Move toward target</name>
        <script>Tempest.move_towards(matches[2])</script>
        <command></command>
        <packageName></packageName>
        <regex>^mvt (.+)$</regex>
      </Alias>
    </AliasGroup>
  </AliasPackage>
  <ActionPackage />
  <ScriptPackage>
    <ScriptGroup isActive="yes" isFolder="yes">
      <name>Tempest Core</name>
      <script></script>
      <command></command>
      <packageName></packageName>
      <regex></regex>
      <Script isActive="yes" isFolder="no">
        <name>target helper</name>
        <packageName></packageName>
        <script><![CDATA[
Tempest = Tempest or {}

Tempest.target = Tempest.target or ""
Tempest.risk_band = Tempest.risk_band or "neutral"
-- Preference when the wielded weapon supports both slashing and blunt (see weapon_can_*).
Tempest.weapon_line = Tempest.weapon_line or "blunt"
-- weapon_can_slash / weapon_can_blunt: nil until triggers fire or you wreset (see TempestCombat.xml).
Tempest.character_name = Tempest.character_name or ""
-- From "You can move Nft(Uunits)!"; optional cap for move_by_units (e.g. 1 when server allows one unit per command).
Tempest.move_feet_per_step = Tempest.move_feet_per_step or nil
Tempest.move_units_available = Tempest.move_units_available or nil
Tempest.move_max_units_per_move = Tempest.move_max_units_per_move or nil

function Tempest.set_character_name(name)
  Tempest.character_name = tostring(name or ""):match("^%s*(.-)%s*$") or ""
  if Tempest.character_name ~= "" then
    cecho("<cyan>[Tempest] Character name set for self-checks: <white>" .. Tempest.character_name .. "\n")
  end
end

function Tempest.get_self_name()
  local n = Tempest.character_name or ""
  if n ~= "" then
    return n
  end
  if type(gmcp) == "table" and gmcp.Char and gmcp.Char.Name then
    return tostring(gmcp.Char.Name)
  end
  return ""
end

local function norm_lower(s)
  return tostring(s or ""):match("^%s*(.-)%s*$"):lower()
end

local function is_target_self(name)
  local self_name = Tempest.get_self_name()
  if self_name == "" or name == "" then
    return false
  end
  return norm_lower(name) == norm_lower(self_name)
end

function Tempest.set_weapon_line(line)
  local key = norm_lower(line)
  if key ~= "sharp" and key ~= "blunt" then
    cecho("<red>[Tempest] weapon_line must be sharp or blunt.\n")
    return false
  end
  Tempest.weapon_line = key
  return true
end

function Tempest.reset_weapon_capabilities()
  Tempest.weapon_can_slash = nil
  Tempest.weapon_can_blunt = nil
end

function Tempest.note_slashing_weapon(line)
  Tempest.weapon_can_slash = true
end

function Tempest.note_blunt_weapon(line)
  Tempest.weapon_can_blunt = true
end

--- Effective line for verb choice: single-style from probes, or weapon_line when both apply.
function Tempest.resolve_effective_weapon_line()
  local s = Tempest.weapon_can_slash
  local b = Tempest.weapon_can_blunt
  if s == true and b ~= true then
    return "sharp"
  end
  if b == true and s ~= true then
    return "blunt"
  end
  if s == true and b == true then
    return Tempest.weapon_line or "blunt"
  end
  return Tempest.weapon_line or "blunt"
end

function Tempest.melee_verb_for_tier(tier)
  local t = tostring(tier or ""):lower()
  local wl = Tempest.resolve_effective_weapon_line()
  if t == "safe" then
    return wl == "sharp" and "slash" or "pound"
  end
  if t == "mid" then
    return wl == "sharp" and "slice" or "crush"
  end
  if t == "heavy" then
    return wl == "sharp" and "cleave" or "smash"
  end
  return nil
end

--- Ranged attack style for fire: safe -> rapid, mid -> deftly, heavy -> precisely (see attackinfo / help fire).
function Tempest.ranged_verb_for_tier(tier)
  local t = tostring(tier or ""):lower()
  if t == "safe" then
    return "rapid"
  end
  if t == "mid" then
    return "deftly"
  end
  if t == "heavy" then
    return "precisely"
  end
  return nil
end

function Tempest.set_target(name)
  name = tostring(name or ""):match("^%s*(.-)%s*$")

  if name == "" then
    cecho("<red>[Tempest] No target supplied.\n")
    return false
  end

  Tempest.target = name

  -- Compatibility with older/simple aliases that may expect a global target.
  target = name

  cecho("<cyan>[Tempest] Target set: <white>" .. name .. "\n")
  return true
end

function Tempest.get_target()
  if Tempest.target and Tempest.target ~= "" then
    return Tempest.target
  end

  if target and target ~= "" then
    return target
  end

  return nil
end

function Tempest.require_target()
  local tgt = Tempest.get_target()

  if not tgt or tgt == "" then
    cecho("<red>[Tempest] No target set. Use: tt <name>\n")
    return nil
  end

  if is_target_self(tgt) then
    cecho("<red>[Tempest] Target is your character name; clear or change target with tt.\n")
    return nil
  end

  return tgt
end

function Tempest.normalize_favors(value, silent)
  local text = tostring(value or ""):match("^%s*(.-)%s*$"):lower()
  if text == "" then
    return nil
  end

  if text == "min" or text == "max" or text:match("^%d+$") then
    return text
  end

  if not silent then
    cecho("<red>[Tempest] Invalid favors value. Use: min, max, or a number.\n")
  end
  return nil
end

function Tempest.send_pray(gift, target_name, favors)
  local cmd = "pray " .. gift
  local target_text = tostring(target_name or ""):match("^%s*(.-)%s*$")
  if target_text ~= "" then
    cmd = cmd .. " " .. target_text
  end

  local favors_text = Tempest.normalize_favors(favors)
  if favors_text then
    cmd = cmd .. " " .. favors_text
  end

  send(cmd)
end

--- Buff-style alias: target is tt (or self if unset). If the first optional word is a
--- favors token (min/max/digits), it applies to that target. Otherwise the first word is
--- a manual denizen name for this cast only (tt unchanged). Optional second word = favors.
--- Not used for celestial grasp (self-only).
function Tempest.send_pray_self_optional_target(gift, opt_first, opt_second)
  local target_name
  local favors

  local first = tostring(opt_first or ""):match("^%s*(.-)%s*$")
  if first == "" then
    local gt = Tempest.get_target()
    target_name = (gt and gt ~= "") and gt or "self"
    favors = nil
  else
    local maybe_favors = Tempest.normalize_favors(first, true)
    if maybe_favors then
      local gt = Tempest.get_target()
      target_name = (gt and gt ~= "") and gt or "self"
      favors = maybe_favors
    else
      target_name = first
      favors = opt_second
    end
  end

  Tempest.send_pray(gift, target_name, favors)
end

--- Offense / require-target prayers: use Tempest.require_target() when no manual name.
--- If the first optional word is a favors token, target comes from require_target().
--- Otherwise the first word is a manual denizen name (tt unchanged); optional second = favors.
function Tempest.send_pray_require_or_manual_target(gift, opt_first, opt_second)
  local target_name
  local favors

  local first = tostring(opt_first or ""):match("^%s*(.-)%s*$")

  if first == "" then
    target_name = Tempest.require_target()
    if not target_name then
      return
    end
    favors = nil
  else
    local maybe_favors = Tempest.normalize_favors(first, true)
    if maybe_favors then
      target_name = Tempest.require_target()
      if not target_name then
        return
      end
      favors = maybe_favors
    else
      target_name = first
      favors = opt_second
    end
  end

  Tempest.send_pray(gift, target_name, favors)
end

local function resolve_attack_target(explicit_target)
  local text = tostring(explicit_target or ""):match("^%s*(.-)%s*$")
  if text ~= "" then
    return text
  end
  return Tempest.get_target()
end

function Tempest.send_basic_attack(attack, explicit_target)
  local target_name = resolve_attack_target(explicit_target)
  if target_name and target_name ~= "" then
    if is_target_self(target_name) then
      cecho("<red>[Tempest] Refusing to attack yourself; fix target (tt) or character name (Tempest.set_character_name).\n")
      return false
    end
    send(attack .. " " .. target_name)
  else
    send(attack)
  end
  return true
end

--- Syntax: fire <rapid|deftly|precisely> <target> (game allows abbreviations; we send full words).
function Tempest.send_basic_ranged(verb, explicit_target)
  local v = tostring(verb or ""):match("^%s*(.-)%s*$")
  if v == "" then
    return false
  end
  local target_name = resolve_attack_target(explicit_target)
  if not target_name or target_name == "" then
    cecho("<red>[Tempest] Ranged fire needs a target (tt or second argument).\n")
    return false
  end
  if is_target_self(target_name) then
    cecho("<red>[Tempest] Refusing to fire on yourself; fix target (tt) or character name (Tempest.set_character_name).\n")
    return false
  end
  send("fire " .. v .. " " .. target_name)
  return true
end

function Tempest.set_risk_band(value)
  local band = tostring(value or ""):lower():match("^%s*(.-)%s*$")
  local valid = {
    good = true,
    neutral = true,
    bad = true,
    critical = true,
  }

  if not valid[band] then
    cecho("<red>[Tempest] Invalid risk band. Use: good, neutral, bad, critical.\n")
    return false
  end

  Tempest.risk_band = band
  cecho("<cyan>[Tempest] Risk band set: <white>" .. band .. "\n")
  return true
end

function Tempest.send_melee(kind, explicit_target)
  local key = tostring(kind or ""):lower()
  local tier = nil

  if key == "safe" then
    tier = "safe"
  elseif key == "mid" then
    tier = "mid"
  elseif key == "heavy" then
    tier = "heavy"
  elseif key == "auto" then
    if Tempest.risk_band == "good" then
      tier = "heavy"
    elseif Tempest.risk_band == "bad" or Tempest.risk_band == "critical" then
      tier = "safe"
    else
      tier = "mid"
    end
  else
    cecho("<red>[Tempest] Unknown melee mode. Use: safe, mid, heavy, auto.\n")
    return false
  end

  local attack = Tempest.melee_verb_for_tier(tier)
  if not attack then
    return false
  end

  Tempest.send_basic_attack(attack, explicit_target)
  return true
end

--- Same modes as send_melee: safe/mid/heavy/auto from Risk (parallel to melee tier mapping).
function Tempest.send_ranged(kind, explicit_target)
  local key = tostring(kind or ""):lower()
  local tier = nil

  if key == "safe" then
    tier = "safe"
  elseif key == "mid" then
    tier = "mid"
  elseif key == "heavy" then
    tier = "heavy"
  elseif key == "auto" then
    if Tempest.risk_band == "good" then
      tier = "heavy"
    elseif Tempest.risk_band == "bad" or Tempest.risk_band == "critical" then
      tier = "safe"
    else
      tier = "mid"
    end
  else
    cecho("<red>[Tempest] Unknown ranged mode. Use: safe, mid, heavy, auto.\n")
    return false
  end

  local verb = Tempest.ranged_verb_for_tier(tier)
  if not verb then
    return false
  end

  return Tempest.send_basic_ranged(verb, explicit_target)
end

--- Room positioning: move +/-<units> (1 unit = 5ft per help move). Honors Tempest.move_max_units_per_move when set.
function Tempest.move_by_units(delta)
  local n = tonumber(delta)
  if not n or n == 0 then
    cecho("<red>[Tempest] move_by_units needs a non-zero number (e.g. +1 or -2).\n")
    return false
  end
  local cap = tonumber(Tempest.move_max_units_per_move)
  if cap and cap > 0 and math.abs(n) > cap then
    n = n > 0 and cap or -cap
  end
  if n > 0 then
    send("move +" .. tostring(n))
  else
    send("move " .. tostring(n))
  end
  return true
end

--- move <target> — advance toward a denizen or player name.
function Tempest.move_towards(name)
  name = tostring(name or ""):match("^%s*(.-)%s*$") or ""
  if name == "" then
    cecho("<red>[Tempest] move_towards needs a target name.\n")
    return false
  end
  send("move " .. name)
  return true
end

--- Trigger helper: game line "You can move 5ft(1units)!". Pass feet and units from capture groups, or one full line string.
function Tempest.note_move_capability(a, b)
  local feet, units = a, b
  if b == nil and type(a) == "string" then
    feet, units = tostring(a):match("You can move (%d+)ft%((%d+)units%)")
  end
  feet = tonumber(feet)
  units = tonumber(units)
  if feet then
    Tempest.move_feet_per_step = feet
  end
  if units then
    Tempest.move_units_available = units
  end
end

]]></script>
        <eventHandlerList />
      </Script>
      <Script isActive="yes" isFolder="no">
        <name>combat automation</name>
        <packageName></packageName>
        <script><![CDATA[
-- Shared Tempest combat automation (all classes). Depends on Tempest.* from target.lua.



Tempest.auto_melee_active = Tempest.auto_melee_active or false

Tempest.posture_tier = Tempest.posture_tier or "mid"

Tempest.auto_attack_delay = Tempest.auto_attack_delay or 2.1

Tempest._auto_attack_timer_id = Tempest._auto_attack_timer_id or nil

-- True while server reports Knocked Down (posture line or knockdown message).

Tempest.knocked_down = Tempest.knocked_down or false

-- Set true when Posture trigger has fired at least once (see update_posture_from_line).

Tempest._posture_line_seen = Tempest._posture_line_seen or false

-- After queue clear + stand, cleared when posture line shows upright (no Knocked Down).
Tempest._kd_recovery_sent = Tempest._kd_recovery_sent or false


local function cancel_attack_timer()

  if Tempest._auto_attack_timer_id and killTimer then

    killTimer(Tempest._auto_attack_timer_id)

    Tempest._auto_attack_timer_id = nil

  end

end



--- Rank for merging: lower = safer (pound/slash), higher = heavier (smash/cleave).

local function _tier_to_rank(t)

  local x = tostring(t or ""):lower()

  if x == "safe" then

    return 1

  end

  if x == "mid" then

    return 2

  end

  if x == "heavy" then

    return 3

  end

  return 2

end



local function _rank_to_tier(r)

  if r <= 1 then

    return "safe"

  end

  if r <= 2 then

    return "mid"

  end

  return "heavy"

end



--- Same tier mapping as Tempest.send_melee("auto", ...): good→heavy, bad/critical→safe, else mid.

local function _risk_tier_from_band()

  local rb = tostring(Tempest.risk_band or "neutral"):lower()

  if rb == "good" then

    return "heavy"

  end

  if rb == "bad" or rb == "critical" then

    return "safe"

  end

  return "mid"

end



--- Merge posture tier with Risk band: use the safer (lower rank) of the two.

--- Until any posture line is seen, Risk alone drives tier (default posture is not real data).

function Tempest.auto_melee_merged_tier()

  local posture = Tempest.posture_tier or "mid"

  local risk = _risk_tier_from_band()

  if not Tempest._posture_line_seen then

    return risk, posture, risk

  end

  local m = math.min(_tier_to_rank(posture), _tier_to_rank(risk))

  return _rank_to_tier(m), posture, risk

end



--- Queue clear + stand once per knockdown; shared by message triggers and posture line.
local function knockdown_apply_recovery()

  Tempest.knocked_down = true

  Tempest.posture_tier = "safe"

  cancel_attack_timer()

  if not Tempest._kd_recovery_sent then

    Tempest._kd_recovery_sent = true

    send("queue clear")

    send("stand")

  end

end



function Tempest.auto_melee_stop(message)

  Tempest.auto_melee_active = false

  cancel_attack_timer()

  if message ~= false then

    cecho("<yellow>[Tempest Combat] Auto combat off.\n")

  end

end



function Tempest.auto_melee_start(message)

  Tempest.auto_melee_active = true

  if message ~= false then

    cecho("<green>[Tempest Combat] Auto combat on (melee or ranged per weapon).\n")

  end

  Tempest.auto_attack_once()

end



function Tempest.schedule_auto_attack()

  cancel_attack_timer()

  if not Tempest.auto_melee_active then

    return

  end

  local delay = tonumber(Tempest.auto_attack_delay) or 2.1

  if tempTimer then

    Tempest._auto_attack_timer_id = tempTimer(delay, function()

      Tempest.auto_attack_once()

    end)

  end

end



function Tempest.auto_attack_once()

  if not Tempest.auto_melee_active then

    return

  end

  -- Do not queue attacks from the client while prone; server queue + delays block stand.

  if Tempest.knocked_down then

    return

  end

  local tier = select(1, Tempest.auto_melee_merged_tier())

  if Tempest.lunar_cores_ranged_only then

    if not Tempest.send_ranged(tier, nil) then

      Tempest.schedule_auto_attack()

      return

    end

    Tempest.schedule_auto_attack()

    return

  end

  local attack = Tempest.melee_verb_for_tier(tier)

  if not attack then

    Tempest.schedule_auto_attack()

    return

  end

  Tempest.send_basic_attack(attack, nil)

  Tempest.schedule_auto_attack()

end



--- Map posture text from game output to melee tier: safe | mid | heavy

function Tempest.update_posture_from_line(line)

  line = tostring(line or "")

  Tempest._posture_line_seen = true

  local lower = line:lower()

  if lower:find("knocked down", 1, true) then

    knockdown_apply_recovery()

    return

  end

  Tempest.knocked_down = false

  Tempest._kd_recovery_sent = false

  if line:find("Feeble") or line:find("Very Weak") or line:find("Very Unsteady") then

    Tempest.posture_tier = "safe"

    if Tempest.auto_melee_active then

      Tempest.schedule_auto_attack()

    end

    return

  end

  if line:find("Unsteady") then

    Tempest.posture_tier = "safe"

    if Tempest.auto_melee_active then

      Tempest.schedule_auto_attack()

    end

    return

  end

  if line:find("Weak") and not line:find("Very Weak") then

    Tempest.posture_tier = "mid"

    if Tempest.auto_melee_active then

      Tempest.schedule_auto_attack()

    end

    return

  end

  if line:find("Neutral") then

    Tempest.posture_tier = "mid"

    if Tempest.auto_melee_active then

      Tempest.schedule_auto_attack()

    end

    return

  end

  if line:find("Good") or line:find("Strong") then

    Tempest.posture_tier = "heavy"

    if Tempest.auto_melee_active then

      Tempest.schedule_auto_attack()

    end

    return

  end

  if Tempest.auto_melee_active then

    Tempest.schedule_auto_attack()

  end

end



function Tempest.on_knockdown()

  knockdown_apply_recovery()

end



--- Stops auto melee when the game rejects melee for "no usable weapon" reasons.
--- Lunar Sage owns the same line and converts to ranged-only; defer to it when set.
function Tempest.on_melee_unusable(reason)

  if not Tempest.auto_melee_active then

    return

  end

  if Tempest.lunar_cores_ranged_only then

    return

  end

  send("queue clear")

  Tempest.auto_melee_stop(false)

  cecho("<yellow>[Tempest Combat] Auto combat off: melee rejected (" .. tostring(reason or "no weapon") .. "). Wield a weapon (or use ranged) and `acon` again.\n")

end



--- Stops auto melee when the game says the current target is invalid (e.g. mob dead, wrong name).

function Tempest.on_invalid_target()

  if not Tempest.auto_melee_active then

    return

  end

  send("queue clear")

  Tempest.auto_melee_stop(false)

  cecho("<yellow>[Tempest Combat] Auto combat off: invalid target. Set target with tt.\n")

end



--- Call from a trigger on your game's stand-success line if posture updates lag behind.

function Tempest.note_stood_up()

  Tempest.knocked_down = false

  Tempest._kd_recovery_sent = false

  if Tempest.auto_melee_active then

    Tempest.schedule_auto_attack()

  end

end


]]></script>
        <eventHandlerList />
      </Script>
    </ScriptGroup>
  </ScriptPackage>
  <KeyPackage />
</MudletPackage>
