<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE MudletPackage>
<MudletPackage version="1.001">
  <TriggerPackage />
  <TimerPackage />
  <AliasPackage>
    <AliasGroup isActive="yes" isFolder="yes">
      <name>Cleric</name>
      <script></script>
      <command></command>
      <packageName></packageName>
      <regex></regex>
      <AliasGroup isActive="yes" isFolder="yes">
        <name>Basic Gifts</name>
        <script></script>
        <command></command>
        <packageName></packageName>
        <regex></regex>
        <AliasGroup isActive="yes" isFolder="yes">
          <name>Buffs</name>
          <script></script>
          <command></command>
          <packageName></packageName>
          <regex></regex>
          <Alias isActive="yes" isFolder="no">
            <name>Alacrity</name>
            <script>Tempest.send_pray(&quot;alacrity&quot;, &quot;self&quot;, matches[2])</script>
            <command></command>
            <packageName></packageName>
            <regex>^alac(?: (\w+))?$</regex>
          </Alias>
          <Alias isActive="yes" isFolder="no">
            <name>Aura</name>
            <script>Tempest.send_pray(&quot;righteousaura&quot;, &quot;self&quot;, matches[2])</script>
            <command></command>
            <packageName></packageName>
            <regex>^aura(?: (\w+))?$</regex>
          </Alias>
          <Alias isActive="yes" isFolder="no">
            <name>Protection</name>
            <script>Tempest.send_pray(&quot;sacredprotection&quot;, &quot;self&quot;, matches[2])</script>
            <command></command>
            <packageName></packageName>
            <regex>^prot(?: (\w+))?$</regex>
          </Alias>
          <Alias isActive="yes" isFolder="no">
            <name>Rejuvenation</name>
            <script>Tempest.send_pray(&quot;rejuvenation&quot;, &quot;self&quot;, matches[2])</script>
            <command></command>
            <packageName></packageName>
            <regex>^rejuv(?: (\w+))?$</regex>
          </Alias>
        </AliasGroup>
        <AliasGroup isActive="yes" isFolder="yes">
          <name>Healing</name>
          <script></script>
          <command></command>
          <packageName></packageName>
          <regex></regex>
          <Alias isActive="yes" isFolder="no">
            <name>Long distance revive</name>
            <script>local tgt = Tempest.require_target()
if not tgt then return end
Tempest.send_pray(&quot;liturgy:ghostwaverevive&quot;, tgt, matches[2])</script>
            <command></command>
            <packageName></packageName>
            <regex>^lrevive(?: (\w+))?$</regex>
          </Alias>
          <Alias isActive="yes" isFolder="no">
            <name>Resurrection</name>
            <script>local tgt = Tempest.require_target()
if not tgt then return end
Tempest.send_pray(&quot;restoration&quot;, tgt, matches[2])</script>
            <command></command>
            <packageName></packageName>
            <regex>^rezz(?: (\w+))?$</regex>
          </Alias>
          <Alias isActive="yes" isFolder="no">
            <name>Self rez</name>
            <script>Tempest.send_pray(&quot;awakening&quot;, &quot;self&quot;, matches[2])</script>
            <command></command>
            <packageName></packageName>
            <regex>^srezz(?: (\w+))?$</regex>
          </Alias>
          <Alias isActive="yes" isFolder="no">
            <name>Vitality</name>
            <script>Tempest.send_pray(&quot;vitality&quot;, &quot;self&quot;, matches[2])</script>
            <command></command>
            <packageName></packageName>
            <regex>^vit(?: (\w+))?$</regex>
          </Alias>
        </AliasGroup>
        <AliasGroup isActive="yes" isFolder="yes">
          <name>Melee</name>
          <script></script>
          <command></command>
          <packageName></packageName>
          <regex></regex>
          <Alias isActive="yes" isFolder="no">
            <name>Pound</name>
            <script>Tempest.send_melee(&quot;safe&quot;, matches[2])</script>
            <command></command>
            <packageName></packageName>
            <regex>^bp(?: (\w+))?$</regex>
          </Alias>
          <Alias isActive="yes" isFolder="no">
            <name>Crush</name>
            <script>Tempest.send_melee(&quot;mid&quot;, matches[2])</script>
            <command></command>
            <packageName></packageName>
            <regex>^bc(?: (\w+))?$</regex>
          </Alias>
          <Alias isActive="yes" isFolder="no">
            <name>Smash</name>
            <script>Tempest.send_melee(&quot;heavy&quot;, matches[2])</script>
            <command></command>
            <packageName></packageName>
            <regex>^bs(?: (\w+))?$</regex>
          </Alias>
          <Alias isActive="yes" isFolder="no">
            <name>Slash</name>
            <script>Tempest.send_basic_attack(&quot;slash&quot;, matches[2])</script>
            <command></command>
            <packageName></packageName>
            <regex>^sl(?: (\w+))?$</regex>
          </Alias>
          <Alias isActive="yes" isFolder="no">
            <name>Slice</name>
            <script>Tempest.send_basic_attack(&quot;slice&quot;, matches[2])</script>
            <command></command>
            <packageName></packageName>
            <regex>^si(?: (\w+))?$</regex>
          </Alias>
          <Alias isActive="yes" isFolder="no">
            <name>Cleave</name>
            <script>Tempest.send_basic_attack(&quot;cleave&quot;, matches[2])</script>
            <command></command>
            <packageName></packageName>
            <regex>^clv(?: (\w+))?$</regex>
          </Alias>
          <Alias isActive="yes" isFolder="no">
            <name>Risk good</name>
            <script>Tempest.set_risk_band(&quot;good&quot;)</script>
            <command></command>
            <packageName></packageName>
            <regex>^rgb$</regex>
          </Alias>
          <Alias isActive="yes" isFolder="no">
            <name>Risk neutral</name>
            <script>Tempest.set_risk_band(&quot;neutral&quot;)</script>
            <command></command>
            <packageName></packageName>
            <regex>^rgn$</regex>
          </Alias>
          <Alias isActive="yes" isFolder="no">
            <name>Risk bad</name>
            <script>Tempest.set_risk_band(&quot;bad&quot;)</script>
            <command></command>
            <packageName></packageName>
            <regex>^rbd$</regex>
          </Alias>
          <Alias isActive="yes" isFolder="no">
            <name>Risk critical</name>
            <script>Tempest.set_risk_band(&quot;critical&quot;)</script>
            <command></command>
            <packageName></packageName>
            <regex>^rcr$</regex>
          </Alias>
          <Alias isActive="yes" isFolder="no">
            <name>Smart melee</name>
            <script>Tempest.send_melee(&quot;auto&quot;, matches[2])</script>
            <command></command>
            <packageName></packageName>
            <regex>^mma(?: (\w+))?$</regex>
          </Alias>
        </AliasGroup>
        <AliasGroup isActive="yes" isFolder="yes">
          <name>Miscellaneous</name>
          <script></script>
          <command></command>
          <packageName></packageName>
          <regex></regex>
          <Alias isActive="yes" isFolder="no">
            <name>Set target</name>
            <script>Tempest.set_target(matches[2])</script>
            <command></command>
            <packageName></packageName>
            <regex>^tt (\w+)$</regex>
          </Alias>
          <Alias isActive="yes" isFolder="no">
            <name>Teleport</name>
            <script>local first = matches[2]
local second = matches[3]
local target_name = &quot;self&quot;
local favors = nil

if first and first ~= &quot;&quot; then
  local maybe_favors = Tempest.normalize_favors(first, true)
  if maybe_favors then
    favors = maybe_favors
  else
    target_name = first
    favors = second
  end
end

Tempest.send_pray(&quot;celestialgrasp&quot;, target_name, favors)</script>
            <command></command>
            <packageName></packageName>
            <regex>^recall(?: (\w+))?(?: (\w+))?$</regex>
          </Alias>
        </AliasGroup>
        <AliasGroup isActive="yes" isFolder="yes">
          <name>Offense</name>
          <script></script>
          <command></command>
          <packageName></packageName>
          <regex></regex>
          <Alias isActive="yes" isFolder="no">
            <name>Divine word</name>
            <script>local tgt = Tempest.require_target()
if not tgt then return end
Tempest.send_pray(&quot;divineword&quot;, tgt, matches[2])</script>
            <command></command>
            <packageName></packageName>
            <regex>^dword(?: (\w+))?$</regex>
          </Alias>
          <Alias isActive="yes" isFolder="no">
            <name>Hymn</name>
            <script>local tgt = Tempest.require_target()
if not tgt then return end
Tempest.send_pray(&quot;hymnoftheinferno&quot;, tgt, matches[2])</script>
            <command></command>
            <packageName></packageName>
            <regex>^hymn(?: (\w+))?$</regex>
          </Alias>
          <Alias isActive="yes" isFolder="no">
            <name>Rebuke</name>
            <script>local tgt = Tempest.require_target()
if not tgt then return end
Tempest.send_pray(&quot;rebuke&quot;, tgt, matches[2])</script>
            <command></command>
            <packageName></packageName>
            <regex>^reb(?: (\w+))?$</regex>
          </Alias>
        </AliasGroup>
      </AliasGroup>
      <AliasGroup isActive="yes" isFolder="yes">
        <name>Empathy Gifts</name>
        <script></script>
        <command></command>
        <packageName></packageName>
        <regex></regex>
        <AliasGroup isActive="yes" isFolder="yes">
          <name>Buffs</name>
          <script></script>
          <command></command>
          <packageName></packageName>
          <regex></regex>
          <Alias isActive="yes" isFolder="no">
            <name>Curative Efflux</name>
            <script>local tgt = Tempest.require_target()
if not tgt then return end
Tempest.send_pray(&quot;curativeefflux&quot;, tgt, matches[2])</script>
            <command></command>
            <packageName></packageName>
            <regex>^eff(?: (\w+))?$</regex>
          </Alias>
          <Alias isActive="yes" isFolder="no">
            <name>Intercession</name>
            <script>Tempest.send_pray(&quot;intercession&quot;, &quot;self&quot;, matches[2])</script>
            <command></command>
            <packageName></packageName>
            <regex>^inter(?: (\w+))?$</regex>
          </Alias>
          <Alias isActive="yes" isFolder="no">
            <name>Alleviation</name>
            <script>Tempest.send_pray(&quot;alleviation&quot;, &quot;self&quot;, matches[2])</script>
            <command></command>
            <packageName></packageName>
            <regex>^allev(?: (\w+))?$</regex>
          </Alias>
          <Alias isActive="yes" isFolder="no">
            <name>Desurmras Blessing</name>
            <script>local tgt = Tempest.require_target()
if not tgt then return end
Tempest.send_pray(&quot;desurmrasblessing&quot;, tgt, matches[2])</script>
            <command></command>
            <packageName></packageName>
            <regex>^desur(?: (\w+))?$</regex>
          </Alias>
        </AliasGroup>
        <AliasGroup isActive="yes" isFolder="yes">
          <name>Offense</name>
          <script></script>
          <command></command>
          <packageName></packageName>
          <regex></regex>
          <Alias isActive="yes" isFolder="no">
            <name>Objurgation Malice</name>
            <script>local tgt = Tempest.require_target()
if not tgt then return end
Tempest.send_pray(&quot;objurgation:malice&quot;, tgt, matches[2])</script>
            <command></command>
            <packageName></packageName>
            <regex>^objm(?: (\w+))?$</regex>
          </Alias>
        </AliasGroup>
      </AliasGroup>
      <AliasGroup isActive="yes" isFolder="yes">
        <name>Cleric Skills</name>
        <script></script>
        <command></command>
        <packageName></packageName>
        <regex></regex>
        <Alias isActive="yes" isFolder="no">
          <name>Last Rites</name>
          <script>local tgt = matches[2]
if not tgt or tgt == &quot;&quot; then
  tgt = Tempest.require_target()
  if not tgt then return end
end
send(&quot;use lastrites &quot; .. tgt)</script>
          <command></command>
          <packageName></packageName>
          <regex>^rites(?: (\w+))?$</regex>
        </Alias>
        <Alias isActive="yes" isFolder="no">
          <name>Preach</name>
          <script>local npc = matches[2]
local message = matches[3]
local cmd = &quot;use preach &quot; .. npc
if message and message ~= &quot;&quot; then
  cmd = cmd .. &quot; &quot; .. message
end
send(cmd)</script>
          <command></command>
          <packageName></packageName>
          <regex>^preach (\w+)(?: (.+))?$</regex>
        </Alias>
        <Alias isActive="yes" isFolder="no">
          <name>Worship list</name>
          <script>send(&quot;use worship list&quot;)</script>
          <command></command>
          <packageName></packageName>
          <regex>^wlist$</regex>
        </Alias>
      </AliasGroup>
      <AliasGroup isActive="yes" isFolder="yes">
        <name>Obscurity Gifts</name>
        <script></script>
        <command></command>
        <packageName></packageName>
        <regex></regex>
        <Alias isActive="yes" isFolder="no">
          <name>Blackout</name>
          <script>local tgt = Tempest.require_target()
if not tgt then return end
Tempest.send_pray(&quot;blackout&quot;, tgt, matches[2])</script>
          <command></command>
          <packageName></packageName>
          <regex>^black(?: (\w+))?$</regex>
        </Alias>
        <Alias isActive="yes" isFolder="no">
          <name>Obscursion</name>
          <script>Tempest.send_pray(&quot;obscursion&quot;, &quot;self&quot;, matches[2])</script>
          <command></command>
          <packageName></packageName>
          <regex>^obsc(?: (\w+))?$</regex>
        </Alias>
      </AliasGroup>
    </AliasGroup>
  </AliasPackage>
  <ActionPackage />
  <ScriptPackage>
    <ScriptGroup isActive="yes" isFolder="yes">
      <name>Cleric Core</name>
      <packageName></packageName>
      <script></script>
      <eventHandlerList />
      <Script isActive="yes" isFolder="no">
        <name>target helper</name>
        <packageName></packageName>
        <script>Tempest = Tempest or {}

Tempest.target = Tempest.target or &quot;&quot;
Tempest.risk_band = Tempest.risk_band or &quot;neutral&quot;
-- Preference when the wielded weapon supports both slashing and blunt (see weapon_can_*).
Tempest.weapon_line = Tempest.weapon_line or &quot;blunt&quot;
-- weapon_can_slash / weapon_can_blunt: nil until triggers fire or you wreset (see TempestCombat.xml).
Tempest.character_name = Tempest.character_name or &quot;&quot;

function Tempest.set_character_name(name)
  Tempest.character_name = tostring(name or &quot;&quot;):match(&quot;^%s*(.-)%s*<name>target helper</name>
        <packageName></packageName>
        <script>Tempest = Tempest or {}

Tempest.target = Tempest.target or &quot;&quot;
Tempest.risk_band = Tempest.risk_band or &quot;neutral&quot;
-- Preference when the wielded weapon supports both slashing and blunt (see weapon_can_*).
Tempest.weapon_line = Tempest.weapon_line or &quot;blunt&quot;
-- weapon_can_slash / weapon_can_blunt: nil until triggers fire or you wreset (see TempestCombat.xml).
Tempest.character_name = Tempest.character_name or &quot;&quot;

function Tempest.set_character_name(name)
  Tempest.character_name = tostring(name or &quot;&quot;):match(&quot;^%s*(.-)%s*<name>target helper</name>
        <packageName></packageName>
        <script>Tempest = Tempest or {}

Tempest.target = Tempest.target or &quot;&quot;
Tempest.risk_band = Tempest.risk_band or &quot;neutral&quot;
-- Preference when the wielded weapon supports both slashing and blunt (see weapon_can_*).
Tempest.weapon_line = Tempest.weapon_line or &quot;blunt&quot;
-- weapon_can_slash / weapon_can_blunt: nil until triggers fire or you wreset (see TempestCombat.xml).
Tempest.character_name = Tempest.character_name or &quot;&quot;

function Tempest.set_character_name(name)
  Tempest.character_name = tostring(name or &quot;&quot;):match(&quot;^%s*(.-)%s*<name>target helper</name>
        <packageName></packageName>
        <script>Tempest = Tempest or {}

Tempest.target = Tempest.target or &quot;&quot;
Tempest.risk_band = Tempest.risk_band or &quot;neutral&quot;
Tempest.weapon_line = Tempest.weapon_line or &quot;blunt&quot;
Tempest.character_name = Tempest.character_name or &quot;&quot;

function Tempest.set_character_name(name)
  Tempest.character_name = tostring(name or &quot;&quot;):match(&quot;^%s*(.-)%s*<name>target helper</name>
        <packageName></packageName>
        <script>Tempest = Tempest or {}

Tempest.target = Tempest.target or &quot;&quot;
Tempest.risk_band = Tempest.risk_band or &quot;neutral&quot;
Tempest.weapon_line = Tempest.weapon_line or &quot;blunt&quot;
Tempest.character_name = Tempest.character_name or &quot;&quot;

function Tempest.set_character_name(name)
  Tempest.character_name = tostring(name or &quot;&quot;):match(&quot;^%s*(.-)%s*<name>target helper</name>
        <packageName></packageName>
        <script>Tempest = Tempest or {}

Tempest.target = Tempest.target or &quot;&quot;
Tempest.risk_band = Tempest.risk_band or &quot;neutral&quot;

function Tempest.set_target(name)
  name = tostring(name or &quot;&quot;):match(&quot;^%s*(.-)%s*$&quot;)

  if name == &quot;&quot; then
    cecho(&quot;&lt;red&gt;[Tempest] No target supplied.\n&quot;)
    return false
  end

  Tempest.target = name

  -- Compatibility with older/simple aliases that may expect a global target.
  target = name

  cecho(&quot;&lt;cyan&gt;[Tempest] Target set: &lt;white&gt;&quot; .. name .. &quot;\n&quot;)
  return true
end

function Tempest.get_target()
  if Tempest.target and Tempest.target ~= &quot;&quot; then
    return Tempest.target
  end

  if target and target ~= &quot;&quot; then
    return target
  end

  return nil
end

function Tempest.require_target()
  local tgt = Tempest.get_target()

  if not tgt or tgt == &quot;&quot; then
    cecho(&quot;&lt;red&gt;[Tempest] No target set. Use: tt &lt;name&gt;\n&quot;)
    return nil
  end

  return tgt
end

function Tempest.normalize_favors(value, silent)
  local text = tostring(value or &quot;&quot;):match(&quot;^%s*(.-)%s*$&quot;):lower()
  if text == &quot;&quot; then
    return nil
  end

  if text == &quot;min&quot; or text == &quot;max&quot; or text:match(&quot;^%d+$&quot;) then
    return text
  end

  if not silent then
    cecho(&quot;&lt;red&gt;[Tempest] Invalid favors value. Use: min, max, or a number.\n&quot;)
  end

  return nil
end

function Tempest.send_pray(gift, target_name, favors)
  local cmd = &quot;pray &quot; .. gift
  local target_text = tostring(target_name or &quot;&quot;):match(&quot;^%s*(.-)%s*$&quot;)
  if target_text ~= &quot;&quot; then
    cmd = cmd .. &quot; &quot; .. target_text
  end

  local favors_text = Tempest.normalize_favors(favors)
  if favors_text then
    cmd = cmd .. &quot; &quot; .. favors_text
  end

  send(cmd)
end

local function resolve_attack_target(explicit_target)
  local text = tostring(explicit_target or &quot;&quot;):match(&quot;^%s*(.-)%s*$&quot;)
  if text ~= &quot;&quot; then
    return text
  end
  return Tempest.get_target()
end

function Tempest.send_basic_attack(attack, explicit_target)
  local target_name = resolve_attack_target(explicit_target)
  if target_name and target_name ~= &quot;&quot; then
    send(attack .. &quot; &quot; .. target_name)
  else
    send(attack)
  end
end

function Tempest.set_risk_band(value)
  local band = tostring(value or &quot;&quot;):lower():match(&quot;^%s*(.-)%s*$&quot;)
  local valid = {
    good = true,
    neutral = true,
    bad = true,
    critical = true,
  }

  if not valid[band] then
    cecho(&quot;&lt;red&gt;[Tempest] Invalid risk band. Use: good, neutral, bad, critical.\n&quot;)
    return false
  end

  Tempest.risk_band = band
  cecho(&quot;&lt;cyan&gt;[Tempest] Risk band set: &lt;white&gt;&quot; .. band .. &quot;\n&quot;)
  return true
end

function Tempest.send_melee(kind, explicit_target)
  local key = tostring(kind or &quot;&quot;):lower()
  local attack = nil

  if key == &quot;safe&quot; then
    attack = &quot;pound&quot;
  elseif key == &quot;mid&quot; then
    attack = &quot;crush&quot;
  elseif key == &quot;heavy&quot; then
    attack = &quot;smash&quot;
  elseif key == &quot;auto&quot; then
    if Tempest.risk_band == &quot;good&quot; then
      attack = &quot;smash&quot;
    elseif Tempest.risk_band == &quot;bad&quot; or Tempest.risk_band == &quot;critical&quot; then
      attack = &quot;pound&quot;
    else
      attack = &quot;crush&quot;
    end
  else
    cecho(&quot;&lt;red&gt;[Tempest] Unknown melee mode. Use: safe, mid, heavy, auto.\n&quot;)
    return false
  end

  Tempest.send_basic_attack(attack, explicit_target)
  return true
end</script>
        <eventHandlerList />quot;) or &quot;&quot;
  if Tempest.character_name ~= &quot;&quot; then
    cecho(&quot;&lt;cyan&gt;[Tempest] Character name set for self-checks: &lt;white&gt;&quot; .. Tempest.character_name .. &quot;\n&quot;)
  end
end

function Tempest.get_self_name()
  local n = Tempest.character_name or &quot;&quot;
  if n ~= &quot;&quot; then
    return n
  end
  if type(gmcp) == &quot;table&quot; and gmcp.Char and gmcp.Char.Name then
    return tostring(gmcp.Char.Name)
  end
  return &quot;&quot;
end

local function norm_lower(s)
  return tostring(s or &quot;&quot;):match(&quot;^%s*(.-)%s*<name>target helper</name>
        <packageName></packageName>
        <script>Tempest = Tempest or {}

Tempest.target = Tempest.target or &quot;&quot;
Tempest.risk_band = Tempest.risk_band or &quot;neutral&quot;
-- Preference when the wielded weapon supports both slashing and blunt (see weapon_can_*).
Tempest.weapon_line = Tempest.weapon_line or &quot;blunt&quot;
-- weapon_can_slash / weapon_can_blunt: nil until triggers fire or you wreset (see TempestCombat.xml).
Tempest.character_name = Tempest.character_name or &quot;&quot;

function Tempest.set_character_name(name)
  Tempest.character_name = tostring(name or &quot;&quot;):match(&quot;^%s*(.-)%s*<name>target helper</name>
        <packageName></packageName>
        <script>Tempest = Tempest or {}

Tempest.target = Tempest.target or &quot;&quot;
Tempest.risk_band = Tempest.risk_band or &quot;neutral&quot;
-- Preference when the wielded weapon supports both slashing and blunt (see weapon_can_*).
Tempest.weapon_line = Tempest.weapon_line or &quot;blunt&quot;
-- weapon_can_slash / weapon_can_blunt: nil until triggers fire or you wreset (see TempestCombat.xml).
Tempest.character_name = Tempest.character_name or &quot;&quot;

function Tempest.set_character_name(name)
  Tempest.character_name = tostring(name or &quot;&quot;):match(&quot;^%s*(.-)%s*<name>target helper</name>
        <packageName></packageName>
        <script>Tempest = Tempest or {}

Tempest.target = Tempest.target or &quot;&quot;
Tempest.risk_band = Tempest.risk_band or &quot;neutral&quot;
Tempest.weapon_line = Tempest.weapon_line or &quot;blunt&quot;
Tempest.character_name = Tempest.character_name or &quot;&quot;

function Tempest.set_character_name(name)
  Tempest.character_name = tostring(name or &quot;&quot;):match(&quot;^%s*(.-)%s*<name>target helper</name>
        <packageName></packageName>
        <script>Tempest = Tempest or {}

Tempest.target = Tempest.target or &quot;&quot;
Tempest.risk_band = Tempest.risk_band or &quot;neutral&quot;
Tempest.weapon_line = Tempest.weapon_line or &quot;blunt&quot;
Tempest.character_name = Tempest.character_name or &quot;&quot;

function Tempest.set_character_name(name)
  Tempest.character_name = tostring(name or &quot;&quot;):match(&quot;^%s*(.-)%s*<name>target helper</name>
        <packageName></packageName>
        <script>Tempest = Tempest or {}

Tempest.target = Tempest.target or &quot;&quot;
Tempest.risk_band = Tempest.risk_band or &quot;neutral&quot;

function Tempest.set_target(name)
  name = tostring(name or &quot;&quot;):match(&quot;^%s*(.-)%s*$&quot;)

  if name == &quot;&quot; then
    cecho(&quot;&lt;red&gt;[Tempest] No target supplied.\n&quot;)
    return false
  end

  Tempest.target = name

  -- Compatibility with older/simple aliases that may expect a global target.
  target = name

  cecho(&quot;&lt;cyan&gt;[Tempest] Target set: &lt;white&gt;&quot; .. name .. &quot;\n&quot;)
  return true
end

function Tempest.get_target()
  if Tempest.target and Tempest.target ~= &quot;&quot; then
    return Tempest.target
  end

  if target and target ~= &quot;&quot; then
    return target
  end

  return nil
end

function Tempest.require_target()
  local tgt = Tempest.get_target()

  if not tgt or tgt == &quot;&quot; then
    cecho(&quot;&lt;red&gt;[Tempest] No target set. Use: tt &lt;name&gt;\n&quot;)
    return nil
  end

  return tgt
end

function Tempest.normalize_favors(value, silent)
  local text = tostring(value or &quot;&quot;):match(&quot;^%s*(.-)%s*$&quot;):lower()
  if text == &quot;&quot; then
    return nil
  end

  if text == &quot;min&quot; or text == &quot;max&quot; or text:match(&quot;^%d+$&quot;) then
    return text
  end

  if not silent then
    cecho(&quot;&lt;red&gt;[Tempest] Invalid favors value. Use: min, max, or a number.\n&quot;)
  end

  return nil
end

function Tempest.send_pray(gift, target_name, favors)
  local cmd = &quot;pray &quot; .. gift
  local target_text = tostring(target_name or &quot;&quot;):match(&quot;^%s*(.-)%s*$&quot;)
  if target_text ~= &quot;&quot; then
    cmd = cmd .. &quot; &quot; .. target_text
  end

  local favors_text = Tempest.normalize_favors(favors)
  if favors_text then
    cmd = cmd .. &quot; &quot; .. favors_text
  end

  send(cmd)
end

local function resolve_attack_target(explicit_target)
  local text = tostring(explicit_target or &quot;&quot;):match(&quot;^%s*(.-)%s*$&quot;)
  if text ~= &quot;&quot; then
    return text
  end
  return Tempest.get_target()
end

function Tempest.send_basic_attack(attack, explicit_target)
  local target_name = resolve_attack_target(explicit_target)
  if target_name and target_name ~= &quot;&quot; then
    send(attack .. &quot; &quot; .. target_name)
  else
    send(attack)
  end
end

function Tempest.set_risk_band(value)
  local band = tostring(value or &quot;&quot;):lower():match(&quot;^%s*(.-)%s*$&quot;)
  local valid = {
    good = true,
    neutral = true,
    bad = true,
    critical = true,
  }

  if not valid[band] then
    cecho(&quot;&lt;red&gt;[Tempest] Invalid risk band. Use: good, neutral, bad, critical.\n&quot;)
    return false
  end

  Tempest.risk_band = band
  cecho(&quot;&lt;cyan&gt;[Tempest] Risk band set: &lt;white&gt;&quot; .. band .. &quot;\n&quot;)
  return true
end

function Tempest.send_melee(kind, explicit_target)
  local key = tostring(kind or &quot;&quot;):lower()
  local attack = nil

  if key == &quot;safe&quot; then
    attack = &quot;pound&quot;
  elseif key == &quot;mid&quot; then
    attack = &quot;crush&quot;
  elseif key == &quot;heavy&quot; then
    attack = &quot;smash&quot;
  elseif key == &quot;auto&quot; then
    if Tempest.risk_band == &quot;good&quot; then
      attack = &quot;smash&quot;
    elseif Tempest.risk_band == &quot;bad&quot; or Tempest.risk_band == &quot;critical&quot; then
      attack = &quot;pound&quot;
    else
      attack = &quot;crush&quot;
    end
  else
    cecho(&quot;&lt;red&gt;[Tempest] Unknown melee mode. Use: safe, mid, heavy, auto.\n&quot;)
    return false
  end

  Tempest.send_basic_attack(attack, explicit_target)
  return true
end</script>
        <eventHandlerList />quot;):lower()
end

local function is_target_self(name)
  local self_name = Tempest.get_self_name()
  if self_name == &quot;&quot; or name == &quot;&quot; then
    return false
  end
  return norm_lower(name) == norm_lower(self_name)
end

function Tempest.set_weapon_line(line)
  local key = norm_lower(line)
  if key ~= &quot;sharp&quot; and key ~= &quot;blunt&quot; then
    cecho(&quot;&lt;red&gt;[Tempest] weapon_line must be sharp or blunt.\n&quot;)
    return false
  end
  Tempest.weapon_line = key
  cecho(&quot;&lt;cyan&gt;[Tempest] Melee style preference (when weapon allows both): &lt;white&gt;&quot; .. key .. &quot;\n&quot;)
  return true
end

function Tempest.reset_weapon_capabilities()
  Tempest.weapon_can_slash = nil
  Tempest.weapon_can_blunt = nil
  cecho(&quot;&lt;cyan&gt;[Tempest] Weapon style flags cleared (probe weapon again).\n&quot;)
end

function Tempest.note_slashing_weapon()
  Tempest.weapon_can_slash = true
end

function Tempest.note_blunt_weapon()
  Tempest.weapon_can_blunt = true
end

--- Effective line for verb choice: single-style from probes, or weapon_line when both apply.
function Tempest.resolve_effective_weapon_line()
  local s = Tempest.weapon_can_slash
  local b = Tempest.weapon_can_blunt
  if s == true and b ~= true then
    return &quot;sharp&quot;
  end
  if b == true and s ~= true then
    return &quot;blunt&quot;
  end
  if s == true and b == true then
    return Tempest.weapon_line or &quot;blunt&quot;
  end
  return Tempest.weapon_line or &quot;blunt&quot;
end

function Tempest.melee_verb_for_tier(tier)
  local t = tostring(tier or &quot;&quot;):lower()
  local wl = Tempest.resolve_effective_weapon_line()
  if t == &quot;safe&quot; then
    return wl == &quot;sharp&quot; and &quot;slash&quot; or &quot;pound&quot;
  end
  if t == &quot;mid&quot; then
    return wl == &quot;sharp&quot; and &quot;slice&quot; or &quot;crush&quot;
  end
  if t == &quot;heavy&quot; then
    return wl == &quot;sharp&quot; and &quot;cleave&quot; or &quot;smash&quot;
  end
  return nil
end

function Tempest.set_target(name)
  name = tostring(name or &quot;&quot;):match(&quot;^%s*(.-)%s*<name>target helper</name>
        <packageName></packageName>
        <script>Tempest = Tempest or {}

Tempest.target = Tempest.target or &quot;&quot;
Tempest.risk_band = Tempest.risk_band or &quot;neutral&quot;
-- Preference when the wielded weapon supports both slashing and blunt (see weapon_can_*).
Tempest.weapon_line = Tempest.weapon_line or &quot;blunt&quot;
-- weapon_can_slash / weapon_can_blunt: nil until triggers fire or you wreset (see TempestCombat.xml).
Tempest.character_name = Tempest.character_name or &quot;&quot;

function Tempest.set_character_name(name)
  Tempest.character_name = tostring(name or &quot;&quot;):match(&quot;^%s*(.-)%s*<name>target helper</name>
        <packageName></packageName>
        <script>Tempest = Tempest or {}

Tempest.target = Tempest.target or &quot;&quot;
Tempest.risk_band = Tempest.risk_band or &quot;neutral&quot;
-- Preference when the wielded weapon supports both slashing and blunt (see weapon_can_*).
Tempest.weapon_line = Tempest.weapon_line or &quot;blunt&quot;
-- weapon_can_slash / weapon_can_blunt: nil until triggers fire or you wreset (see TempestCombat.xml).
Tempest.character_name = Tempest.character_name or &quot;&quot;

function Tempest.set_character_name(name)
  Tempest.character_name = tostring(name or &quot;&quot;):match(&quot;^%s*(.-)%s*<name>target helper</name>
        <packageName></packageName>
        <script>Tempest = Tempest or {}

Tempest.target = Tempest.target or &quot;&quot;
Tempest.risk_band = Tempest.risk_band or &quot;neutral&quot;
Tempest.weapon_line = Tempest.weapon_line or &quot;blunt&quot;
Tempest.character_name = Tempest.character_name or &quot;&quot;

function Tempest.set_character_name(name)
  Tempest.character_name = tostring(name or &quot;&quot;):match(&quot;^%s*(.-)%s*<name>target helper</name>
        <packageName></packageName>
        <script>Tempest = Tempest or {}

Tempest.target = Tempest.target or &quot;&quot;
Tempest.risk_band = Tempest.risk_band or &quot;neutral&quot;
Tempest.weapon_line = Tempest.weapon_line or &quot;blunt&quot;
Tempest.character_name = Tempest.character_name or &quot;&quot;

function Tempest.set_character_name(name)
  Tempest.character_name = tostring(name or &quot;&quot;):match(&quot;^%s*(.-)%s*<name>target helper</name>
        <packageName></packageName>
        <script>Tempest = Tempest or {}

Tempest.target = Tempest.target or &quot;&quot;
Tempest.risk_band = Tempest.risk_band or &quot;neutral&quot;

function Tempest.set_target(name)
  name = tostring(name or &quot;&quot;):match(&quot;^%s*(.-)%s*$&quot;)

  if name == &quot;&quot; then
    cecho(&quot;&lt;red&gt;[Tempest] No target supplied.\n&quot;)
    return false
  end

  Tempest.target = name

  -- Compatibility with older/simple aliases that may expect a global target.
  target = name

  cecho(&quot;&lt;cyan&gt;[Tempest] Target set: &lt;white&gt;&quot; .. name .. &quot;\n&quot;)
  return true
end

function Tempest.get_target()
  if Tempest.target and Tempest.target ~= &quot;&quot; then
    return Tempest.target
  end

  if target and target ~= &quot;&quot; then
    return target
  end

  return nil
end

function Tempest.require_target()
  local tgt = Tempest.get_target()

  if not tgt or tgt == &quot;&quot; then
    cecho(&quot;&lt;red&gt;[Tempest] No target set. Use: tt &lt;name&gt;\n&quot;)
    return nil
  end

  return tgt
end

function Tempest.normalize_favors(value, silent)
  local text = tostring(value or &quot;&quot;):match(&quot;^%s*(.-)%s*$&quot;):lower()
  if text == &quot;&quot; then
    return nil
  end

  if text == &quot;min&quot; or text == &quot;max&quot; or text:match(&quot;^%d+$&quot;) then
    return text
  end

  if not silent then
    cecho(&quot;&lt;red&gt;[Tempest] Invalid favors value. Use: min, max, or a number.\n&quot;)
  end

  return nil
end

function Tempest.send_pray(gift, target_name, favors)
  local cmd = &quot;pray &quot; .. gift
  local target_text = tostring(target_name or &quot;&quot;):match(&quot;^%s*(.-)%s*$&quot;)
  if target_text ~= &quot;&quot; then
    cmd = cmd .. &quot; &quot; .. target_text
  end

  local favors_text = Tempest.normalize_favors(favors)
  if favors_text then
    cmd = cmd .. &quot; &quot; .. favors_text
  end

  send(cmd)
end

local function resolve_attack_target(explicit_target)
  local text = tostring(explicit_target or &quot;&quot;):match(&quot;^%s*(.-)%s*$&quot;)
  if text ~= &quot;&quot; then
    return text
  end
  return Tempest.get_target()
end

function Tempest.send_basic_attack(attack, explicit_target)
  local target_name = resolve_attack_target(explicit_target)
  if target_name and target_name ~= &quot;&quot; then
    send(attack .. &quot; &quot; .. target_name)
  else
    send(attack)
  end
end

function Tempest.set_risk_band(value)
  local band = tostring(value or &quot;&quot;):lower():match(&quot;^%s*(.-)%s*$&quot;)
  local valid = {
    good = true,
    neutral = true,
    bad = true,
    critical = true,
  }

  if not valid[band] then
    cecho(&quot;&lt;red&gt;[Tempest] Invalid risk band. Use: good, neutral, bad, critical.\n&quot;)
    return false
  end

  Tempest.risk_band = band
  cecho(&quot;&lt;cyan&gt;[Tempest] Risk band set: &lt;white&gt;&quot; .. band .. &quot;\n&quot;)
  return true
end

function Tempest.send_melee(kind, explicit_target)
  local key = tostring(kind or &quot;&quot;):lower()
  local attack = nil

  if key == &quot;safe&quot; then
    attack = &quot;pound&quot;
  elseif key == &quot;mid&quot; then
    attack = &quot;crush&quot;
  elseif key == &quot;heavy&quot; then
    attack = &quot;smash&quot;
  elseif key == &quot;auto&quot; then
    if Tempest.risk_band == &quot;good&quot; then
      attack = &quot;smash&quot;
    elseif Tempest.risk_band == &quot;bad&quot; or Tempest.risk_band == &quot;critical&quot; then
      attack = &quot;pound&quot;
    else
      attack = &quot;crush&quot;
    end
  else
    cecho(&quot;&lt;red&gt;[Tempest] Unknown melee mode. Use: safe, mid, heavy, auto.\n&quot;)
    return false
  end

  Tempest.send_basic_attack(attack, explicit_target)
  return true
end</script>
        <eventHandlerList />quot;)

  if name == &quot;&quot; then
    cecho(&quot;&lt;red&gt;[Tempest] No target supplied.\n&quot;)
    return false
  end

  Tempest.target = name

  -- Compatibility with older/simple aliases that may expect a global target.
  target = name

  cecho(&quot;&lt;cyan&gt;[Tempest] Target set: &lt;white&gt;&quot; .. name .. &quot;\n&quot;)
  return true
end

function Tempest.get_target()
  if Tempest.target and Tempest.target ~= &quot;&quot; then
    return Tempest.target
  end

  if target and target ~= &quot;&quot; then
    return target
  end

  return nil
end

function Tempest.require_target()
  local tgt = Tempest.get_target()

  if not tgt or tgt == &quot;&quot; then
    cecho(&quot;&lt;red&gt;[Tempest] No target set. Use: tt &lt;name&gt;\n&quot;)
    return nil
  end

  if is_target_self(tgt) then
    cecho(&quot;&lt;red&gt;[Tempest] Target is your character name; clear or change target with tt.\n&quot;)
    return nil
  end

  return tgt
end

function Tempest.normalize_favors(value, silent)
  local text = tostring(value or &quot;&quot;):match(&quot;^%s*(.-)%s*<name>target helper</name>
        <packageName></packageName>
        <script>Tempest = Tempest or {}

Tempest.target = Tempest.target or &quot;&quot;
Tempest.risk_band = Tempest.risk_band or &quot;neutral&quot;
-- Preference when the wielded weapon supports both slashing and blunt (see weapon_can_*).
Tempest.weapon_line = Tempest.weapon_line or &quot;blunt&quot;
-- weapon_can_slash / weapon_can_blunt: nil until triggers fire or you wreset (see TempestCombat.xml).
Tempest.character_name = Tempest.character_name or &quot;&quot;

function Tempest.set_character_name(name)
  Tempest.character_name = tostring(name or &quot;&quot;):match(&quot;^%s*(.-)%s*<name>target helper</name>
        <packageName></packageName>
        <script>Tempest = Tempest or {}

Tempest.target = Tempest.target or &quot;&quot;
Tempest.risk_band = Tempest.risk_band or &quot;neutral&quot;
-- Preference when the wielded weapon supports both slashing and blunt (see weapon_can_*).
Tempest.weapon_line = Tempest.weapon_line or &quot;blunt&quot;
-- weapon_can_slash / weapon_can_blunt: nil until triggers fire or you wreset (see TempestCombat.xml).
Tempest.character_name = Tempest.character_name or &quot;&quot;

function Tempest.set_character_name(name)
  Tempest.character_name = tostring(name or &quot;&quot;):match(&quot;^%s*(.-)%s*<name>target helper</name>
        <packageName></packageName>
        <script>Tempest = Tempest or {}

Tempest.target = Tempest.target or &quot;&quot;
Tempest.risk_band = Tempest.risk_band or &quot;neutral&quot;
Tempest.weapon_line = Tempest.weapon_line or &quot;blunt&quot;
Tempest.character_name = Tempest.character_name or &quot;&quot;

function Tempest.set_character_name(name)
  Tempest.character_name = tostring(name or &quot;&quot;):match(&quot;^%s*(.-)%s*<name>target helper</name>
        <packageName></packageName>
        <script>Tempest = Tempest or {}

Tempest.target = Tempest.target or &quot;&quot;
Tempest.risk_band = Tempest.risk_band or &quot;neutral&quot;
Tempest.weapon_line = Tempest.weapon_line or &quot;blunt&quot;
Tempest.character_name = Tempest.character_name or &quot;&quot;

function Tempest.set_character_name(name)
  Tempest.character_name = tostring(name or &quot;&quot;):match(&quot;^%s*(.-)%s*<name>target helper</name>
        <packageName></packageName>
        <script>Tempest = Tempest or {}

Tempest.target = Tempest.target or &quot;&quot;
Tempest.risk_band = Tempest.risk_band or &quot;neutral&quot;

function Tempest.set_target(name)
  name = tostring(name or &quot;&quot;):match(&quot;^%s*(.-)%s*$&quot;)

  if name == &quot;&quot; then
    cecho(&quot;&lt;red&gt;[Tempest] No target supplied.\n&quot;)
    return false
  end

  Tempest.target = name

  -- Compatibility with older/simple aliases that may expect a global target.
  target = name

  cecho(&quot;&lt;cyan&gt;[Tempest] Target set: &lt;white&gt;&quot; .. name .. &quot;\n&quot;)
  return true
end

function Tempest.get_target()
  if Tempest.target and Tempest.target ~= &quot;&quot; then
    return Tempest.target
  end

  if target and target ~= &quot;&quot; then
    return target
  end

  return nil
end

function Tempest.require_target()
  local tgt = Tempest.get_target()

  if not tgt or tgt == &quot;&quot; then
    cecho(&quot;&lt;red&gt;[Tempest] No target set. Use: tt &lt;name&gt;\n&quot;)
    return nil
  end

  return tgt
end

function Tempest.normalize_favors(value, silent)
  local text = tostring(value or &quot;&quot;):match(&quot;^%s*(.-)%s*$&quot;):lower()
  if text == &quot;&quot; then
    return nil
  end

  if text == &quot;min&quot; or text == &quot;max&quot; or text:match(&quot;^%d+$&quot;) then
    return text
  end

  if not silent then
    cecho(&quot;&lt;red&gt;[Tempest] Invalid favors value. Use: min, max, or a number.\n&quot;)
  end

  return nil
end

function Tempest.send_pray(gift, target_name, favors)
  local cmd = &quot;pray &quot; .. gift
  local target_text = tostring(target_name or &quot;&quot;):match(&quot;^%s*(.-)%s*$&quot;)
  if target_text ~= &quot;&quot; then
    cmd = cmd .. &quot; &quot; .. target_text
  end

  local favors_text = Tempest.normalize_favors(favors)
  if favors_text then
    cmd = cmd .. &quot; &quot; .. favors_text
  end

  send(cmd)
end

local function resolve_attack_target(explicit_target)
  local text = tostring(explicit_target or &quot;&quot;):match(&quot;^%s*(.-)%s*$&quot;)
  if text ~= &quot;&quot; then
    return text
  end
  return Tempest.get_target()
end

function Tempest.send_basic_attack(attack, explicit_target)
  local target_name = resolve_attack_target(explicit_target)
  if target_name and target_name ~= &quot;&quot; then
    send(attack .. &quot; &quot; .. target_name)
  else
    send(attack)
  end
end

function Tempest.set_risk_band(value)
  local band = tostring(value or &quot;&quot;):lower():match(&quot;^%s*(.-)%s*$&quot;)
  local valid = {
    good = true,
    neutral = true,
    bad = true,
    critical = true,
  }

  if not valid[band] then
    cecho(&quot;&lt;red&gt;[Tempest] Invalid risk band. Use: good, neutral, bad, critical.\n&quot;)
    return false
  end

  Tempest.risk_band = band
  cecho(&quot;&lt;cyan&gt;[Tempest] Risk band set: &lt;white&gt;&quot; .. band .. &quot;\n&quot;)
  return true
end

function Tempest.send_melee(kind, explicit_target)
  local key = tostring(kind or &quot;&quot;):lower()
  local attack = nil

  if key == &quot;safe&quot; then
    attack = &quot;pound&quot;
  elseif key == &quot;mid&quot; then
    attack = &quot;crush&quot;
  elseif key == &quot;heavy&quot; then
    attack = &quot;smash&quot;
  elseif key == &quot;auto&quot; then
    if Tempest.risk_band == &quot;good&quot; then
      attack = &quot;smash&quot;
    elseif Tempest.risk_band == &quot;bad&quot; or Tempest.risk_band == &quot;critical&quot; then
      attack = &quot;pound&quot;
    else
      attack = &quot;crush&quot;
    end
  else
    cecho(&quot;&lt;red&gt;[Tempest] Unknown melee mode. Use: safe, mid, heavy, auto.\n&quot;)
    return false
  end

  Tempest.send_basic_attack(attack, explicit_target)
  return true
end</script>
        <eventHandlerList />quot;):lower()
  if text == &quot;&quot; then
    return nil
  end

  if text == &quot;min&quot; or text == &quot;max&quot; or text:match(&quot;^%d+<name>target helper</name>
        <packageName></packageName>
        <script>Tempest = Tempest or {}

Tempest.target = Tempest.target or &quot;&quot;
Tempest.risk_band = Tempest.risk_band or &quot;neutral&quot;
-- Preference when the wielded weapon supports both slashing and blunt (see weapon_can_*).
Tempest.weapon_line = Tempest.weapon_line or &quot;blunt&quot;
-- weapon_can_slash / weapon_can_blunt: nil until triggers fire or you wreset (see TempestCombat.xml).
Tempest.character_name = Tempest.character_name or &quot;&quot;

function Tempest.set_character_name(name)
  Tempest.character_name = tostring(name or &quot;&quot;):match(&quot;^%s*(.-)%s*<name>target helper</name>
        <packageName></packageName>
        <script>Tempest = Tempest or {}

Tempest.target = Tempest.target or &quot;&quot;
Tempest.risk_band = Tempest.risk_band or &quot;neutral&quot;
-- Preference when the wielded weapon supports both slashing and blunt (see weapon_can_*).
Tempest.weapon_line = Tempest.weapon_line or &quot;blunt&quot;
-- weapon_can_slash / weapon_can_blunt: nil until triggers fire or you wreset (see TempestCombat.xml).
Tempest.character_name = Tempest.character_name or &quot;&quot;

function Tempest.set_character_name(name)
  Tempest.character_name = tostring(name or &quot;&quot;):match(&quot;^%s*(.-)%s*<name>target helper</name>
        <packageName></packageName>
        <script>Tempest = Tempest or {}

Tempest.target = Tempest.target or &quot;&quot;
Tempest.risk_band = Tempest.risk_band or &quot;neutral&quot;
Tempest.weapon_line = Tempest.weapon_line or &quot;blunt&quot;
Tempest.character_name = Tempest.character_name or &quot;&quot;

function Tempest.set_character_name(name)
  Tempest.character_name = tostring(name or &quot;&quot;):match(&quot;^%s*(.-)%s*<name>target helper</name>
        <packageName></packageName>
        <script>Tempest = Tempest or {}

Tempest.target = Tempest.target or &quot;&quot;
Tempest.risk_band = Tempest.risk_band or &quot;neutral&quot;
Tempest.weapon_line = Tempest.weapon_line or &quot;blunt&quot;
Tempest.character_name = Tempest.character_name or &quot;&quot;

function Tempest.set_character_name(name)
  Tempest.character_name = tostring(name or &quot;&quot;):match(&quot;^%s*(.-)%s*<name>target helper</name>
        <packageName></packageName>
        <script>Tempest = Tempest or {}

Tempest.target = Tempest.target or &quot;&quot;
Tempest.risk_band = Tempest.risk_band or &quot;neutral&quot;

function Tempest.set_target(name)
  name = tostring(name or &quot;&quot;):match(&quot;^%s*(.-)%s*$&quot;)

  if name == &quot;&quot; then
    cecho(&quot;&lt;red&gt;[Tempest] No target supplied.\n&quot;)
    return false
  end

  Tempest.target = name

  -- Compatibility with older/simple aliases that may expect a global target.
  target = name

  cecho(&quot;&lt;cyan&gt;[Tempest] Target set: &lt;white&gt;&quot; .. name .. &quot;\n&quot;)
  return true
end

function Tempest.get_target()
  if Tempest.target and Tempest.target ~= &quot;&quot; then
    return Tempest.target
  end

  if target and target ~= &quot;&quot; then
    return target
  end

  return nil
end

function Tempest.require_target()
  local tgt = Tempest.get_target()

  if not tgt or tgt == &quot;&quot; then
    cecho(&quot;&lt;red&gt;[Tempest] No target set. Use: tt &lt;name&gt;\n&quot;)
    return nil
  end

  return tgt
end

function Tempest.normalize_favors(value, silent)
  local text = tostring(value or &quot;&quot;):match(&quot;^%s*(.-)%s*$&quot;):lower()
  if text == &quot;&quot; then
    return nil
  end

  if text == &quot;min&quot; or text == &quot;max&quot; or text:match(&quot;^%d+$&quot;) then
    return text
  end

  if not silent then
    cecho(&quot;&lt;red&gt;[Tempest] Invalid favors value. Use: min, max, or a number.\n&quot;)
  end

  return nil
end

function Tempest.send_pray(gift, target_name, favors)
  local cmd = &quot;pray &quot; .. gift
  local target_text = tostring(target_name or &quot;&quot;):match(&quot;^%s*(.-)%s*$&quot;)
  if target_text ~= &quot;&quot; then
    cmd = cmd .. &quot; &quot; .. target_text
  end

  local favors_text = Tempest.normalize_favors(favors)
  if favors_text then
    cmd = cmd .. &quot; &quot; .. favors_text
  end

  send(cmd)
end

local function resolve_attack_target(explicit_target)
  local text = tostring(explicit_target or &quot;&quot;):match(&quot;^%s*(.-)%s*$&quot;)
  if text ~= &quot;&quot; then
    return text
  end
  return Tempest.get_target()
end

function Tempest.send_basic_attack(attack, explicit_target)
  local target_name = resolve_attack_target(explicit_target)
  if target_name and target_name ~= &quot;&quot; then
    send(attack .. &quot; &quot; .. target_name)
  else
    send(attack)
  end
end

function Tempest.set_risk_band(value)
  local band = tostring(value or &quot;&quot;):lower():match(&quot;^%s*(.-)%s*$&quot;)
  local valid = {
    good = true,
    neutral = true,
    bad = true,
    critical = true,
  }

  if not valid[band] then
    cecho(&quot;&lt;red&gt;[Tempest] Invalid risk band. Use: good, neutral, bad, critical.\n&quot;)
    return false
  end

  Tempest.risk_band = band
  cecho(&quot;&lt;cyan&gt;[Tempest] Risk band set: &lt;white&gt;&quot; .. band .. &quot;\n&quot;)
  return true
end

function Tempest.send_melee(kind, explicit_target)
  local key = tostring(kind or &quot;&quot;):lower()
  local attack = nil

  if key == &quot;safe&quot; then
    attack = &quot;pound&quot;
  elseif key == &quot;mid&quot; then
    attack = &quot;crush&quot;
  elseif key == &quot;heavy&quot; then
    attack = &quot;smash&quot;
  elseif key == &quot;auto&quot; then
    if Tempest.risk_band == &quot;good&quot; then
      attack = &quot;smash&quot;
    elseif Tempest.risk_band == &quot;bad&quot; or Tempest.risk_band == &quot;critical&quot; then
      attack = &quot;pound&quot;
    else
      attack = &quot;crush&quot;
    end
  else
    cecho(&quot;&lt;red&gt;[Tempest] Unknown melee mode. Use: safe, mid, heavy, auto.\n&quot;)
    return false
  end

  Tempest.send_basic_attack(attack, explicit_target)
  return true
end</script>
        <eventHandlerList />quot;) then
    return text
  end

  if not silent then
    cecho(&quot;&lt;red&gt;[Tempest] Invalid favors value. Use: min, max, or a number.\n&quot;)
  end
  return nil
end

function Tempest.send_pray(gift, target_name, favors)
  local cmd = &quot;pray &quot; .. gift
  local target_text = tostring(target_name or &quot;&quot;):match(&quot;^%s*(.-)%s*<name>target helper</name>
        <packageName></packageName>
        <script>Tempest = Tempest or {}

Tempest.target = Tempest.target or &quot;&quot;
Tempest.risk_band = Tempest.risk_band or &quot;neutral&quot;
-- Preference when the wielded weapon supports both slashing and blunt (see weapon_can_*).
Tempest.weapon_line = Tempest.weapon_line or &quot;blunt&quot;
-- weapon_can_slash / weapon_can_blunt: nil until triggers fire or you wreset (see TempestCombat.xml).
Tempest.character_name = Tempest.character_name or &quot;&quot;

function Tempest.set_character_name(name)
  Tempest.character_name = tostring(name or &quot;&quot;):match(&quot;^%s*(.-)%s*<name>target helper</name>
        <packageName></packageName>
        <script>Tempest = Tempest or {}

Tempest.target = Tempest.target or &quot;&quot;
Tempest.risk_band = Tempest.risk_band or &quot;neutral&quot;
-- Preference when the wielded weapon supports both slashing and blunt (see weapon_can_*).
Tempest.weapon_line = Tempest.weapon_line or &quot;blunt&quot;
-- weapon_can_slash / weapon_can_blunt: nil until triggers fire or you wreset (see TempestCombat.xml).
Tempest.character_name = Tempest.character_name or &quot;&quot;

function Tempest.set_character_name(name)
  Tempest.character_name = tostring(name or &quot;&quot;):match(&quot;^%s*(.-)%s*<name>target helper</name>
        <packageName></packageName>
        <script>Tempest = Tempest or {}

Tempest.target = Tempest.target or &quot;&quot;
Tempest.risk_band = Tempest.risk_band or &quot;neutral&quot;
Tempest.weapon_line = Tempest.weapon_line or &quot;blunt&quot;
Tempest.character_name = Tempest.character_name or &quot;&quot;

function Tempest.set_character_name(name)
  Tempest.character_name = tostring(name or &quot;&quot;):match(&quot;^%s*(.-)%s*<name>target helper</name>
        <packageName></packageName>
        <script>Tempest = Tempest or {}

Tempest.target = Tempest.target or &quot;&quot;
Tempest.risk_band = Tempest.risk_band or &quot;neutral&quot;
Tempest.weapon_line = Tempest.weapon_line or &quot;blunt&quot;
Tempest.character_name = Tempest.character_name or &quot;&quot;

function Tempest.set_character_name(name)
  Tempest.character_name = tostring(name or &quot;&quot;):match(&quot;^%s*(.-)%s*<name>target helper</name>
        <packageName></packageName>
        <script>Tempest = Tempest or {}

Tempest.target = Tempest.target or &quot;&quot;
Tempest.risk_band = Tempest.risk_band or &quot;neutral&quot;

function Tempest.set_target(name)
  name = tostring(name or &quot;&quot;):match(&quot;^%s*(.-)%s*$&quot;)

  if name == &quot;&quot; then
    cecho(&quot;&lt;red&gt;[Tempest] No target supplied.\n&quot;)
    return false
  end

  Tempest.target = name

  -- Compatibility with older/simple aliases that may expect a global target.
  target = name

  cecho(&quot;&lt;cyan&gt;[Tempest] Target set: &lt;white&gt;&quot; .. name .. &quot;\n&quot;)
  return true
end

function Tempest.get_target()
  if Tempest.target and Tempest.target ~= &quot;&quot; then
    return Tempest.target
  end

  if target and target ~= &quot;&quot; then
    return target
  end

  return nil
end

function Tempest.require_target()
  local tgt = Tempest.get_target()

  if not tgt or tgt == &quot;&quot; then
    cecho(&quot;&lt;red&gt;[Tempest] No target set. Use: tt &lt;name&gt;\n&quot;)
    return nil
  end

  return tgt
end

function Tempest.normalize_favors(value, silent)
  local text = tostring(value or &quot;&quot;):match(&quot;^%s*(.-)%s*$&quot;):lower()
  if text == &quot;&quot; then
    return nil
  end

  if text == &quot;min&quot; or text == &quot;max&quot; or text:match(&quot;^%d+$&quot;) then
    return text
  end

  if not silent then
    cecho(&quot;&lt;red&gt;[Tempest] Invalid favors value. Use: min, max, or a number.\n&quot;)
  end

  return nil
end

function Tempest.send_pray(gift, target_name, favors)
  local cmd = &quot;pray &quot; .. gift
  local target_text = tostring(target_name or &quot;&quot;):match(&quot;^%s*(.-)%s*$&quot;)
  if target_text ~= &quot;&quot; then
    cmd = cmd .. &quot; &quot; .. target_text
  end

  local favors_text = Tempest.normalize_favors(favors)
  if favors_text then
    cmd = cmd .. &quot; &quot; .. favors_text
  end

  send(cmd)
end

local function resolve_attack_target(explicit_target)
  local text = tostring(explicit_target or &quot;&quot;):match(&quot;^%s*(.-)%s*$&quot;)
  if text ~= &quot;&quot; then
    return text
  end
  return Tempest.get_target()
end

function Tempest.send_basic_attack(attack, explicit_target)
  local target_name = resolve_attack_target(explicit_target)
  if target_name and target_name ~= &quot;&quot; then
    send(attack .. &quot; &quot; .. target_name)
  else
    send(attack)
  end
end

function Tempest.set_risk_band(value)
  local band = tostring(value or &quot;&quot;):lower():match(&quot;^%s*(.-)%s*$&quot;)
  local valid = {
    good = true,
    neutral = true,
    bad = true,
    critical = true,
  }

  if not valid[band] then
    cecho(&quot;&lt;red&gt;[Tempest] Invalid risk band. Use: good, neutral, bad, critical.\n&quot;)
    return false
  end

  Tempest.risk_band = band
  cecho(&quot;&lt;cyan&gt;[Tempest] Risk band set: &lt;white&gt;&quot; .. band .. &quot;\n&quot;)
  return true
end

function Tempest.send_melee(kind, explicit_target)
  local key = tostring(kind or &quot;&quot;):lower()
  local attack = nil

  if key == &quot;safe&quot; then
    attack = &quot;pound&quot;
  elseif key == &quot;mid&quot; then
    attack = &quot;crush&quot;
  elseif key == &quot;heavy&quot; then
    attack = &quot;smash&quot;
  elseif key == &quot;auto&quot; then
    if Tempest.risk_band == &quot;good&quot; then
      attack = &quot;smash&quot;
    elseif Tempest.risk_band == &quot;bad&quot; or Tempest.risk_band == &quot;critical&quot; then
      attack = &quot;pound&quot;
    else
      attack = &quot;crush&quot;
    end
  else
    cecho(&quot;&lt;red&gt;[Tempest] Unknown melee mode. Use: safe, mid, heavy, auto.\n&quot;)
    return false
  end

  Tempest.send_basic_attack(attack, explicit_target)
  return true
end</script>
        <eventHandlerList />quot;)
  if target_text ~= &quot;&quot; then
    cmd = cmd .. &quot; &quot; .. target_text
  end

  local favors_text = Tempest.normalize_favors(favors)
  if favors_text then
    cmd = cmd .. &quot; &quot; .. favors_text
  end

  send(cmd)
end

local function resolve_attack_target(explicit_target)
  local text = tostring(explicit_target or &quot;&quot;):match(&quot;^%s*(.-)%s*<name>target helper</name>
        <packageName></packageName>
        <script>Tempest = Tempest or {}

Tempest.target = Tempest.target or &quot;&quot;
Tempest.risk_band = Tempest.risk_band or &quot;neutral&quot;
-- Preference when the wielded weapon supports both slashing and blunt (see weapon_can_*).
Tempest.weapon_line = Tempest.weapon_line or &quot;blunt&quot;
-- weapon_can_slash / weapon_can_blunt: nil until triggers fire or you wreset (see TempestCombat.xml).
Tempest.character_name = Tempest.character_name or &quot;&quot;

function Tempest.set_character_name(name)
  Tempest.character_name = tostring(name or &quot;&quot;):match(&quot;^%s*(.-)%s*<name>target helper</name>
        <packageName></packageName>
        <script>Tempest = Tempest or {}

Tempest.target = Tempest.target or &quot;&quot;
Tempest.risk_band = Tempest.risk_band or &quot;neutral&quot;
-- Preference when the wielded weapon supports both slashing and blunt (see weapon_can_*).
Tempest.weapon_line = Tempest.weapon_line or &quot;blunt&quot;
-- weapon_can_slash / weapon_can_blunt: nil until triggers fire or you wreset (see TempestCombat.xml).
Tempest.character_name = Tempest.character_name or &quot;&quot;

function Tempest.set_character_name(name)
  Tempest.character_name = tostring(name or &quot;&quot;):match(&quot;^%s*(.-)%s*<name>target helper</name>
        <packageName></packageName>
        <script>Tempest = Tempest or {}

Tempest.target = Tempest.target or &quot;&quot;
Tempest.risk_band = Tempest.risk_band or &quot;neutral&quot;
Tempest.weapon_line = Tempest.weapon_line or &quot;blunt&quot;
Tempest.character_name = Tempest.character_name or &quot;&quot;

function Tempest.set_character_name(name)
  Tempest.character_name = tostring(name or &quot;&quot;):match(&quot;^%s*(.-)%s*<name>target helper</name>
        <packageName></packageName>
        <script>Tempest = Tempest or {}

Tempest.target = Tempest.target or &quot;&quot;
Tempest.risk_band = Tempest.risk_band or &quot;neutral&quot;
Tempest.weapon_line = Tempest.weapon_line or &quot;blunt&quot;
Tempest.character_name = Tempest.character_name or &quot;&quot;

function Tempest.set_character_name(name)
  Tempest.character_name = tostring(name or &quot;&quot;):match(&quot;^%s*(.-)%s*<name>target helper</name>
        <packageName></packageName>
        <script>Tempest = Tempest or {}

Tempest.target = Tempest.target or &quot;&quot;
Tempest.risk_band = Tempest.risk_band or &quot;neutral&quot;

function Tempest.set_target(name)
  name = tostring(name or &quot;&quot;):match(&quot;^%s*(.-)%s*$&quot;)

  if name == &quot;&quot; then
    cecho(&quot;&lt;red&gt;[Tempest] No target supplied.\n&quot;)
    return false
  end

  Tempest.target = name

  -- Compatibility with older/simple aliases that may expect a global target.
  target = name

  cecho(&quot;&lt;cyan&gt;[Tempest] Target set: &lt;white&gt;&quot; .. name .. &quot;\n&quot;)
  return true
end

function Tempest.get_target()
  if Tempest.target and Tempest.target ~= &quot;&quot; then
    return Tempest.target
  end

  if target and target ~= &quot;&quot; then
    return target
  end

  return nil
end

function Tempest.require_target()
  local tgt = Tempest.get_target()

  if not tgt or tgt == &quot;&quot; then
    cecho(&quot;&lt;red&gt;[Tempest] No target set. Use: tt &lt;name&gt;\n&quot;)
    return nil
  end

  return tgt
end

function Tempest.normalize_favors(value, silent)
  local text = tostring(value or &quot;&quot;):match(&quot;^%s*(.-)%s*$&quot;):lower()
  if text == &quot;&quot; then
    return nil
  end

  if text == &quot;min&quot; or text == &quot;max&quot; or text:match(&quot;^%d+$&quot;) then
    return text
  end

  if not silent then
    cecho(&quot;&lt;red&gt;[Tempest] Invalid favors value. Use: min, max, or a number.\n&quot;)
  end

  return nil
end

function Tempest.send_pray(gift, target_name, favors)
  local cmd = &quot;pray &quot; .. gift
  local target_text = tostring(target_name or &quot;&quot;):match(&quot;^%s*(.-)%s*$&quot;)
  if target_text ~= &quot;&quot; then
    cmd = cmd .. &quot; &quot; .. target_text
  end

  local favors_text = Tempest.normalize_favors(favors)
  if favors_text then
    cmd = cmd .. &quot; &quot; .. favors_text
  end

  send(cmd)
end

local function resolve_attack_target(explicit_target)
  local text = tostring(explicit_target or &quot;&quot;):match(&quot;^%s*(.-)%s*$&quot;)
  if text ~= &quot;&quot; then
    return text
  end
  return Tempest.get_target()
end

function Tempest.send_basic_attack(attack, explicit_target)
  local target_name = resolve_attack_target(explicit_target)
  if target_name and target_name ~= &quot;&quot; then
    send(attack .. &quot; &quot; .. target_name)
  else
    send(attack)
  end
end

function Tempest.set_risk_band(value)
  local band = tostring(value or &quot;&quot;):lower():match(&quot;^%s*(.-)%s*$&quot;)
  local valid = {
    good = true,
    neutral = true,
    bad = true,
    critical = true,
  }

  if not valid[band] then
    cecho(&quot;&lt;red&gt;[Tempest] Invalid risk band. Use: good, neutral, bad, critical.\n&quot;)
    return false
  end

  Tempest.risk_band = band
  cecho(&quot;&lt;cyan&gt;[Tempest] Risk band set: &lt;white&gt;&quot; .. band .. &quot;\n&quot;)
  return true
end

function Tempest.send_melee(kind, explicit_target)
  local key = tostring(kind or &quot;&quot;):lower()
  local attack = nil

  if key == &quot;safe&quot; then
    attack = &quot;pound&quot;
  elseif key == &quot;mid&quot; then
    attack = &quot;crush&quot;
  elseif key == &quot;heavy&quot; then
    attack = &quot;smash&quot;
  elseif key == &quot;auto&quot; then
    if Tempest.risk_band == &quot;good&quot; then
      attack = &quot;smash&quot;
    elseif Tempest.risk_band == &quot;bad&quot; or Tempest.risk_band == &quot;critical&quot; then
      attack = &quot;pound&quot;
    else
      attack = &quot;crush&quot;
    end
  else
    cecho(&quot;&lt;red&gt;[Tempest] Unknown melee mode. Use: safe, mid, heavy, auto.\n&quot;)
    return false
  end

  Tempest.send_basic_attack(attack, explicit_target)
  return true
end</script>
        <eventHandlerList />quot;)
  if text ~= &quot;&quot; then
    return text
  end
  return Tempest.get_target()
end

function Tempest.send_basic_attack(attack, explicit_target)
  local target_name = resolve_attack_target(explicit_target)
  if target_name and target_name ~= &quot;&quot; then
    if is_target_self(target_name) then
      cecho(&quot;&lt;red&gt;[Tempest] Refusing to attack yourself; fix target (tt) or character name (Tempest.set_character_name).\n&quot;)
      return false
    end
    send(attack .. &quot; &quot; .. target_name)
  else
    send(attack)
  end
  return true
end

function Tempest.set_risk_band(value)
  local band = tostring(value or &quot;&quot;):lower():match(&quot;^%s*(.-)%s*<name>target helper</name>
        <packageName></packageName>
        <script>Tempest = Tempest or {}

Tempest.target = Tempest.target or &quot;&quot;
Tempest.risk_band = Tempest.risk_band or &quot;neutral&quot;
-- Preference when the wielded weapon supports both slashing and blunt (see weapon_can_*).
Tempest.weapon_line = Tempest.weapon_line or &quot;blunt&quot;
-- weapon_can_slash / weapon_can_blunt: nil until triggers fire or you wreset (see TempestCombat.xml).
Tempest.character_name = Tempest.character_name or &quot;&quot;

function Tempest.set_character_name(name)
  Tempest.character_name = tostring(name or &quot;&quot;):match(&quot;^%s*(.-)%s*<name>target helper</name>
        <packageName></packageName>
        <script>Tempest = Tempest or {}

Tempest.target = Tempest.target or &quot;&quot;
Tempest.risk_band = Tempest.risk_band or &quot;neutral&quot;
-- Preference when the wielded weapon supports both slashing and blunt (see weapon_can_*).
Tempest.weapon_line = Tempest.weapon_line or &quot;blunt&quot;
-- weapon_can_slash / weapon_can_blunt: nil until triggers fire or you wreset (see TempestCombat.xml).
Tempest.character_name = Tempest.character_name or &quot;&quot;

function Tempest.set_character_name(name)
  Tempest.character_name = tostring(name or &quot;&quot;):match(&quot;^%s*(.-)%s*<name>target helper</name>
        <packageName></packageName>
        <script>Tempest = Tempest or {}

Tempest.target = Tempest.target or &quot;&quot;
Tempest.risk_band = Tempest.risk_band or &quot;neutral&quot;
Tempest.weapon_line = Tempest.weapon_line or &quot;blunt&quot;
Tempest.character_name = Tempest.character_name or &quot;&quot;

function Tempest.set_character_name(name)
  Tempest.character_name = tostring(name or &quot;&quot;):match(&quot;^%s*(.-)%s*<name>target helper</name>
        <packageName></packageName>
        <script>Tempest = Tempest or {}

Tempest.target = Tempest.target or &quot;&quot;
Tempest.risk_band = Tempest.risk_band or &quot;neutral&quot;
Tempest.weapon_line = Tempest.weapon_line or &quot;blunt&quot;
Tempest.character_name = Tempest.character_name or &quot;&quot;

function Tempest.set_character_name(name)
  Tempest.character_name = tostring(name or &quot;&quot;):match(&quot;^%s*(.-)%s*<name>target helper</name>
        <packageName></packageName>
        <script>Tempest = Tempest or {}

Tempest.target = Tempest.target or &quot;&quot;
Tempest.risk_band = Tempest.risk_band or &quot;neutral&quot;

function Tempest.set_target(name)
  name = tostring(name or &quot;&quot;):match(&quot;^%s*(.-)%s*$&quot;)

  if name == &quot;&quot; then
    cecho(&quot;&lt;red&gt;[Tempest] No target supplied.\n&quot;)
    return false
  end

  Tempest.target = name

  -- Compatibility with older/simple aliases that may expect a global target.
  target = name

  cecho(&quot;&lt;cyan&gt;[Tempest] Target set: &lt;white&gt;&quot; .. name .. &quot;\n&quot;)
  return true
end

function Tempest.get_target()
  if Tempest.target and Tempest.target ~= &quot;&quot; then
    return Tempest.target
  end

  if target and target ~= &quot;&quot; then
    return target
  end

  return nil
end

function Tempest.require_target()
  local tgt = Tempest.get_target()

  if not tgt or tgt == &quot;&quot; then
    cecho(&quot;&lt;red&gt;[Tempest] No target set. Use: tt &lt;name&gt;\n&quot;)
    return nil
  end

  return tgt
end

function Tempest.normalize_favors(value, silent)
  local text = tostring(value or &quot;&quot;):match(&quot;^%s*(.-)%s*$&quot;):lower()
  if text == &quot;&quot; then
    return nil
  end

  if text == &quot;min&quot; or text == &quot;max&quot; or text:match(&quot;^%d+$&quot;) then
    return text
  end

  if not silent then
    cecho(&quot;&lt;red&gt;[Tempest] Invalid favors value. Use: min, max, or a number.\n&quot;)
  end

  return nil
end

function Tempest.send_pray(gift, target_name, favors)
  local cmd = &quot;pray &quot; .. gift
  local target_text = tostring(target_name or &quot;&quot;):match(&quot;^%s*(.-)%s*$&quot;)
  if target_text ~= &quot;&quot; then
    cmd = cmd .. &quot; &quot; .. target_text
  end

  local favors_text = Tempest.normalize_favors(favors)
  if favors_text then
    cmd = cmd .. &quot; &quot; .. favors_text
  end

  send(cmd)
end

local function resolve_attack_target(explicit_target)
  local text = tostring(explicit_target or &quot;&quot;):match(&quot;^%s*(.-)%s*$&quot;)
  if text ~= &quot;&quot; then
    return text
  end
  return Tempest.get_target()
end

function Tempest.send_basic_attack(attack, explicit_target)
  local target_name = resolve_attack_target(explicit_target)
  if target_name and target_name ~= &quot;&quot; then
    send(attack .. &quot; &quot; .. target_name)
  else
    send(attack)
  end
end

function Tempest.set_risk_band(value)
  local band = tostring(value or &quot;&quot;):lower():match(&quot;^%s*(.-)%s*$&quot;)
  local valid = {
    good = true,
    neutral = true,
    bad = true,
    critical = true,
  }

  if not valid[band] then
    cecho(&quot;&lt;red&gt;[Tempest] Invalid risk band. Use: good, neutral, bad, critical.\n&quot;)
    return false
  end

  Tempest.risk_band = band
  cecho(&quot;&lt;cyan&gt;[Tempest] Risk band set: &lt;white&gt;&quot; .. band .. &quot;\n&quot;)
  return true
end

function Tempest.send_melee(kind, explicit_target)
  local key = tostring(kind or &quot;&quot;):lower()
  local attack = nil

  if key == &quot;safe&quot; then
    attack = &quot;pound&quot;
  elseif key == &quot;mid&quot; then
    attack = &quot;crush&quot;
  elseif key == &quot;heavy&quot; then
    attack = &quot;smash&quot;
  elseif key == &quot;auto&quot; then
    if Tempest.risk_band == &quot;good&quot; then
      attack = &quot;smash&quot;
    elseif Tempest.risk_band == &quot;bad&quot; or Tempest.risk_band == &quot;critical&quot; then
      attack = &quot;pound&quot;
    else
      attack = &quot;crush&quot;
    end
  else
    cecho(&quot;&lt;red&gt;[Tempest] Unknown melee mode. Use: safe, mid, heavy, auto.\n&quot;)
    return false
  end

  Tempest.send_basic_attack(attack, explicit_target)
  return true
end</script>
        <eventHandlerList />quot;)
  local valid = {
    good = true,
    neutral = true,
    bad = true,
    critical = true,
  }

  if not valid[band] then
    cecho(&quot;&lt;red&gt;[Tempest] Invalid risk band. Use: good, neutral, bad, critical.\n&quot;)
    return false
  end

  Tempest.risk_band = band
  cecho(&quot;&lt;cyan&gt;[Tempest] Risk band set: &lt;white&gt;&quot; .. band .. &quot;\n&quot;)
  return true
end

function Tempest.send_melee(kind, explicit_target)
  local key = tostring(kind or &quot;&quot;):lower()
  local tier = nil

  if key == &quot;safe&quot; then
    tier = &quot;safe&quot;
  elseif key == &quot;mid&quot; then
    tier = &quot;mid&quot;
  elseif key == &quot;heavy&quot; then
    tier = &quot;heavy&quot;
  elseif key == &quot;auto&quot; then
    if Tempest.risk_band == &quot;good&quot; then
      tier = &quot;heavy&quot;
    elseif Tempest.risk_band == &quot;bad&quot; or Tempest.risk_band == &quot;critical&quot; then
      tier = &quot;safe&quot;
    else
      tier = &quot;mid&quot;
    end
  else
    cecho(&quot;&lt;red&gt;[Tempest] Unknown melee mode. Use: safe, mid, heavy, auto.\n&quot;)
    return false
  end

  local attack = Tempest.melee_verb_for_tier(tier)
  if not attack then
    return false
  end

  Tempest.send_basic_attack(attack, explicit_target)
  return true
end
</script>
        <eventHandlerList />quot;) or &quot;&quot;
  if Tempest.character_name ~= &quot;&quot; then
    cecho(&quot;&lt;cyan&gt;[Tempest] Character name set for self-checks: &lt;white&gt;&quot; .. Tempest.character_name .. &quot;\n&quot;)
  end
end

function Tempest.get_self_name()
  local n = Tempest.character_name or &quot;&quot;
  if n ~= &quot;&quot; then
    return n
  end
  if type(gmcp) == &quot;table&quot; and gmcp.Char and gmcp.Char.Name then
    return tostring(gmcp.Char.Name)
  end
  return &quot;&quot;
end

local function norm_lower(s)
  return tostring(s or &quot;&quot;):match(&quot;^%s*(.-)%s*<name>target helper</name>
        <packageName></packageName>
        <script>Tempest = Tempest or {}

Tempest.target = Tempest.target or &quot;&quot;
Tempest.risk_band = Tempest.risk_band or &quot;neutral&quot;
-- Preference when the wielded weapon supports both slashing and blunt (see weapon_can_*).
Tempest.weapon_line = Tempest.weapon_line or &quot;blunt&quot;
-- weapon_can_slash / weapon_can_blunt: nil until triggers fire or you wreset (see TempestCombat.xml).
Tempest.character_name = Tempest.character_name or &quot;&quot;

function Tempest.set_character_name(name)
  Tempest.character_name = tostring(name or &quot;&quot;):match(&quot;^%s*(.-)%s*<name>target helper</name>
        <packageName></packageName>
        <script>Tempest = Tempest or {}

Tempest.target = Tempest.target or &quot;&quot;
Tempest.risk_band = Tempest.risk_band or &quot;neutral&quot;
Tempest.weapon_line = Tempest.weapon_line or &quot;blunt&quot;
Tempest.character_name = Tempest.character_name or &quot;&quot;

function Tempest.set_character_name(name)
  Tempest.character_name = tostring(name or &quot;&quot;):match(&quot;^%s*(.-)%s*<name>target helper</name>
        <packageName></packageName>
        <script>Tempest = Tempest or {}

Tempest.target = Tempest.target or &quot;&quot;
Tempest.risk_band = Tempest.risk_band or &quot;neutral&quot;
Tempest.weapon_line = Tempest.weapon_line or &quot;blunt&quot;
Tempest.character_name = Tempest.character_name or &quot;&quot;

function Tempest.set_character_name(name)
  Tempest.character_name = tostring(name or &quot;&quot;):match(&quot;^%s*(.-)%s*<name>target helper</name>
        <packageName></packageName>
        <script>Tempest = Tempest or {}

Tempest.target = Tempest.target or &quot;&quot;
Tempest.risk_band = Tempest.risk_band or &quot;neutral&quot;

function Tempest.set_target(name)
  name = tostring(name or &quot;&quot;):match(&quot;^%s*(.-)%s*$&quot;)

  if name == &quot;&quot; then
    cecho(&quot;&lt;red&gt;[Tempest] No target supplied.\n&quot;)
    return false
  end

  Tempest.target = name

  -- Compatibility with older/simple aliases that may expect a global target.
  target = name

  cecho(&quot;&lt;cyan&gt;[Tempest] Target set: &lt;white&gt;&quot; .. name .. &quot;\n&quot;)
  return true
end

function Tempest.get_target()
  if Tempest.target and Tempest.target ~= &quot;&quot; then
    return Tempest.target
  end

  if target and target ~= &quot;&quot; then
    return target
  end

  return nil
end

function Tempest.require_target()
  local tgt = Tempest.get_target()

  if not tgt or tgt == &quot;&quot; then
    cecho(&quot;&lt;red&gt;[Tempest] No target set. Use: tt &lt;name&gt;\n&quot;)
    return nil
  end

  return tgt
end

function Tempest.normalize_favors(value, silent)
  local text = tostring(value or &quot;&quot;):match(&quot;^%s*(.-)%s*$&quot;):lower()
  if text == &quot;&quot; then
    return nil
  end

  if text == &quot;min&quot; or text == &quot;max&quot; or text:match(&quot;^%d+$&quot;) then
    return text
  end

  if not silent then
    cecho(&quot;&lt;red&gt;[Tempest] Invalid favors value. Use: min, max, or a number.\n&quot;)
  end

  return nil
end

function Tempest.send_pray(gift, target_name, favors)
  local cmd = &quot;pray &quot; .. gift
  local target_text = tostring(target_name or &quot;&quot;):match(&quot;^%s*(.-)%s*$&quot;)
  if target_text ~= &quot;&quot; then
    cmd = cmd .. &quot; &quot; .. target_text
  end

  local favors_text = Tempest.normalize_favors(favors)
  if favors_text then
    cmd = cmd .. &quot; &quot; .. favors_text
  end

  send(cmd)
end

local function resolve_attack_target(explicit_target)
  local text = tostring(explicit_target or &quot;&quot;):match(&quot;^%s*(.-)%s*$&quot;)
  if text ~= &quot;&quot; then
    return text
  end
  return Tempest.get_target()
end

function Tempest.send_basic_attack(attack, explicit_target)
  local target_name = resolve_attack_target(explicit_target)
  if target_name and target_name ~= &quot;&quot; then
    send(attack .. &quot; &quot; .. target_name)
  else
    send(attack)
  end
end

function Tempest.set_risk_band(value)
  local band = tostring(value or &quot;&quot;):lower():match(&quot;^%s*(.-)%s*$&quot;)
  local valid = {
    good = true,
    neutral = true,
    bad = true,
    critical = true,
  }

  if not valid[band] then
    cecho(&quot;&lt;red&gt;[Tempest] Invalid risk band. Use: good, neutral, bad, critical.\n&quot;)
    return false
  end

  Tempest.risk_band = band
  cecho(&quot;&lt;cyan&gt;[Tempest] Risk band set: &lt;white&gt;&quot; .. band .. &quot;\n&quot;)
  return true
end

function Tempest.send_melee(kind, explicit_target)
  local key = tostring(kind or &quot;&quot;):lower()
  local attack = nil

  if key == &quot;safe&quot; then
    attack = &quot;pound&quot;
  elseif key == &quot;mid&quot; then
    attack = &quot;crush&quot;
  elseif key == &quot;heavy&quot; then
    attack = &quot;smash&quot;
  elseif key == &quot;auto&quot; then
    if Tempest.risk_band == &quot;good&quot; then
      attack = &quot;smash&quot;
    elseif Tempest.risk_band == &quot;bad&quot; or Tempest.risk_band == &quot;critical&quot; then
      attack = &quot;pound&quot;
    else
      attack = &quot;crush&quot;
    end
  else
    cecho(&quot;&lt;red&gt;[Tempest] Unknown melee mode. Use: safe, mid, heavy, auto.\n&quot;)
    return false
  end

  Tempest.send_basic_attack(attack, explicit_target)
  return true
end</script>
        <eventHandlerList />quot;):lower()
end

local function is_target_self(name)
  local self_name = Tempest.get_self_name()
  if self_name == &quot;&quot; or name == &quot;&quot; then
    return false
  end
  return norm_lower(name) == norm_lower(self_name)
end

function Tempest.set_weapon_line(line)
  local key = norm_lower(line)
  if key ~= &quot;sharp&quot; and key ~= &quot;blunt&quot; then
    cecho(&quot;&lt;red&gt;[Tempest] weapon_line must be sharp or blunt.\n&quot;)
    return false
  end
  Tempest.weapon_line = key
  cecho(&quot;&lt;cyan&gt;[Tempest] Melee style preference (when weapon allows both): &lt;white&gt;&quot; .. key .. &quot;\n&quot;)
  return true
end

function Tempest.reset_weapon_capabilities()
  Tempest.weapon_can_slash = nil
  Tempest.weapon_can_blunt = nil
  cecho(&quot;&lt;cyan&gt;[Tempest] Weapon style flags cleared (probe weapon again).\n&quot;)
end

function Tempest.note_slashing_weapon()
  Tempest.weapon_can_slash = true
end

function Tempest.note_blunt_weapon()
  Tempest.weapon_can_blunt = true
end

--- Effective line for verb choice: single-style from probes, or weapon_line when both apply.
function Tempest.resolve_effective_weapon_line()
  local s = Tempest.weapon_can_slash
  local b = Tempest.weapon_can_blunt
  if s == true and b ~= true then
    return &quot;sharp&quot;
  end
  if b == true and s ~= true then
    return &quot;blunt&quot;
  end
  if s == true and b == true then
    return Tempest.weapon_line or &quot;blunt&quot;
  end
  return Tempest.weapon_line or &quot;blunt&quot;
end

function Tempest.melee_verb_for_tier(tier)
  local t = tostring(tier or &quot;&quot;):lower()
  local wl = Tempest.resolve_effective_weapon_line()
  if t == &quot;safe&quot; then
    return wl == &quot;sharp&quot; and &quot;slash&quot; or &quot;pound&quot;
  end
  if t == &quot;mid&quot; then
    return wl == &quot;sharp&quot; and &quot;slice&quot; or &quot;crush&quot;
  end
  if t == &quot;heavy&quot; then
    return wl == &quot;sharp&quot; and &quot;cleave&quot; or &quot;smash&quot;
  end
  return nil
end

function Tempest.set_target(name)
  name = tostring(name or &quot;&quot;):match(&quot;^%s*(.-)%s*<name>target helper</name>
        <packageName></packageName>
        <script>Tempest = Tempest or {}

Tempest.target = Tempest.target or &quot;&quot;
Tempest.risk_band = Tempest.risk_band or &quot;neutral&quot;
-- Preference when the wielded weapon supports both slashing and blunt (see weapon_can_*).
Tempest.weapon_line = Tempest.weapon_line or &quot;blunt&quot;
-- weapon_can_slash / weapon_can_blunt: nil until triggers fire or you wreset (see TempestCombat.xml).
Tempest.character_name = Tempest.character_name or &quot;&quot;

function Tempest.set_character_name(name)
  Tempest.character_name = tostring(name or &quot;&quot;):match(&quot;^%s*(.-)%s*<name>target helper</name>
        <packageName></packageName>
        <script>Tempest = Tempest or {}

Tempest.target = Tempest.target or &quot;&quot;
Tempest.risk_band = Tempest.risk_band or &quot;neutral&quot;
Tempest.weapon_line = Tempest.weapon_line or &quot;blunt&quot;
Tempest.character_name = Tempest.character_name or &quot;&quot;

function Tempest.set_character_name(name)
  Tempest.character_name = tostring(name or &quot;&quot;):match(&quot;^%s*(.-)%s*<name>target helper</name>
        <packageName></packageName>
        <script>Tempest = Tempest or {}

Tempest.target = Tempest.target or &quot;&quot;
Tempest.risk_band = Tempest.risk_band or &quot;neutral&quot;
Tempest.weapon_line = Tempest.weapon_line or &quot;blunt&quot;
Tempest.character_name = Tempest.character_name or &quot;&quot;

function Tempest.set_character_name(name)
  Tempest.character_name = tostring(name or &quot;&quot;):match(&quot;^%s*(.-)%s*<name>target helper</name>
        <packageName></packageName>
        <script>Tempest = Tempest or {}

Tempest.target = Tempest.target or &quot;&quot;
Tempest.risk_band = Tempest.risk_band or &quot;neutral&quot;

function Tempest.set_target(name)
  name = tostring(name or &quot;&quot;):match(&quot;^%s*(.-)%s*$&quot;)

  if name == &quot;&quot; then
    cecho(&quot;&lt;red&gt;[Tempest] No target supplied.\n&quot;)
    return false
  end

  Tempest.target = name

  -- Compatibility with older/simple aliases that may expect a global target.
  target = name

  cecho(&quot;&lt;cyan&gt;[Tempest] Target set: &lt;white&gt;&quot; .. name .. &quot;\n&quot;)
  return true
end

function Tempest.get_target()
  if Tempest.target and Tempest.target ~= &quot;&quot; then
    return Tempest.target
  end

  if target and target ~= &quot;&quot; then
    return target
  end

  return nil
end

function Tempest.require_target()
  local tgt = Tempest.get_target()

  if not tgt or tgt == &quot;&quot; then
    cecho(&quot;&lt;red&gt;[Tempest] No target set. Use: tt &lt;name&gt;\n&quot;)
    return nil
  end

  return tgt
end

function Tempest.normalize_favors(value, silent)
  local text = tostring(value or &quot;&quot;):match(&quot;^%s*(.-)%s*$&quot;):lower()
  if text == &quot;&quot; then
    return nil
  end

  if text == &quot;min&quot; or text == &quot;max&quot; or text:match(&quot;^%d+$&quot;) then
    return text
  end

  if not silent then
    cecho(&quot;&lt;red&gt;[Tempest] Invalid favors value. Use: min, max, or a number.\n&quot;)
  end

  return nil
end

function Tempest.send_pray(gift, target_name, favors)
  local cmd = &quot;pray &quot; .. gift
  local target_text = tostring(target_name or &quot;&quot;):match(&quot;^%s*(.-)%s*$&quot;)
  if target_text ~= &quot;&quot; then
    cmd = cmd .. &quot; &quot; .. target_text
  end

  local favors_text = Tempest.normalize_favors(favors)
  if favors_text then
    cmd = cmd .. &quot; &quot; .. favors_text
  end

  send(cmd)
end

local function resolve_attack_target(explicit_target)
  local text = tostring(explicit_target or &quot;&quot;):match(&quot;^%s*(.-)%s*$&quot;)
  if text ~= &quot;&quot; then
    return text
  end
  return Tempest.get_target()
end

function Tempest.send_basic_attack(attack, explicit_target)
  local target_name = resolve_attack_target(explicit_target)
  if target_name and target_name ~= &quot;&quot; then
    send(attack .. &quot; &quot; .. target_name)
  else
    send(attack)
  end
end

function Tempest.set_risk_band(value)
  local band = tostring(value or &quot;&quot;):lower():match(&quot;^%s*(.-)%s*$&quot;)
  local valid = {
    good = true,
    neutral = true,
    bad = true,
    critical = true,
  }

  if not valid[band] then
    cecho(&quot;&lt;red&gt;[Tempest] Invalid risk band. Use: good, neutral, bad, critical.\n&quot;)
    return false
  end

  Tempest.risk_band = band
  cecho(&quot;&lt;cyan&gt;[Tempest] Risk band set: &lt;white&gt;&quot; .. band .. &quot;\n&quot;)
  return true
end

function Tempest.send_melee(kind, explicit_target)
  local key = tostring(kind or &quot;&quot;):lower()
  local attack = nil

  if key == &quot;safe&quot; then
    attack = &quot;pound&quot;
  elseif key == &quot;mid&quot; then
    attack = &quot;crush&quot;
  elseif key == &quot;heavy&quot; then
    attack = &quot;smash&quot;
  elseif key == &quot;auto&quot; then
    if Tempest.risk_band == &quot;good&quot; then
      attack = &quot;smash&quot;
    elseif Tempest.risk_band == &quot;bad&quot; or Tempest.risk_band == &quot;critical&quot; then
      attack = &quot;pound&quot;
    else
      attack = &quot;crush&quot;
    end
  else
    cecho(&quot;&lt;red&gt;[Tempest] Unknown melee mode. Use: safe, mid, heavy, auto.\n&quot;)
    return false
  end

  Tempest.send_basic_attack(attack, explicit_target)
  return true
end</script>
        <eventHandlerList />quot;)

  if name == &quot;&quot; then
    cecho(&quot;&lt;red&gt;[Tempest] No target supplied.\n&quot;)
    return false
  end

  Tempest.target = name

  -- Compatibility with older/simple aliases that may expect a global target.
  target = name

  cecho(&quot;&lt;cyan&gt;[Tempest] Target set: &lt;white&gt;&quot; .. name .. &quot;\n&quot;)
  return true
end

function Tempest.get_target()
  if Tempest.target and Tempest.target ~= &quot;&quot; then
    return Tempest.target
  end

  if target and target ~= &quot;&quot; then
    return target
  end

  return nil
end

function Tempest.require_target()
  local tgt = Tempest.get_target()

  if not tgt or tgt == &quot;&quot; then
    cecho(&quot;&lt;red&gt;[Tempest] No target set. Use: tt &lt;name&gt;\n&quot;)
    return nil
  end

  if is_target_self(tgt) then
    cecho(&quot;&lt;red&gt;[Tempest] Target is your character name; clear or change target with tt.\n&quot;)
    return nil
  end

  return tgt
end

function Tempest.normalize_favors(value, silent)
  local text = tostring(value or &quot;&quot;):match(&quot;^%s*(.-)%s*<name>target helper</name>
        <packageName></packageName>
        <script>Tempest = Tempest or {}

Tempest.target = Tempest.target or &quot;&quot;
Tempest.risk_band = Tempest.risk_band or &quot;neutral&quot;
-- Preference when the wielded weapon supports both slashing and blunt (see weapon_can_*).
Tempest.weapon_line = Tempest.weapon_line or &quot;blunt&quot;
-- weapon_can_slash / weapon_can_blunt: nil until triggers fire or you wreset (see TempestCombat.xml).
Tempest.character_name = Tempest.character_name or &quot;&quot;

function Tempest.set_character_name(name)
  Tempest.character_name = tostring(name or &quot;&quot;):match(&quot;^%s*(.-)%s*<name>target helper</name>
        <packageName></packageName>
        <script>Tempest = Tempest or {}

Tempest.target = Tempest.target or &quot;&quot;
Tempest.risk_band = Tempest.risk_band or &quot;neutral&quot;
Tempest.weapon_line = Tempest.weapon_line or &quot;blunt&quot;
Tempest.character_name = Tempest.character_name or &quot;&quot;

function Tempest.set_character_name(name)
  Tempest.character_name = tostring(name or &quot;&quot;):match(&quot;^%s*(.-)%s*<name>target helper</name>
        <packageName></packageName>
        <script>Tempest = Tempest or {}

Tempest.target = Tempest.target or &quot;&quot;
Tempest.risk_band = Tempest.risk_band or &quot;neutral&quot;
Tempest.weapon_line = Tempest.weapon_line or &quot;blunt&quot;
Tempest.character_name = Tempest.character_name or &quot;&quot;

function Tempest.set_character_name(name)
  Tempest.character_name = tostring(name or &quot;&quot;):match(&quot;^%s*(.-)%s*<name>target helper</name>
        <packageName></packageName>
        <script>Tempest = Tempest or {}

Tempest.target = Tempest.target or &quot;&quot;
Tempest.risk_band = Tempest.risk_band or &quot;neutral&quot;

function Tempest.set_target(name)
  name = tostring(name or &quot;&quot;):match(&quot;^%s*(.-)%s*$&quot;)

  if name == &quot;&quot; then
    cecho(&quot;&lt;red&gt;[Tempest] No target supplied.\n&quot;)
    return false
  end

  Tempest.target = name

  -- Compatibility with older/simple aliases that may expect a global target.
  target = name

  cecho(&quot;&lt;cyan&gt;[Tempest] Target set: &lt;white&gt;&quot; .. name .. &quot;\n&quot;)
  return true
end

function Tempest.get_target()
  if Tempest.target and Tempest.target ~= &quot;&quot; then
    return Tempest.target
  end

  if target and target ~= &quot;&quot; then
    return target
  end

  return nil
end

function Tempest.require_target()
  local tgt = Tempest.get_target()

  if not tgt or tgt == &quot;&quot; then
    cecho(&quot;&lt;red&gt;[Tempest] No target set. Use: tt &lt;name&gt;\n&quot;)
    return nil
  end

  return tgt
end

function Tempest.normalize_favors(value, silent)
  local text = tostring(value or &quot;&quot;):match(&quot;^%s*(.-)%s*$&quot;):lower()
  if text == &quot;&quot; then
    return nil
  end

  if text == &quot;min&quot; or text == &quot;max&quot; or text:match(&quot;^%d+$&quot;) then
    return text
  end

  if not silent then
    cecho(&quot;&lt;red&gt;[Tempest] Invalid favors value. Use: min, max, or a number.\n&quot;)
  end

  return nil
end

function Tempest.send_pray(gift, target_name, favors)
  local cmd = &quot;pray &quot; .. gift
  local target_text = tostring(target_name or &quot;&quot;):match(&quot;^%s*(.-)%s*$&quot;)
  if target_text ~= &quot;&quot; then
    cmd = cmd .. &quot; &quot; .. target_text
  end

  local favors_text = Tempest.normalize_favors(favors)
  if favors_text then
    cmd = cmd .. &quot; &quot; .. favors_text
  end

  send(cmd)
end

local function resolve_attack_target(explicit_target)
  local text = tostring(explicit_target or &quot;&quot;):match(&quot;^%s*(.-)%s*$&quot;)
  if text ~= &quot;&quot; then
    return text
  end
  return Tempest.get_target()
end

function Tempest.send_basic_attack(attack, explicit_target)
  local target_name = resolve_attack_target(explicit_target)
  if target_name and target_name ~= &quot;&quot; then
    send(attack .. &quot; &quot; .. target_name)
  else
    send(attack)
  end
end

function Tempest.set_risk_band(value)
  local band = tostring(value or &quot;&quot;):lower():match(&quot;^%s*(.-)%s*$&quot;)
  local valid = {
    good = true,
    neutral = true,
    bad = true,
    critical = true,
  }

  if not valid[band] then
    cecho(&quot;&lt;red&gt;[Tempest] Invalid risk band. Use: good, neutral, bad, critical.\n&quot;)
    return false
  end

  Tempest.risk_band = band
  cecho(&quot;&lt;cyan&gt;[Tempest] Risk band set: &lt;white&gt;&quot; .. band .. &quot;\n&quot;)
  return true
end

function Tempest.send_melee(kind, explicit_target)
  local key = tostring(kind or &quot;&quot;):lower()
  local attack = nil

  if key == &quot;safe&quot; then
    attack = &quot;pound&quot;
  elseif key == &quot;mid&quot; then
    attack = &quot;crush&quot;
  elseif key == &quot;heavy&quot; then
    attack = &quot;smash&quot;
  elseif key == &quot;auto&quot; then
    if Tempest.risk_band == &quot;good&quot; then
      attack = &quot;smash&quot;
    elseif Tempest.risk_band == &quot;bad&quot; or Tempest.risk_band == &quot;critical&quot; then
      attack = &quot;pound&quot;
    else
      attack = &quot;crush&quot;
    end
  else
    cecho(&quot;&lt;red&gt;[Tempest] Unknown melee mode. Use: safe, mid, heavy, auto.\n&quot;)
    return false
  end

  Tempest.send_basic_attack(attack, explicit_target)
  return true
end</script>
        <eventHandlerList />quot;):lower()
  if text == &quot;&quot; then
    return nil
  end

  if text == &quot;min&quot; or text == &quot;max&quot; or text:match(&quot;^%d+<name>target helper</name>
        <packageName></packageName>
        <script>Tempest = Tempest or {}

Tempest.target = Tempest.target or &quot;&quot;
Tempest.risk_band = Tempest.risk_band or &quot;neutral&quot;
-- Preference when the wielded weapon supports both slashing and blunt (see weapon_can_*).
Tempest.weapon_line = Tempest.weapon_line or &quot;blunt&quot;
-- weapon_can_slash / weapon_can_blunt: nil until triggers fire or you wreset (see TempestCombat.xml).
Tempest.character_name = Tempest.character_name or &quot;&quot;

function Tempest.set_character_name(name)
  Tempest.character_name = tostring(name or &quot;&quot;):match(&quot;^%s*(.-)%s*<name>target helper</name>
        <packageName></packageName>
        <script>Tempest = Tempest or {}

Tempest.target = Tempest.target or &quot;&quot;
Tempest.risk_band = Tempest.risk_band or &quot;neutral&quot;
Tempest.weapon_line = Tempest.weapon_line or &quot;blunt&quot;
Tempest.character_name = Tempest.character_name or &quot;&quot;

function Tempest.set_character_name(name)
  Tempest.character_name = tostring(name or &quot;&quot;):match(&quot;^%s*(.-)%s*<name>target helper</name>
        <packageName></packageName>
        <script>Tempest = Tempest or {}

Tempest.target = Tempest.target or &quot;&quot;
Tempest.risk_band = Tempest.risk_band or &quot;neutral&quot;
Tempest.weapon_line = Tempest.weapon_line or &quot;blunt&quot;
Tempest.character_name = Tempest.character_name or &quot;&quot;

function Tempest.set_character_name(name)
  Tempest.character_name = tostring(name or &quot;&quot;):match(&quot;^%s*(.-)%s*<name>target helper</name>
        <packageName></packageName>
        <script>Tempest = Tempest or {}

Tempest.target = Tempest.target or &quot;&quot;
Tempest.risk_band = Tempest.risk_band or &quot;neutral&quot;

function Tempest.set_target(name)
  name = tostring(name or &quot;&quot;):match(&quot;^%s*(.-)%s*$&quot;)

  if name == &quot;&quot; then
    cecho(&quot;&lt;red&gt;[Tempest] No target supplied.\n&quot;)
    return false
  end

  Tempest.target = name

  -- Compatibility with older/simple aliases that may expect a global target.
  target = name

  cecho(&quot;&lt;cyan&gt;[Tempest] Target set: &lt;white&gt;&quot; .. name .. &quot;\n&quot;)
  return true
end

function Tempest.get_target()
  if Tempest.target and Tempest.target ~= &quot;&quot; then
    return Tempest.target
  end

  if target and target ~= &quot;&quot; then
    return target
  end

  return nil
end

function Tempest.require_target()
  local tgt = Tempest.get_target()

  if not tgt or tgt == &quot;&quot; then
    cecho(&quot;&lt;red&gt;[Tempest] No target set. Use: tt &lt;name&gt;\n&quot;)
    return nil
  end

  return tgt
end

function Tempest.normalize_favors(value, silent)
  local text = tostring(value or &quot;&quot;):match(&quot;^%s*(.-)%s*$&quot;):lower()
  if text == &quot;&quot; then
    return nil
  end

  if text == &quot;min&quot; or text == &quot;max&quot; or text:match(&quot;^%d+$&quot;) then
    return text
  end

  if not silent then
    cecho(&quot;&lt;red&gt;[Tempest] Invalid favors value. Use: min, max, or a number.\n&quot;)
  end

  return nil
end

function Tempest.send_pray(gift, target_name, favors)
  local cmd = &quot;pray &quot; .. gift
  local target_text = tostring(target_name or &quot;&quot;):match(&quot;^%s*(.-)%s*$&quot;)
  if target_text ~= &quot;&quot; then
    cmd = cmd .. &quot; &quot; .. target_text
  end

  local favors_text = Tempest.normalize_favors(favors)
  if favors_text then
    cmd = cmd .. &quot; &quot; .. favors_text
  end

  send(cmd)
end

local function resolve_attack_target(explicit_target)
  local text = tostring(explicit_target or &quot;&quot;):match(&quot;^%s*(.-)%s*$&quot;)
  if text ~= &quot;&quot; then
    return text
  end
  return Tempest.get_target()
end

function Tempest.send_basic_attack(attack, explicit_target)
  local target_name = resolve_attack_target(explicit_target)
  if target_name and target_name ~= &quot;&quot; then
    send(attack .. &quot; &quot; .. target_name)
  else
    send(attack)
  end
end

function Tempest.set_risk_band(value)
  local band = tostring(value or &quot;&quot;):lower():match(&quot;^%s*(.-)%s*$&quot;)
  local valid = {
    good = true,
    neutral = true,
    bad = true,
    critical = true,
  }

  if not valid[band] then
    cecho(&quot;&lt;red&gt;[Tempest] Invalid risk band. Use: good, neutral, bad, critical.\n&quot;)
    return false
  end

  Tempest.risk_band = band
  cecho(&quot;&lt;cyan&gt;[Tempest] Risk band set: &lt;white&gt;&quot; .. band .. &quot;\n&quot;)
  return true
end

function Tempest.send_melee(kind, explicit_target)
  local key = tostring(kind or &quot;&quot;):lower()
  local attack = nil

  if key == &quot;safe&quot; then
    attack = &quot;pound&quot;
  elseif key == &quot;mid&quot; then
    attack = &quot;crush&quot;
  elseif key == &quot;heavy&quot; then
    attack = &quot;smash&quot;
  elseif key == &quot;auto&quot; then
    if Tempest.risk_band == &quot;good&quot; then
      attack = &quot;smash&quot;
    elseif Tempest.risk_band == &quot;bad&quot; or Tempest.risk_band == &quot;critical&quot; then
      attack = &quot;pound&quot;
    else
      attack = &quot;crush&quot;
    end
  else
    cecho(&quot;&lt;red&gt;[Tempest] Unknown melee mode. Use: safe, mid, heavy, auto.\n&quot;)
    return false
  end

  Tempest.send_basic_attack(attack, explicit_target)
  return true
end</script>
        <eventHandlerList />quot;) then
    return text
  end

  if not silent then
    cecho(&quot;&lt;red&gt;[Tempest] Invalid favors value. Use: min, max, or a number.\n&quot;)
  end
  return nil
end

function Tempest.send_pray(gift, target_name, favors)
  local cmd = &quot;pray &quot; .. gift
  local target_text = tostring(target_name or &quot;&quot;):match(&quot;^%s*(.-)%s*<name>target helper</name>
        <packageName></packageName>
        <script>Tempest = Tempest or {}

Tempest.target = Tempest.target or &quot;&quot;
Tempest.risk_band = Tempest.risk_band or &quot;neutral&quot;
-- Preference when the wielded weapon supports both slashing and blunt (see weapon_can_*).
Tempest.weapon_line = Tempest.weapon_line or &quot;blunt&quot;
-- weapon_can_slash / weapon_can_blunt: nil until triggers fire or you wreset (see TempestCombat.xml).
Tempest.character_name = Tempest.character_name or &quot;&quot;

function Tempest.set_character_name(name)
  Tempest.character_name = tostring(name or &quot;&quot;):match(&quot;^%s*(.-)%s*<name>target helper</name>
        <packageName></packageName>
        <script>Tempest = Tempest or {}

Tempest.target = Tempest.target or &quot;&quot;
Tempest.risk_band = Tempest.risk_band or &quot;neutral&quot;
Tempest.weapon_line = Tempest.weapon_line or &quot;blunt&quot;
Tempest.character_name = Tempest.character_name or &quot;&quot;

function Tempest.set_character_name(name)
  Tempest.character_name = tostring(name or &quot;&quot;):match(&quot;^%s*(.-)%s*<name>target helper</name>
        <packageName></packageName>
        <script>Tempest = Tempest or {}

Tempest.target = Tempest.target or &quot;&quot;
Tempest.risk_band = Tempest.risk_band or &quot;neutral&quot;
Tempest.weapon_line = Tempest.weapon_line or &quot;blunt&quot;
Tempest.character_name = Tempest.character_name or &quot;&quot;

function Tempest.set_character_name(name)
  Tempest.character_name = tostring(name or &quot;&quot;):match(&quot;^%s*(.-)%s*<name>target helper</name>
        <packageName></packageName>
        <script>Tempest = Tempest or {}

Tempest.target = Tempest.target or &quot;&quot;
Tempest.risk_band = Tempest.risk_band or &quot;neutral&quot;

function Tempest.set_target(name)
  name = tostring(name or &quot;&quot;):match(&quot;^%s*(.-)%s*$&quot;)

  if name == &quot;&quot; then
    cecho(&quot;&lt;red&gt;[Tempest] No target supplied.\n&quot;)
    return false
  end

  Tempest.target = name

  -- Compatibility with older/simple aliases that may expect a global target.
  target = name

  cecho(&quot;&lt;cyan&gt;[Tempest] Target set: &lt;white&gt;&quot; .. name .. &quot;\n&quot;)
  return true
end

function Tempest.get_target()
  if Tempest.target and Tempest.target ~= &quot;&quot; then
    return Tempest.target
  end

  if target and target ~= &quot;&quot; then
    return target
  end

  return nil
end

function Tempest.require_target()
  local tgt = Tempest.get_target()

  if not tgt or tgt == &quot;&quot; then
    cecho(&quot;&lt;red&gt;[Tempest] No target set. Use: tt &lt;name&gt;\n&quot;)
    return nil
  end

  return tgt
end

function Tempest.normalize_favors(value, silent)
  local text = tostring(value or &quot;&quot;):match(&quot;^%s*(.-)%s*$&quot;):lower()
  if text == &quot;&quot; then
    return nil
  end

  if text == &quot;min&quot; or text == &quot;max&quot; or text:match(&quot;^%d+$&quot;) then
    return text
  end

  if not silent then
    cecho(&quot;&lt;red&gt;[Tempest] Invalid favors value. Use: min, max, or a number.\n&quot;)
  end

  return nil
end

function Tempest.send_pray(gift, target_name, favors)
  local cmd = &quot;pray &quot; .. gift
  local target_text = tostring(target_name or &quot;&quot;):match(&quot;^%s*(.-)%s*$&quot;)
  if target_text ~= &quot;&quot; then
    cmd = cmd .. &quot; &quot; .. target_text
  end

  local favors_text = Tempest.normalize_favors(favors)
  if favors_text then
    cmd = cmd .. &quot; &quot; .. favors_text
  end

  send(cmd)
end

local function resolve_attack_target(explicit_target)
  local text = tostring(explicit_target or &quot;&quot;):match(&quot;^%s*(.-)%s*$&quot;)
  if text ~= &quot;&quot; then
    return text
  end
  return Tempest.get_target()
end

function Tempest.send_basic_attack(attack, explicit_target)
  local target_name = resolve_attack_target(explicit_target)
  if target_name and target_name ~= &quot;&quot; then
    send(attack .. &quot; &quot; .. target_name)
  else
    send(attack)
  end
end

function Tempest.set_risk_band(value)
  local band = tostring(value or &quot;&quot;):lower():match(&quot;^%s*(.-)%s*$&quot;)
  local valid = {
    good = true,
    neutral = true,
    bad = true,
    critical = true,
  }

  if not valid[band] then
    cecho(&quot;&lt;red&gt;[Tempest] Invalid risk band. Use: good, neutral, bad, critical.\n&quot;)
    return false
  end

  Tempest.risk_band = band
  cecho(&quot;&lt;cyan&gt;[Tempest] Risk band set: &lt;white&gt;&quot; .. band .. &quot;\n&quot;)
  return true
end

function Tempest.send_melee(kind, explicit_target)
  local key = tostring(kind or &quot;&quot;):lower()
  local attack = nil

  if key == &quot;safe&quot; then
    attack = &quot;pound&quot;
  elseif key == &quot;mid&quot; then
    attack = &quot;crush&quot;
  elseif key == &quot;heavy&quot; then
    attack = &quot;smash&quot;
  elseif key == &quot;auto&quot; then
    if Tempest.risk_band == &quot;good&quot; then
      attack = &quot;smash&quot;
    elseif Tempest.risk_band == &quot;bad&quot; or Tempest.risk_band == &quot;critical&quot; then
      attack = &quot;pound&quot;
    else
      attack = &quot;crush&quot;
    end
  else
    cecho(&quot;&lt;red&gt;[Tempest] Unknown melee mode. Use: safe, mid, heavy, auto.\n&quot;)
    return false
  end

  Tempest.send_basic_attack(attack, explicit_target)
  return true
end</script>
        <eventHandlerList />quot;)
  if target_text ~= &quot;&quot; then
    cmd = cmd .. &quot; &quot; .. target_text
  end

  local favors_text = Tempest.normalize_favors(favors)
  if favors_text then
    cmd = cmd .. &quot; &quot; .. favors_text
  end

  send(cmd)
end

local function resolve_attack_target(explicit_target)
  local text = tostring(explicit_target or &quot;&quot;):match(&quot;^%s*(.-)%s*<name>target helper</name>
        <packageName></packageName>
        <script>Tempest = Tempest or {}

Tempest.target = Tempest.target or &quot;&quot;
Tempest.risk_band = Tempest.risk_band or &quot;neutral&quot;
-- Preference when the wielded weapon supports both slashing and blunt (see weapon_can_*).
Tempest.weapon_line = Tempest.weapon_line or &quot;blunt&quot;
-- weapon_can_slash / weapon_can_blunt: nil until triggers fire or you wreset (see TempestCombat.xml).
Tempest.character_name = Tempest.character_name or &quot;&quot;

function Tempest.set_character_name(name)
  Tempest.character_name = tostring(name or &quot;&quot;):match(&quot;^%s*(.-)%s*<name>target helper</name>
        <packageName></packageName>
        <script>Tempest = Tempest or {}

Tempest.target = Tempest.target or &quot;&quot;
Tempest.risk_band = Tempest.risk_band or &quot;neutral&quot;
Tempest.weapon_line = Tempest.weapon_line or &quot;blunt&quot;
Tempest.character_name = Tempest.character_name or &quot;&quot;

function Tempest.set_character_name(name)
  Tempest.character_name = tostring(name or &quot;&quot;):match(&quot;^%s*(.-)%s*<name>target helper</name>
        <packageName></packageName>
        <script>Tempest = Tempest or {}

Tempest.target = Tempest.target or &quot;&quot;
Tempest.risk_band = Tempest.risk_band or &quot;neutral&quot;
Tempest.weapon_line = Tempest.weapon_line or &quot;blunt&quot;
Tempest.character_name = Tempest.character_name or &quot;&quot;

function Tempest.set_character_name(name)
  Tempest.character_name = tostring(name or &quot;&quot;):match(&quot;^%s*(.-)%s*<name>target helper</name>
        <packageName></packageName>
        <script>Tempest = Tempest or {}

Tempest.target = Tempest.target or &quot;&quot;
Tempest.risk_band = Tempest.risk_band or &quot;neutral&quot;

function Tempest.set_target(name)
  name = tostring(name or &quot;&quot;):match(&quot;^%s*(.-)%s*$&quot;)

  if name == &quot;&quot; then
    cecho(&quot;&lt;red&gt;[Tempest] No target supplied.\n&quot;)
    return false
  end

  Tempest.target = name

  -- Compatibility with older/simple aliases that may expect a global target.
  target = name

  cecho(&quot;&lt;cyan&gt;[Tempest] Target set: &lt;white&gt;&quot; .. name .. &quot;\n&quot;)
  return true
end

function Tempest.get_target()
  if Tempest.target and Tempest.target ~= &quot;&quot; then
    return Tempest.target
  end

  if target and target ~= &quot;&quot; then
    return target
  end

  return nil
end

function Tempest.require_target()
  local tgt = Tempest.get_target()

  if not tgt or tgt == &quot;&quot; then
    cecho(&quot;&lt;red&gt;[Tempest] No target set. Use: tt &lt;name&gt;\n&quot;)
    return nil
  end

  return tgt
end

function Tempest.normalize_favors(value, silent)
  local text = tostring(value or &quot;&quot;):match(&quot;^%s*(.-)%s*$&quot;):lower()
  if text == &quot;&quot; then
    return nil
  end

  if text == &quot;min&quot; or text == &quot;max&quot; or text:match(&quot;^%d+$&quot;) then
    return text
  end

  if not silent then
    cecho(&quot;&lt;red&gt;[Tempest] Invalid favors value. Use: min, max, or a number.\n&quot;)
  end

  return nil
end

function Tempest.send_pray(gift, target_name, favors)
  local cmd = &quot;pray &quot; .. gift
  local target_text = tostring(target_name or &quot;&quot;):match(&quot;^%s*(.-)%s*$&quot;)
  if target_text ~= &quot;&quot; then
    cmd = cmd .. &quot; &quot; .. target_text
  end

  local favors_text = Tempest.normalize_favors(favors)
  if favors_text then
    cmd = cmd .. &quot; &quot; .. favors_text
  end

  send(cmd)
end

local function resolve_attack_target(explicit_target)
  local text = tostring(explicit_target or &quot;&quot;):match(&quot;^%s*(.-)%s*$&quot;)
  if text ~= &quot;&quot; then
    return text
  end
  return Tempest.get_target()
end

function Tempest.send_basic_attack(attack, explicit_target)
  local target_name = resolve_attack_target(explicit_target)
  if target_name and target_name ~= &quot;&quot; then
    send(attack .. &quot; &quot; .. target_name)
  else
    send(attack)
  end
end

function Tempest.set_risk_band(value)
  local band = tostring(value or &quot;&quot;):lower():match(&quot;^%s*(.-)%s*$&quot;)
  local valid = {
    good = true,
    neutral = true,
    bad = true,
    critical = true,
  }

  if not valid[band] then
    cecho(&quot;&lt;red&gt;[Tempest] Invalid risk band. Use: good, neutral, bad, critical.\n&quot;)
    return false
  end

  Tempest.risk_band = band
  cecho(&quot;&lt;cyan&gt;[Tempest] Risk band set: &lt;white&gt;&quot; .. band .. &quot;\n&quot;)
  return true
end

function Tempest.send_melee(kind, explicit_target)
  local key = tostring(kind or &quot;&quot;):lower()
  local attack = nil

  if key == &quot;safe&quot; then
    attack = &quot;pound&quot;
  elseif key == &quot;mid&quot; then
    attack = &quot;crush&quot;
  elseif key == &quot;heavy&quot; then
    attack = &quot;smash&quot;
  elseif key == &quot;auto&quot; then
    if Tempest.risk_band == &quot;good&quot; then
      attack = &quot;smash&quot;
    elseif Tempest.risk_band == &quot;bad&quot; or Tempest.risk_band == &quot;critical&quot; then
      attack = &quot;pound&quot;
    else
      attack = &quot;crush&quot;
    end
  else
    cecho(&quot;&lt;red&gt;[Tempest] Unknown melee mode. Use: safe, mid, heavy, auto.\n&quot;)
    return false
  end

  Tempest.send_basic_attack(attack, explicit_target)
  return true
end</script>
        <eventHandlerList />quot;)
  if text ~= &quot;&quot; then
    return text
  end
  return Tempest.get_target()
end

function Tempest.send_basic_attack(attack, explicit_target)
  local target_name = resolve_attack_target(explicit_target)
  if target_name and target_name ~= &quot;&quot; then
    if is_target_self(target_name) then
      cecho(&quot;&lt;red&gt;[Tempest] Refusing to attack yourself; fix target (tt) or character name (Tempest.set_character_name).\n&quot;)
      return false
    end
    send(attack .. &quot; &quot; .. target_name)
  else
    send(attack)
  end
  return true
end

function Tempest.set_risk_band(value)
  local band = tostring(value or &quot;&quot;):lower():match(&quot;^%s*(.-)%s*<name>target helper</name>
        <packageName></packageName>
        <script>Tempest = Tempest or {}

Tempest.target = Tempest.target or &quot;&quot;
Tempest.risk_band = Tempest.risk_band or &quot;neutral&quot;
-- Preference when the wielded weapon supports both slashing and blunt (see weapon_can_*).
Tempest.weapon_line = Tempest.weapon_line or &quot;blunt&quot;
-- weapon_can_slash / weapon_can_blunt: nil until triggers fire or you wreset (see TempestCombat.xml).
Tempest.character_name = Tempest.character_name or &quot;&quot;

function Tempest.set_character_name(name)
  Tempest.character_name = tostring(name or &quot;&quot;):match(&quot;^%s*(.-)%s*<name>target helper</name>
        <packageName></packageName>
        <script>Tempest = Tempest or {}

Tempest.target = Tempest.target or &quot;&quot;
Tempest.risk_band = Tempest.risk_band or &quot;neutral&quot;
Tempest.weapon_line = Tempest.weapon_line or &quot;blunt&quot;
Tempest.character_name = Tempest.character_name or &quot;&quot;

function Tempest.set_character_name(name)
  Tempest.character_name = tostring(name or &quot;&quot;):match(&quot;^%s*(.-)%s*<name>target helper</name>
        <packageName></packageName>
        <script>Tempest = Tempest or {}

Tempest.target = Tempest.target or &quot;&quot;
Tempest.risk_band = Tempest.risk_band or &quot;neutral&quot;
Tempest.weapon_line = Tempest.weapon_line or &quot;blunt&quot;
Tempest.character_name = Tempest.character_name or &quot;&quot;

function Tempest.set_character_name(name)
  Tempest.character_name = tostring(name or &quot;&quot;):match(&quot;^%s*(.-)%s*<name>target helper</name>
        <packageName></packageName>
        <script>Tempest = Tempest or {}

Tempest.target = Tempest.target or &quot;&quot;
Tempest.risk_band = Tempest.risk_band or &quot;neutral&quot;

function Tempest.set_target(name)
  name = tostring(name or &quot;&quot;):match(&quot;^%s*(.-)%s*$&quot;)

  if name == &quot;&quot; then
    cecho(&quot;&lt;red&gt;[Tempest] No target supplied.\n&quot;)
    return false
  end

  Tempest.target = name

  -- Compatibility with older/simple aliases that may expect a global target.
  target = name

  cecho(&quot;&lt;cyan&gt;[Tempest] Target set: &lt;white&gt;&quot; .. name .. &quot;\n&quot;)
  return true
end

function Tempest.get_target()
  if Tempest.target and Tempest.target ~= &quot;&quot; then
    return Tempest.target
  end

  if target and target ~= &quot;&quot; then
    return target
  end

  return nil
end

function Tempest.require_target()
  local tgt = Tempest.get_target()

  if not tgt or tgt == &quot;&quot; then
    cecho(&quot;&lt;red&gt;[Tempest] No target set. Use: tt &lt;name&gt;\n&quot;)
    return nil
  end

  return tgt
end

function Tempest.normalize_favors(value, silent)
  local text = tostring(value or &quot;&quot;):match(&quot;^%s*(.-)%s*$&quot;):lower()
  if text == &quot;&quot; then
    return nil
  end

  if text == &quot;min&quot; or text == &quot;max&quot; or text:match(&quot;^%d+$&quot;) then
    return text
  end

  if not silent then
    cecho(&quot;&lt;red&gt;[Tempest] Invalid favors value. Use: min, max, or a number.\n&quot;)
  end

  return nil
end

function Tempest.send_pray(gift, target_name, favors)
  local cmd = &quot;pray &quot; .. gift
  local target_text = tostring(target_name or &quot;&quot;):match(&quot;^%s*(.-)%s*$&quot;)
  if target_text ~= &quot;&quot; then
    cmd = cmd .. &quot; &quot; .. target_text
  end

  local favors_text = Tempest.normalize_favors(favors)
  if favors_text then
    cmd = cmd .. &quot; &quot; .. favors_text
  end

  send(cmd)
end

local function resolve_attack_target(explicit_target)
  local text = tostring(explicit_target or &quot;&quot;):match(&quot;^%s*(.-)%s*$&quot;)
  if text ~= &quot;&quot; then
    return text
  end
  return Tempest.get_target()
end

function Tempest.send_basic_attack(attack, explicit_target)
  local target_name = resolve_attack_target(explicit_target)
  if target_name and target_name ~= &quot;&quot; then
    send(attack .. &quot; &quot; .. target_name)
  else
    send(attack)
  end
end

function Tempest.set_risk_band(value)
  local band = tostring(value or &quot;&quot;):lower():match(&quot;^%s*(.-)%s*$&quot;)
  local valid = {
    good = true,
    neutral = true,
    bad = true,
    critical = true,
  }

  if not valid[band] then
    cecho(&quot;&lt;red&gt;[Tempest] Invalid risk band. Use: good, neutral, bad, critical.\n&quot;)
    return false
  end

  Tempest.risk_band = band
  cecho(&quot;&lt;cyan&gt;[Tempest] Risk band set: &lt;white&gt;&quot; .. band .. &quot;\n&quot;)
  return true
end

function Tempest.send_melee(kind, explicit_target)
  local key = tostring(kind or &quot;&quot;):lower()
  local attack = nil

  if key == &quot;safe&quot; then
    attack = &quot;pound&quot;
  elseif key == &quot;mid&quot; then
    attack = &quot;crush&quot;
  elseif key == &quot;heavy&quot; then
    attack = &quot;smash&quot;
  elseif key == &quot;auto&quot; then
    if Tempest.risk_band == &quot;good&quot; then
      attack = &quot;smash&quot;
    elseif Tempest.risk_band == &quot;bad&quot; or Tempest.risk_band == &quot;critical&quot; then
      attack = &quot;pound&quot;
    else
      attack = &quot;crush&quot;
    end
  else
    cecho(&quot;&lt;red&gt;[Tempest] Unknown melee mode. Use: safe, mid, heavy, auto.\n&quot;)
    return false
  end

  Tempest.send_basic_attack(attack, explicit_target)
  return true
end</script>
        <eventHandlerList />quot;)
  local valid = {
    good = true,
    neutral = true,
    bad = true,
    critical = true,
  }

  if not valid[band] then
    cecho(&quot;&lt;red&gt;[Tempest] Invalid risk band. Use: good, neutral, bad, critical.\n&quot;)
    return false
  end

  Tempest.risk_band = band
  cecho(&quot;&lt;cyan&gt;[Tempest] Risk band set: &lt;white&gt;&quot; .. band .. &quot;\n&quot;)
  return true
end

function Tempest.send_melee(kind, explicit_target)
  local key = tostring(kind or &quot;&quot;):lower()
  local tier = nil

  if key == &quot;safe&quot; then
    tier = &quot;safe&quot;
  elseif key == &quot;mid&quot; then
    tier = &quot;mid&quot;
  elseif key == &quot;heavy&quot; then
    tier = &quot;heavy&quot;
  elseif key == &quot;auto&quot; then
    if Tempest.risk_band == &quot;good&quot; then
      tier = &quot;heavy&quot;
    elseif Tempest.risk_band == &quot;bad&quot; or Tempest.risk_band == &quot;critical&quot; then
      tier = &quot;safe&quot;
    else
      tier = &quot;mid&quot;
    end
  else
    cecho(&quot;&lt;red&gt;[Tempest] Unknown melee mode. Use: safe, mid, heavy, auto.\n&quot;)
    return false
  end

  local attack = Tempest.melee_verb_for_tier(tier)
  if not attack then
    return false
  end

  Tempest.send_basic_attack(attack, explicit_target)
  return true
end
</script>
        <eventHandlerList />quot;) or &quot;&quot;
  if Tempest.character_name ~= &quot;&quot; then
    cecho(&quot;&lt;cyan&gt;[Tempest] Character name set for self-checks: &lt;white&gt;&quot; .. Tempest.character_name .. &quot;\n&quot;)
  end
end

function Tempest.get_self_name()
  local n = Tempest.character_name or &quot;&quot;
  if n ~= &quot;&quot; then
    return n
  end
  if type(gmcp) == &quot;table&quot; and gmcp.Char and gmcp.Char.Name then
    return tostring(gmcp.Char.Name)
  end
  return &quot;&quot;
end

local function norm_lower(s)
  return tostring(s or &quot;&quot;):match(&quot;^%s*(.-)%s*<name>target helper</name>
        <packageName></packageName>
        <script>Tempest = Tempest or {}

Tempest.target = Tempest.target or &quot;&quot;
Tempest.risk_band = Tempest.risk_band or &quot;neutral&quot;
Tempest.weapon_line = Tempest.weapon_line or &quot;blunt&quot;
Tempest.character_name = Tempest.character_name or &quot;&quot;

function Tempest.set_character_name(name)
  Tempest.character_name = tostring(name or &quot;&quot;):match(&quot;^%s*(.-)%s*<name>target helper</name>
        <packageName></packageName>
        <script>Tempest = Tempest or {}

Tempest.target = Tempest.target or &quot;&quot;
Tempest.risk_band = Tempest.risk_band or &quot;neutral&quot;
Tempest.weapon_line = Tempest.weapon_line or &quot;blunt&quot;
Tempest.character_name = Tempest.character_name or &quot;&quot;

function Tempest.set_character_name(name)
  Tempest.character_name = tostring(name or &quot;&quot;):match(&quot;^%s*(.-)%s*<name>target helper</name>
        <packageName></packageName>
        <script>Tempest = Tempest or {}

Tempest.target = Tempest.target or &quot;&quot;
Tempest.risk_band = Tempest.risk_band or &quot;neutral&quot;

function Tempest.set_target(name)
  name = tostring(name or &quot;&quot;):match(&quot;^%s*(.-)%s*$&quot;)

  if name == &quot;&quot; then
    cecho(&quot;&lt;red&gt;[Tempest] No target supplied.\n&quot;)
    return false
  end

  Tempest.target = name

  -- Compatibility with older/simple aliases that may expect a global target.
  target = name

  cecho(&quot;&lt;cyan&gt;[Tempest] Target set: &lt;white&gt;&quot; .. name .. &quot;\n&quot;)
  return true
end

function Tempest.get_target()
  if Tempest.target and Tempest.target ~= &quot;&quot; then
    return Tempest.target
  end

  if target and target ~= &quot;&quot; then
    return target
  end

  return nil
end

function Tempest.require_target()
  local tgt = Tempest.get_target()

  if not tgt or tgt == &quot;&quot; then
    cecho(&quot;&lt;red&gt;[Tempest] No target set. Use: tt &lt;name&gt;\n&quot;)
    return nil
  end

  return tgt
end

function Tempest.normalize_favors(value, silent)
  local text = tostring(value or &quot;&quot;):match(&quot;^%s*(.-)%s*$&quot;):lower()
  if text == &quot;&quot; then
    return nil
  end

  if text == &quot;min&quot; or text == &quot;max&quot; or text:match(&quot;^%d+$&quot;) then
    return text
  end

  if not silent then
    cecho(&quot;&lt;red&gt;[Tempest] Invalid favors value. Use: min, max, or a number.\n&quot;)
  end

  return nil
end

function Tempest.send_pray(gift, target_name, favors)
  local cmd = &quot;pray &quot; .. gift
  local target_text = tostring(target_name or &quot;&quot;):match(&quot;^%s*(.-)%s*$&quot;)
  if target_text ~= &quot;&quot; then
    cmd = cmd .. &quot; &quot; .. target_text
  end

  local favors_text = Tempest.normalize_favors(favors)
  if favors_text then
    cmd = cmd .. &quot; &quot; .. favors_text
  end

  send(cmd)
end

local function resolve_attack_target(explicit_target)
  local text = tostring(explicit_target or &quot;&quot;):match(&quot;^%s*(.-)%s*$&quot;)
  if text ~= &quot;&quot; then
    return text
  end
  return Tempest.get_target()
end

function Tempest.send_basic_attack(attack, explicit_target)
  local target_name = resolve_attack_target(explicit_target)
  if target_name and target_name ~= &quot;&quot; then
    send(attack .. &quot; &quot; .. target_name)
  else
    send(attack)
  end
end

function Tempest.set_risk_band(value)
  local band = tostring(value or &quot;&quot;):lower():match(&quot;^%s*(.-)%s*$&quot;)
  local valid = {
    good = true,
    neutral = true,
    bad = true,
    critical = true,
  }

  if not valid[band] then
    cecho(&quot;&lt;red&gt;[Tempest] Invalid risk band. Use: good, neutral, bad, critical.\n&quot;)
    return false
  end

  Tempest.risk_band = band
  cecho(&quot;&lt;cyan&gt;[Tempest] Risk band set: &lt;white&gt;&quot; .. band .. &quot;\n&quot;)
  return true
end

function Tempest.send_melee(kind, explicit_target)
  local key = tostring(kind or &quot;&quot;):lower()
  local attack = nil

  if key == &quot;safe&quot; then
    attack = &quot;pound&quot;
  elseif key == &quot;mid&quot; then
    attack = &quot;crush&quot;
  elseif key == &quot;heavy&quot; then
    attack = &quot;smash&quot;
  elseif key == &quot;auto&quot; then
    if Tempest.risk_band == &quot;good&quot; then
      attack = &quot;smash&quot;
    elseif Tempest.risk_band == &quot;bad&quot; or Tempest.risk_band == &quot;critical&quot; then
      attack = &quot;pound&quot;
    else
      attack = &quot;crush&quot;
    end
  else
    cecho(&quot;&lt;red&gt;[Tempest] Unknown melee mode. Use: safe, mid, heavy, auto.\n&quot;)
    return false
  end

  Tempest.send_basic_attack(attack, explicit_target)
  return true
end</script>
        <eventHandlerList />quot;):lower()
end

local function is_target_self(name)
  local self_name = Tempest.get_self_name()
  if self_name == &quot;&quot; or name == &quot;&quot; then
    return false
  end
  return norm_lower(name) == norm_lower(self_name)
end

function Tempest.set_weapon_line(line)
  local key = norm_lower(line)
  if key ~= &quot;sharp&quot; and key ~= &quot;blunt&quot; then
    cecho(&quot;&lt;red&gt;[Tempest] weapon_line must be sharp or blunt.\n&quot;)
    return false
  end
  Tempest.weapon_line = key
  cecho(&quot;&lt;cyan&gt;[Tempest] Melee style preference (when weapon allows both): &lt;white&gt;&quot; .. key .. &quot;\n&quot;)
  return true
end

function Tempest.reset_weapon_capabilities()
  Tempest.weapon_can_slash = nil
  Tempest.weapon_can_blunt = nil
end

function Tempest.note_slashing_weapon()
  Tempest.weapon_can_slash = true
end

function Tempest.note_blunt_weapon()
  Tempest.weapon_can_blunt = true
end

--- Effective line for verb choice: single-style from probes, or weapon_line when both apply.
function Tempest.resolve_effective_weapon_line()
  local s = Tempest.weapon_can_slash
  local b = Tempest.weapon_can_blunt
  if s == true and b ~= true then
    return &quot;sharp&quot;
  end
  if b == true and s ~= true then
    return &quot;blunt&quot;
  end
  if s == true and b == true then
    return Tempest.weapon_line or &quot;blunt&quot;
  end
  return Tempest.weapon_line or &quot;blunt&quot;
end

function Tempest.melee_verb_for_tier(tier)
  local t = tostring(tier or &quot;&quot;):lower()
  local wl = Tempest.resolve_effective_weapon_line()
  if t == &quot;safe&quot; then
    return wl == &quot;sharp&quot; and &quot;slash&quot; or &quot;pound&quot;
  end
  if t == &quot;mid&quot; then
    return wl == &quot;sharp&quot; and &quot;slice&quot; or &quot;crush&quot;
  end
  if t == &quot;heavy&quot; then
    return wl == &quot;sharp&quot; and &quot;cleave&quot; or &quot;smash&quot;
  end
  return nil
end

function Tempest.set_target(name)
  name = tostring(name or &quot;&quot;):match(&quot;^%s*(.-)%s*<name>target helper</name>
        <packageName></packageName>
        <script>Tempest = Tempest or {}

Tempest.target = Tempest.target or &quot;&quot;
Tempest.risk_band = Tempest.risk_band or &quot;neutral&quot;
Tempest.weapon_line = Tempest.weapon_line or &quot;blunt&quot;
Tempest.character_name = Tempest.character_name or &quot;&quot;

function Tempest.set_character_name(name)
  Tempest.character_name = tostring(name or &quot;&quot;):match(&quot;^%s*(.-)%s*<name>target helper</name>
        <packageName></packageName>
        <script>Tempest = Tempest or {}

Tempest.target = Tempest.target or &quot;&quot;
Tempest.risk_band = Tempest.risk_band or &quot;neutral&quot;
Tempest.weapon_line = Tempest.weapon_line or &quot;blunt&quot;
Tempest.character_name = Tempest.character_name or &quot;&quot;

function Tempest.set_character_name(name)
  Tempest.character_name = tostring(name or &quot;&quot;):match(&quot;^%s*(.-)%s*<name>target helper</name>
        <packageName></packageName>
        <script>Tempest = Tempest or {}

Tempest.target = Tempest.target or &quot;&quot;
Tempest.risk_band = Tempest.risk_band or &quot;neutral&quot;

function Tempest.set_target(name)
  name = tostring(name or &quot;&quot;):match(&quot;^%s*(.-)%s*$&quot;)

  if name == &quot;&quot; then
    cecho(&quot;&lt;red&gt;[Tempest] No target supplied.\n&quot;)
    return false
  end

  Tempest.target = name

  -- Compatibility with older/simple aliases that may expect a global target.
  target = name

  cecho(&quot;&lt;cyan&gt;[Tempest] Target set: &lt;white&gt;&quot; .. name .. &quot;\n&quot;)
  return true
end

function Tempest.get_target()
  if Tempest.target and Tempest.target ~= &quot;&quot; then
    return Tempest.target
  end

  if target and target ~= &quot;&quot; then
    return target
  end

  return nil
end

function Tempest.require_target()
  local tgt = Tempest.get_target()

  if not tgt or tgt == &quot;&quot; then
    cecho(&quot;&lt;red&gt;[Tempest] No target set. Use: tt &lt;name&gt;\n&quot;)
    return nil
  end

  return tgt
end

function Tempest.normalize_favors(value, silent)
  local text = tostring(value or &quot;&quot;):match(&quot;^%s*(.-)%s*$&quot;):lower()
  if text == &quot;&quot; then
    return nil
  end

  if text == &quot;min&quot; or text == &quot;max&quot; or text:match(&quot;^%d+$&quot;) then
    return text
  end

  if not silent then
    cecho(&quot;&lt;red&gt;[Tempest] Invalid favors value. Use: min, max, or a number.\n&quot;)
  end

  return nil
end

function Tempest.send_pray(gift, target_name, favors)
  local cmd = &quot;pray &quot; .. gift
  local target_text = tostring(target_name or &quot;&quot;):match(&quot;^%s*(.-)%s*$&quot;)
  if target_text ~= &quot;&quot; then
    cmd = cmd .. &quot; &quot; .. target_text
  end

  local favors_text = Tempest.normalize_favors(favors)
  if favors_text then
    cmd = cmd .. &quot; &quot; .. favors_text
  end

  send(cmd)
end

local function resolve_attack_target(explicit_target)
  local text = tostring(explicit_target or &quot;&quot;):match(&quot;^%s*(.-)%s*$&quot;)
  if text ~= &quot;&quot; then
    return text
  end
  return Tempest.get_target()
end

function Tempest.send_basic_attack(attack, explicit_target)
  local target_name = resolve_attack_target(explicit_target)
  if target_name and target_name ~= &quot;&quot; then
    send(attack .. &quot; &quot; .. target_name)
  else
    send(attack)
  end
end

function Tempest.set_risk_band(value)
  local band = tostring(value or &quot;&quot;):lower():match(&quot;^%s*(.-)%s*$&quot;)
  local valid = {
    good = true,
    neutral = true,
    bad = true,
    critical = true,
  }

  if not valid[band] then
    cecho(&quot;&lt;red&gt;[Tempest] Invalid risk band. Use: good, neutral, bad, critical.\n&quot;)
    return false
  end

  Tempest.risk_band = band
  cecho(&quot;&lt;cyan&gt;[Tempest] Risk band set: &lt;white&gt;&quot; .. band .. &quot;\n&quot;)
  return true
end

function Tempest.send_melee(kind, explicit_target)
  local key = tostring(kind or &quot;&quot;):lower()
  local attack = nil

  if key == &quot;safe&quot; then
    attack = &quot;pound&quot;
  elseif key == &quot;mid&quot; then
    attack = &quot;crush&quot;
  elseif key == &quot;heavy&quot; then
    attack = &quot;smash&quot;
  elseif key == &quot;auto&quot; then
    if Tempest.risk_band == &quot;good&quot; then
      attack = &quot;smash&quot;
    elseif Tempest.risk_band == &quot;bad&quot; or Tempest.risk_band == &quot;critical&quot; then
      attack = &quot;pound&quot;
    else
      attack = &quot;crush&quot;
    end
  else
    cecho(&quot;&lt;red&gt;[Tempest] Unknown melee mode. Use: safe, mid, heavy, auto.\n&quot;)
    return false
  end

  Tempest.send_basic_attack(attack, explicit_target)
  return true
end</script>
        <eventHandlerList />quot;)

  if name == &quot;&quot; then
    cecho(&quot;&lt;red&gt;[Tempest] No target supplied.\n&quot;)
    return false
  end

  Tempest.target = name

  -- Compatibility with older/simple aliases that may expect a global target.
  target = name

  cecho(&quot;&lt;cyan&gt;[Tempest] Target set: &lt;white&gt;&quot; .. name .. &quot;\n&quot;)
  return true
end

function Tempest.get_target()
  if Tempest.target and Tempest.target ~= &quot;&quot; then
    return Tempest.target
  end

  if target and target ~= &quot;&quot; then
    return target
  end

  return nil
end

function Tempest.require_target()
  local tgt = Tempest.get_target()

  if not tgt or tgt == &quot;&quot; then
    cecho(&quot;&lt;red&gt;[Tempest] No target set. Use: tt &lt;name&gt;\n&quot;)
    return nil
  end

  if is_target_self(tgt) then
    cecho(&quot;&lt;red&gt;[Tempest] Target is your character name; clear or change target with tt.\n&quot;)
    return nil
  end

  return tgt
end

function Tempest.normalize_favors(value, silent)
  local text = tostring(value or &quot;&quot;):match(&quot;^%s*(.-)%s*<name>target helper</name>
        <packageName></packageName>
        <script>Tempest = Tempest or {}

Tempest.target = Tempest.target or &quot;&quot;
Tempest.risk_band = Tempest.risk_band or &quot;neutral&quot;
Tempest.weapon_line = Tempest.weapon_line or &quot;blunt&quot;
Tempest.character_name = Tempest.character_name or &quot;&quot;

function Tempest.set_character_name(name)
  Tempest.character_name = tostring(name or &quot;&quot;):match(&quot;^%s*(.-)%s*<name>target helper</name>
        <packageName></packageName>
        <script>Tempest = Tempest or {}

Tempest.target = Tempest.target or &quot;&quot;
Tempest.risk_band = Tempest.risk_band or &quot;neutral&quot;
Tempest.weapon_line = Tempest.weapon_line or &quot;blunt&quot;
Tempest.character_name = Tempest.character_name or &quot;&quot;

function Tempest.set_character_name(name)
  Tempest.character_name = tostring(name or &quot;&quot;):match(&quot;^%s*(.-)%s*<name>target helper</name>
        <packageName></packageName>
        <script>Tempest = Tempest or {}

Tempest.target = Tempest.target or &quot;&quot;
Tempest.risk_band = Tempest.risk_band or &quot;neutral&quot;

function Tempest.set_target(name)
  name = tostring(name or &quot;&quot;):match(&quot;^%s*(.-)%s*$&quot;)

  if name == &quot;&quot; then
    cecho(&quot;&lt;red&gt;[Tempest] No target supplied.\n&quot;)
    return false
  end

  Tempest.target = name

  -- Compatibility with older/simple aliases that may expect a global target.
  target = name

  cecho(&quot;&lt;cyan&gt;[Tempest] Target set: &lt;white&gt;&quot; .. name .. &quot;\n&quot;)
  return true
end

function Tempest.get_target()
  if Tempest.target and Tempest.target ~= &quot;&quot; then
    return Tempest.target
  end

  if target and target ~= &quot;&quot; then
    return target
  end

  return nil
end

function Tempest.require_target()
  local tgt = Tempest.get_target()

  if not tgt or tgt == &quot;&quot; then
    cecho(&quot;&lt;red&gt;[Tempest] No target set. Use: tt &lt;name&gt;\n&quot;)
    return nil
  end

  return tgt
end

function Tempest.normalize_favors(value, silent)
  local text = tostring(value or &quot;&quot;):match(&quot;^%s*(.-)%s*$&quot;):lower()
  if text == &quot;&quot; then
    return nil
  end

  if text == &quot;min&quot; or text == &quot;max&quot; or text:match(&quot;^%d+$&quot;) then
    return text
  end

  if not silent then
    cecho(&quot;&lt;red&gt;[Tempest] Invalid favors value. Use: min, max, or a number.\n&quot;)
  end

  return nil
end

function Tempest.send_pray(gift, target_name, favors)
  local cmd = &quot;pray &quot; .. gift
  local target_text = tostring(target_name or &quot;&quot;):match(&quot;^%s*(.-)%s*$&quot;)
  if target_text ~= &quot;&quot; then
    cmd = cmd .. &quot; &quot; .. target_text
  end

  local favors_text = Tempest.normalize_favors(favors)
  if favors_text then
    cmd = cmd .. &quot; &quot; .. favors_text
  end

  send(cmd)
end

local function resolve_attack_target(explicit_target)
  local text = tostring(explicit_target or &quot;&quot;):match(&quot;^%s*(.-)%s*$&quot;)
  if text ~= &quot;&quot; then
    return text
  end
  return Tempest.get_target()
end

function Tempest.send_basic_attack(attack, explicit_target)
  local target_name = resolve_attack_target(explicit_target)
  if target_name and target_name ~= &quot;&quot; then
    send(attack .. &quot; &quot; .. target_name)
  else
    send(attack)
  end
end

function Tempest.set_risk_band(value)
  local band = tostring(value or &quot;&quot;):lower():match(&quot;^%s*(.-)%s*$&quot;)
  local valid = {
    good = true,
    neutral = true,
    bad = true,
    critical = true,
  }

  if not valid[band] then
    cecho(&quot;&lt;red&gt;[Tempest] Invalid risk band. Use: good, neutral, bad, critical.\n&quot;)
    return false
  end

  Tempest.risk_band = band
  cecho(&quot;&lt;cyan&gt;[Tempest] Risk band set: &lt;white&gt;&quot; .. band .. &quot;\n&quot;)
  return true
end

function Tempest.send_melee(kind, explicit_target)
  local key = tostring(kind or &quot;&quot;):lower()
  local attack = nil

  if key == &quot;safe&quot; then
    attack = &quot;pound&quot;
  elseif key == &quot;mid&quot; then
    attack = &quot;crush&quot;
  elseif key == &quot;heavy&quot; then
    attack = &quot;smash&quot;
  elseif key == &quot;auto&quot; then
    if Tempest.risk_band == &quot;good&quot; then
      attack = &quot;smash&quot;
    elseif Tempest.risk_band == &quot;bad&quot; or Tempest.risk_band == &quot;critical&quot; then
      attack = &quot;pound&quot;
    else
      attack = &quot;crush&quot;
    end
  else
    cecho(&quot;&lt;red&gt;[Tempest] Unknown melee mode. Use: safe, mid, heavy, auto.\n&quot;)
    return false
  end

  Tempest.send_basic_attack(attack, explicit_target)
  return true
end</script>
        <eventHandlerList />quot;):lower()
  if text == &quot;&quot; then
    return nil
  end

  if text == &quot;min&quot; or text == &quot;max&quot; or text:match(&quot;^%d+<name>target helper</name>
        <packageName></packageName>
        <script>Tempest = Tempest or {}

Tempest.target = Tempest.target or &quot;&quot;
Tempest.risk_band = Tempest.risk_band or &quot;neutral&quot;
Tempest.weapon_line = Tempest.weapon_line or &quot;blunt&quot;
Tempest.character_name = Tempest.character_name or &quot;&quot;

function Tempest.set_character_name(name)
  Tempest.character_name = tostring(name or &quot;&quot;):match(&quot;^%s*(.-)%s*<name>target helper</name>
        <packageName></packageName>
        <script>Tempest = Tempest or {}

Tempest.target = Tempest.target or &quot;&quot;
Tempest.risk_band = Tempest.risk_band or &quot;neutral&quot;
Tempest.weapon_line = Tempest.weapon_line or &quot;blunt&quot;
Tempest.character_name = Tempest.character_name or &quot;&quot;

function Tempest.set_character_name(name)
  Tempest.character_name = tostring(name or &quot;&quot;):match(&quot;^%s*(.-)%s*<name>target helper</name>
        <packageName></packageName>
        <script>Tempest = Tempest or {}

Tempest.target = Tempest.target or &quot;&quot;
Tempest.risk_band = Tempest.risk_band or &quot;neutral&quot;

function Tempest.set_target(name)
  name = tostring(name or &quot;&quot;):match(&quot;^%s*(.-)%s*$&quot;)

  if name == &quot;&quot; then
    cecho(&quot;&lt;red&gt;[Tempest] No target supplied.\n&quot;)
    return false
  end

  Tempest.target = name

  -- Compatibility with older/simple aliases that may expect a global target.
  target = name

  cecho(&quot;&lt;cyan&gt;[Tempest] Target set: &lt;white&gt;&quot; .. name .. &quot;\n&quot;)
  return true
end

function Tempest.get_target()
  if Tempest.target and Tempest.target ~= &quot;&quot; then
    return Tempest.target
  end

  if target and target ~= &quot;&quot; then
    return target
  end

  return nil
end

function Tempest.require_target()
  local tgt = Tempest.get_target()

  if not tgt or tgt == &quot;&quot; then
    cecho(&quot;&lt;red&gt;[Tempest] No target set. Use: tt &lt;name&gt;\n&quot;)
    return nil
  end

  return tgt
end

function Tempest.normalize_favors(value, silent)
  local text = tostring(value or &quot;&quot;):match(&quot;^%s*(.-)%s*$&quot;):lower()
  if text == &quot;&quot; then
    return nil
  end

  if text == &quot;min&quot; or text == &quot;max&quot; or text:match(&quot;^%d+$&quot;) then
    return text
  end

  if not silent then
    cecho(&quot;&lt;red&gt;[Tempest] Invalid favors value. Use: min, max, or a number.\n&quot;)
  end

  return nil
end

function Tempest.send_pray(gift, target_name, favors)
  local cmd = &quot;pray &quot; .. gift
  local target_text = tostring(target_name or &quot;&quot;):match(&quot;^%s*(.-)%s*$&quot;)
  if target_text ~= &quot;&quot; then
    cmd = cmd .. &quot; &quot; .. target_text
  end

  local favors_text = Tempest.normalize_favors(favors)
  if favors_text then
    cmd = cmd .. &quot; &quot; .. favors_text
  end

  send(cmd)
end

local function resolve_attack_target(explicit_target)
  local text = tostring(explicit_target or &quot;&quot;):match(&quot;^%s*(.-)%s*$&quot;)
  if text ~= &quot;&quot; then
    return text
  end
  return Tempest.get_target()
end

function Tempest.send_basic_attack(attack, explicit_target)
  local target_name = resolve_attack_target(explicit_target)
  if target_name and target_name ~= &quot;&quot; then
    send(attack .. &quot; &quot; .. target_name)
  else
    send(attack)
  end
end

function Tempest.set_risk_band(value)
  local band = tostring(value or &quot;&quot;):lower():match(&quot;^%s*(.-)%s*$&quot;)
  local valid = {
    good = true,
    neutral = true,
    bad = true,
    critical = true,
  }

  if not valid[band] then
    cecho(&quot;&lt;red&gt;[Tempest] Invalid risk band. Use: good, neutral, bad, critical.\n&quot;)
    return false
  end

  Tempest.risk_band = band
  cecho(&quot;&lt;cyan&gt;[Tempest] Risk band set: &lt;white&gt;&quot; .. band .. &quot;\n&quot;)
  return true
end

function Tempest.send_melee(kind, explicit_target)
  local key = tostring(kind or &quot;&quot;):lower()
  local attack = nil

  if key == &quot;safe&quot; then
    attack = &quot;pound&quot;
  elseif key == &quot;mid&quot; then
    attack = &quot;crush&quot;
  elseif key == &quot;heavy&quot; then
    attack = &quot;smash&quot;
  elseif key == &quot;auto&quot; then
    if Tempest.risk_band == &quot;good&quot; then
      attack = &quot;smash&quot;
    elseif Tempest.risk_band == &quot;bad&quot; or Tempest.risk_band == &quot;critical&quot; then
      attack = &quot;pound&quot;
    else
      attack = &quot;crush&quot;
    end
  else
    cecho(&quot;&lt;red&gt;[Tempest] Unknown melee mode. Use: safe, mid, heavy, auto.\n&quot;)
    return false
  end

  Tempest.send_basic_attack(attack, explicit_target)
  return true
end</script>
        <eventHandlerList />quot;) then
    return text
  end

  if not silent then
    cecho(&quot;&lt;red&gt;[Tempest] Invalid favors value. Use: min, max, or a number.\n&quot;)
  end
  return nil
end

function Tempest.send_pray(gift, target_name, favors)
  local cmd = &quot;pray &quot; .. gift
  local target_text = tostring(target_name or &quot;&quot;):match(&quot;^%s*(.-)%s*<name>target helper</name>
        <packageName></packageName>
        <script>Tempest = Tempest or {}

Tempest.target = Tempest.target or &quot;&quot;
Tempest.risk_band = Tempest.risk_band or &quot;neutral&quot;
Tempest.weapon_line = Tempest.weapon_line or &quot;blunt&quot;
Tempest.character_name = Tempest.character_name or &quot;&quot;

function Tempest.set_character_name(name)
  Tempest.character_name = tostring(name or &quot;&quot;):match(&quot;^%s*(.-)%s*<name>target helper</name>
        <packageName></packageName>
        <script>Tempest = Tempest or {}

Tempest.target = Tempest.target or &quot;&quot;
Tempest.risk_band = Tempest.risk_band or &quot;neutral&quot;
Tempest.weapon_line = Tempest.weapon_line or &quot;blunt&quot;
Tempest.character_name = Tempest.character_name or &quot;&quot;

function Tempest.set_character_name(name)
  Tempest.character_name = tostring(name or &quot;&quot;):match(&quot;^%s*(.-)%s*<name>target helper</name>
        <packageName></packageName>
        <script>Tempest = Tempest or {}

Tempest.target = Tempest.target or &quot;&quot;
Tempest.risk_band = Tempest.risk_band or &quot;neutral&quot;

function Tempest.set_target(name)
  name = tostring(name or &quot;&quot;):match(&quot;^%s*(.-)%s*$&quot;)

  if name == &quot;&quot; then
    cecho(&quot;&lt;red&gt;[Tempest] No target supplied.\n&quot;)
    return false
  end

  Tempest.target = name

  -- Compatibility with older/simple aliases that may expect a global target.
  target = name

  cecho(&quot;&lt;cyan&gt;[Tempest] Target set: &lt;white&gt;&quot; .. name .. &quot;\n&quot;)
  return true
end

function Tempest.get_target()
  if Tempest.target and Tempest.target ~= &quot;&quot; then
    return Tempest.target
  end

  if target and target ~= &quot;&quot; then
    return target
  end

  return nil
end

function Tempest.require_target()
  local tgt = Tempest.get_target()

  if not tgt or tgt == &quot;&quot; then
    cecho(&quot;&lt;red&gt;[Tempest] No target set. Use: tt &lt;name&gt;\n&quot;)
    return nil
  end

  return tgt
end

function Tempest.normalize_favors(value, silent)
  local text = tostring(value or &quot;&quot;):match(&quot;^%s*(.-)%s*$&quot;):lower()
  if text == &quot;&quot; then
    return nil
  end

  if text == &quot;min&quot; or text == &quot;max&quot; or text:match(&quot;^%d+$&quot;) then
    return text
  end

  if not silent then
    cecho(&quot;&lt;red&gt;[Tempest] Invalid favors value. Use: min, max, or a number.\n&quot;)
  end

  return nil
end

function Tempest.send_pray(gift, target_name, favors)
  local cmd = &quot;pray &quot; .. gift
  local target_text = tostring(target_name or &quot;&quot;):match(&quot;^%s*(.-)%s*$&quot;)
  if target_text ~= &quot;&quot; then
    cmd = cmd .. &quot; &quot; .. target_text
  end

  local favors_text = Tempest.normalize_favors(favors)
  if favors_text then
    cmd = cmd .. &quot; &quot; .. favors_text
  end

  send(cmd)
end

local function resolve_attack_target(explicit_target)
  local text = tostring(explicit_target or &quot;&quot;):match(&quot;^%s*(.-)%s*$&quot;)
  if text ~= &quot;&quot; then
    return text
  end
  return Tempest.get_target()
end

function Tempest.send_basic_attack(attack, explicit_target)
  local target_name = resolve_attack_target(explicit_target)
  if target_name and target_name ~= &quot;&quot; then
    send(attack .. &quot; &quot; .. target_name)
  else
    send(attack)
  end
end

function Tempest.set_risk_band(value)
  local band = tostring(value or &quot;&quot;):lower():match(&quot;^%s*(.-)%s*$&quot;)
  local valid = {
    good = true,
    neutral = true,
    bad = true,
    critical = true,
  }

  if not valid[band] then
    cecho(&quot;&lt;red&gt;[Tempest] Invalid risk band. Use: good, neutral, bad, critical.\n&quot;)
    return false
  end

  Tempest.risk_band = band
  cecho(&quot;&lt;cyan&gt;[Tempest] Risk band set: &lt;white&gt;&quot; .. band .. &quot;\n&quot;)
  return true
end

function Tempest.send_melee(kind, explicit_target)
  local key = tostring(kind or &quot;&quot;):lower()
  local attack = nil

  if key == &quot;safe&quot; then
    attack = &quot;pound&quot;
  elseif key == &quot;mid&quot; then
    attack = &quot;crush&quot;
  elseif key == &quot;heavy&quot; then
    attack = &quot;smash&quot;
  elseif key == &quot;auto&quot; then
    if Tempest.risk_band == &quot;good&quot; then
      attack = &quot;smash&quot;
    elseif Tempest.risk_band == &quot;bad&quot; or Tempest.risk_band == &quot;critical&quot; then
      attack = &quot;pound&quot;
    else
      attack = &quot;crush&quot;
    end
  else
    cecho(&quot;&lt;red&gt;[Tempest] Unknown melee mode. Use: safe, mid, heavy, auto.\n&quot;)
    return false
  end

  Tempest.send_basic_attack(attack, explicit_target)
  return true
end</script>
        <eventHandlerList />quot;)
  if target_text ~= &quot;&quot; then
    cmd = cmd .. &quot; &quot; .. target_text
  end

  local favors_text = Tempest.normalize_favors(favors)
  if favors_text then
    cmd = cmd .. &quot; &quot; .. favors_text
  end

  send(cmd)
end

local function resolve_attack_target(explicit_target)
  local text = tostring(explicit_target or &quot;&quot;):match(&quot;^%s*(.-)%s*<name>target helper</name>
        <packageName></packageName>
        <script>Tempest = Tempest or {}

Tempest.target = Tempest.target or &quot;&quot;
Tempest.risk_band = Tempest.risk_band or &quot;neutral&quot;
Tempest.weapon_line = Tempest.weapon_line or &quot;blunt&quot;
Tempest.character_name = Tempest.character_name or &quot;&quot;

function Tempest.set_character_name(name)
  Tempest.character_name = tostring(name or &quot;&quot;):match(&quot;^%s*(.-)%s*<name>target helper</name>
        <packageName></packageName>
        <script>Tempest = Tempest or {}

Tempest.target = Tempest.target or &quot;&quot;
Tempest.risk_band = Tempest.risk_band or &quot;neutral&quot;
Tempest.weapon_line = Tempest.weapon_line or &quot;blunt&quot;
Tempest.character_name = Tempest.character_name or &quot;&quot;

function Tempest.set_character_name(name)
  Tempest.character_name = tostring(name or &quot;&quot;):match(&quot;^%s*(.-)%s*<name>target helper</name>
        <packageName></packageName>
        <script>Tempest = Tempest or {}

Tempest.target = Tempest.target or &quot;&quot;
Tempest.risk_band = Tempest.risk_band or &quot;neutral&quot;

function Tempest.set_target(name)
  name = tostring(name or &quot;&quot;):match(&quot;^%s*(.-)%s*$&quot;)

  if name == &quot;&quot; then
    cecho(&quot;&lt;red&gt;[Tempest] No target supplied.\n&quot;)
    return false
  end

  Tempest.target = name

  -- Compatibility with older/simple aliases that may expect a global target.
  target = name

  cecho(&quot;&lt;cyan&gt;[Tempest] Target set: &lt;white&gt;&quot; .. name .. &quot;\n&quot;)
  return true
end

function Tempest.get_target()
  if Tempest.target and Tempest.target ~= &quot;&quot; then
    return Tempest.target
  end

  if target and target ~= &quot;&quot; then
    return target
  end

  return nil
end

function Tempest.require_target()
  local tgt = Tempest.get_target()

  if not tgt or tgt == &quot;&quot; then
    cecho(&quot;&lt;red&gt;[Tempest] No target set. Use: tt &lt;name&gt;\n&quot;)
    return nil
  end

  return tgt
end

function Tempest.normalize_favors(value, silent)
  local text = tostring(value or &quot;&quot;):match(&quot;^%s*(.-)%s*$&quot;):lower()
  if text == &quot;&quot; then
    return nil
  end

  if text == &quot;min&quot; or text == &quot;max&quot; or text:match(&quot;^%d+$&quot;) then
    return text
  end

  if not silent then
    cecho(&quot;&lt;red&gt;[Tempest] Invalid favors value. Use: min, max, or a number.\n&quot;)
  end

  return nil
end

function Tempest.send_pray(gift, target_name, favors)
  local cmd = &quot;pray &quot; .. gift
  local target_text = tostring(target_name or &quot;&quot;):match(&quot;^%s*(.-)%s*$&quot;)
  if target_text ~= &quot;&quot; then
    cmd = cmd .. &quot; &quot; .. target_text
  end

  local favors_text = Tempest.normalize_favors(favors)
  if favors_text then
    cmd = cmd .. &quot; &quot; .. favors_text
  end

  send(cmd)
end

local function resolve_attack_target(explicit_target)
  local text = tostring(explicit_target or &quot;&quot;):match(&quot;^%s*(.-)%s*$&quot;)
  if text ~= &quot;&quot; then
    return text
  end
  return Tempest.get_target()
end

function Tempest.send_basic_attack(attack, explicit_target)
  local target_name = resolve_attack_target(explicit_target)
  if target_name and target_name ~= &quot;&quot; then
    send(attack .. &quot; &quot; .. target_name)
  else
    send(attack)
  end
end

function Tempest.set_risk_band(value)
  local band = tostring(value or &quot;&quot;):lower():match(&quot;^%s*(.-)%s*$&quot;)
  local valid = {
    good = true,
    neutral = true,
    bad = true,
    critical = true,
  }

  if not valid[band] then
    cecho(&quot;&lt;red&gt;[Tempest] Invalid risk band. Use: good, neutral, bad, critical.\n&quot;)
    return false
  end

  Tempest.risk_band = band
  cecho(&quot;&lt;cyan&gt;[Tempest] Risk band set: &lt;white&gt;&quot; .. band .. &quot;\n&quot;)
  return true
end

function Tempest.send_melee(kind, explicit_target)
  local key = tostring(kind or &quot;&quot;):lower()
  local attack = nil

  if key == &quot;safe&quot; then
    attack = &quot;pound&quot;
  elseif key == &quot;mid&quot; then
    attack = &quot;crush&quot;
  elseif key == &quot;heavy&quot; then
    attack = &quot;smash&quot;
  elseif key == &quot;auto&quot; then
    if Tempest.risk_band == &quot;good&quot; then
      attack = &quot;smash&quot;
    elseif Tempest.risk_band == &quot;bad&quot; or Tempest.risk_band == &quot;critical&quot; then
      attack = &quot;pound&quot;
    else
      attack = &quot;crush&quot;
    end
  else
    cecho(&quot;&lt;red&gt;[Tempest] Unknown melee mode. Use: safe, mid, heavy, auto.\n&quot;)
    return false
  end

  Tempest.send_basic_attack(attack, explicit_target)
  return true
end</script>
        <eventHandlerList />quot;)
  if text ~= &quot;&quot; then
    return text
  end
  return Tempest.get_target()
end

function Tempest.send_basic_attack(attack, explicit_target)
  local target_name = resolve_attack_target(explicit_target)
  if target_name and target_name ~= &quot;&quot; then
    if is_target_self(target_name) then
      cecho(&quot;&lt;red&gt;[Tempest] Refusing to attack yourself; fix target (tt) or character name (Tempest.set_character_name).\n&quot;)
      return false
    end
    send(attack .. &quot; &quot; .. target_name)
  else
    send(attack)
  end
  return true
end

function Tempest.set_risk_band(value)
  local band = tostring(value or &quot;&quot;):lower():match(&quot;^%s*(.-)%s*<name>target helper</name>
        <packageName></packageName>
        <script>Tempest = Tempest or {}

Tempest.target = Tempest.target or &quot;&quot;
Tempest.risk_band = Tempest.risk_band or &quot;neutral&quot;
Tempest.weapon_line = Tempest.weapon_line or &quot;blunt&quot;
Tempest.character_name = Tempest.character_name or &quot;&quot;

function Tempest.set_character_name(name)
  Tempest.character_name = tostring(name or &quot;&quot;):match(&quot;^%s*(.-)%s*<name>target helper</name>
        <packageName></packageName>
        <script>Tempest = Tempest or {}

Tempest.target = Tempest.target or &quot;&quot;
Tempest.risk_band = Tempest.risk_band or &quot;neutral&quot;
Tempest.weapon_line = Tempest.weapon_line or &quot;blunt&quot;
Tempest.character_name = Tempest.character_name or &quot;&quot;

function Tempest.set_character_name(name)
  Tempest.character_name = tostring(name or &quot;&quot;):match(&quot;^%s*(.-)%s*<name>target helper</name>
        <packageName></packageName>
        <script>Tempest = Tempest or {}

Tempest.target = Tempest.target or &quot;&quot;
Tempest.risk_band = Tempest.risk_band or &quot;neutral&quot;

function Tempest.set_target(name)
  name = tostring(name or &quot;&quot;):match(&quot;^%s*(.-)%s*$&quot;)

  if name == &quot;&quot; then
    cecho(&quot;&lt;red&gt;[Tempest] No target supplied.\n&quot;)
    return false
  end

  Tempest.target = name

  -- Compatibility with older/simple aliases that may expect a global target.
  target = name

  cecho(&quot;&lt;cyan&gt;[Tempest] Target set: &lt;white&gt;&quot; .. name .. &quot;\n&quot;)
  return true
end

function Tempest.get_target()
  if Tempest.target and Tempest.target ~= &quot;&quot; then
    return Tempest.target
  end

  if target and target ~= &quot;&quot; then
    return target
  end

  return nil
end

function Tempest.require_target()
  local tgt = Tempest.get_target()

  if not tgt or tgt == &quot;&quot; then
    cecho(&quot;&lt;red&gt;[Tempest] No target set. Use: tt &lt;name&gt;\n&quot;)
    return nil
  end

  return tgt
end

function Tempest.normalize_favors(value, silent)
  local text = tostring(value or &quot;&quot;):match(&quot;^%s*(.-)%s*$&quot;):lower()
  if text == &quot;&quot; then
    return nil
  end

  if text == &quot;min&quot; or text == &quot;max&quot; or text:match(&quot;^%d+$&quot;) then
    return text
  end

  if not silent then
    cecho(&quot;&lt;red&gt;[Tempest] Invalid favors value. Use: min, max, or a number.\n&quot;)
  end

  return nil
end

function Tempest.send_pray(gift, target_name, favors)
  local cmd = &quot;pray &quot; .. gift
  local target_text = tostring(target_name or &quot;&quot;):match(&quot;^%s*(.-)%s*$&quot;)
  if target_text ~= &quot;&quot; then
    cmd = cmd .. &quot; &quot; .. target_text
  end

  local favors_text = Tempest.normalize_favors(favors)
  if favors_text then
    cmd = cmd .. &quot; &quot; .. favors_text
  end

  send(cmd)
end

local function resolve_attack_target(explicit_target)
  local text = tostring(explicit_target or &quot;&quot;):match(&quot;^%s*(.-)%s*$&quot;)
  if text ~= &quot;&quot; then
    return text
  end
  return Tempest.get_target()
end

function Tempest.send_basic_attack(attack, explicit_target)
  local target_name = resolve_attack_target(explicit_target)
  if target_name and target_name ~= &quot;&quot; then
    send(attack .. &quot; &quot; .. target_name)
  else
    send(attack)
  end
end

function Tempest.set_risk_band(value)
  local band = tostring(value or &quot;&quot;):lower():match(&quot;^%s*(.-)%s*$&quot;)
  local valid = {
    good = true,
    neutral = true,
    bad = true,
    critical = true,
  }

  if not valid[band] then
    cecho(&quot;&lt;red&gt;[Tempest] Invalid risk band. Use: good, neutral, bad, critical.\n&quot;)
    return false
  end

  Tempest.risk_band = band
  cecho(&quot;&lt;cyan&gt;[Tempest] Risk band set: &lt;white&gt;&quot; .. band .. &quot;\n&quot;)
  return true
end

function Tempest.send_melee(kind, explicit_target)
  local key = tostring(kind or &quot;&quot;):lower()
  local attack = nil

  if key == &quot;safe&quot; then
    attack = &quot;pound&quot;
  elseif key == &quot;mid&quot; then
    attack = &quot;crush&quot;
  elseif key == &quot;heavy&quot; then
    attack = &quot;smash&quot;
  elseif key == &quot;auto&quot; then
    if Tempest.risk_band == &quot;good&quot; then
      attack = &quot;smash&quot;
    elseif Tempest.risk_band == &quot;bad&quot; or Tempest.risk_band == &quot;critical&quot; then
      attack = &quot;pound&quot;
    else
      attack = &quot;crush&quot;
    end
  else
    cecho(&quot;&lt;red&gt;[Tempest] Unknown melee mode. Use: safe, mid, heavy, auto.\n&quot;)
    return false
  end

  Tempest.send_basic_attack(attack, explicit_target)
  return true
end</script>
        <eventHandlerList />quot;)
  local valid = {
    good = true,
    neutral = true,
    bad = true,
    critical = true,
  }

  if not valid[band] then
    cecho(&quot;&lt;red&gt;[Tempest] Invalid risk band. Use: good, neutral, bad, critical.\n&quot;)
    return false
  end

  Tempest.risk_band = band
  cecho(&quot;&lt;cyan&gt;[Tempest] Risk band set: &lt;white&gt;&quot; .. band .. &quot;\n&quot;)
  return true
end

function Tempest.send_melee(kind, explicit_target)
  local key = tostring(kind or &quot;&quot;):lower()
  local tier = nil

  if key == &quot;safe&quot; then
    tier = &quot;safe&quot;
  elseif key == &quot;mid&quot; then
    tier = &quot;mid&quot;
  elseif key == &quot;heavy&quot; then
    tier = &quot;heavy&quot;
  elseif key == &quot;auto&quot; then
    if Tempest.risk_band == &quot;good&quot; then
      tier = &quot;heavy&quot;
    elseif Tempest.risk_band == &quot;bad&quot; or Tempest.risk_band == &quot;critical&quot; then
      tier = &quot;safe&quot;
    else
      tier = &quot;mid&quot;
    end
  else
    cecho(&quot;&lt;red&gt;[Tempest] Unknown melee mode. Use: safe, mid, heavy, auto.\n&quot;)
    return false
  end

  local attack = Tempest.melee_verb_for_tier(tier)
  if not attack then
    return false
  end

  Tempest.send_basic_attack(attack, explicit_target)
  return true
end
</script>
        <eventHandlerList />quot;) or &quot;&quot;
  if Tempest.character_name ~= &quot;&quot; then
    cecho(&quot;&lt;cyan&gt;[Tempest] Character name set for self-checks: &lt;white&gt;&quot; .. Tempest.character_name .. &quot;\n&quot;)
  end
end

function Tempest.get_self_name()
  local n = Tempest.character_name or &quot;&quot;
  if n ~= &quot;&quot; then
    return n
  end
  if type(gmcp) == &quot;table&quot; and gmcp.Char and gmcp.Char.Name then
    return tostring(gmcp.Char.Name)
  end
  return &quot;&quot;
end

local function norm_lower(s)
  return tostring(s or &quot;&quot;):match(&quot;^%s*(.-)%s*<name>target helper</name>
        <packageName></packageName>
        <script>Tempest = Tempest or {}

Tempest.target = Tempest.target or &quot;&quot;
Tempest.risk_band = Tempest.risk_band or &quot;neutral&quot;
Tempest.weapon_line = Tempest.weapon_line or &quot;blunt&quot;
Tempest.character_name = Tempest.character_name or &quot;&quot;

function Tempest.set_character_name(name)
  Tempest.character_name = tostring(name or &quot;&quot;):match(&quot;^%s*(.-)%s*<name>target helper</name>
        <packageName></packageName>
        <script>Tempest = Tempest or {}

Tempest.target = Tempest.target or &quot;&quot;
Tempest.risk_band = Tempest.risk_band or &quot;neutral&quot;

function Tempest.set_target(name)
  name = tostring(name or &quot;&quot;):match(&quot;^%s*(.-)%s*$&quot;)

  if name == &quot;&quot; then
    cecho(&quot;&lt;red&gt;[Tempest] No target supplied.\n&quot;)
    return false
  end

  Tempest.target = name

  -- Compatibility with older/simple aliases that may expect a global target.
  target = name

  cecho(&quot;&lt;cyan&gt;[Tempest] Target set: &lt;white&gt;&quot; .. name .. &quot;\n&quot;)
  return true
end

function Tempest.get_target()
  if Tempest.target and Tempest.target ~= &quot;&quot; then
    return Tempest.target
  end

  if target and target ~= &quot;&quot; then
    return target
  end

  return nil
end

function Tempest.require_target()
  local tgt = Tempest.get_target()

  if not tgt or tgt == &quot;&quot; then
    cecho(&quot;&lt;red&gt;[Tempest] No target set. Use: tt &lt;name&gt;\n&quot;)
    return nil
  end

  return tgt
end

function Tempest.normalize_favors(value, silent)
  local text = tostring(value or &quot;&quot;):match(&quot;^%s*(.-)%s*$&quot;):lower()
  if text == &quot;&quot; then
    return nil
  end

  if text == &quot;min&quot; or text == &quot;max&quot; or text:match(&quot;^%d+$&quot;) then
    return text
  end

  if not silent then
    cecho(&quot;&lt;red&gt;[Tempest] Invalid favors value. Use: min, max, or a number.\n&quot;)
  end

  return nil
end

function Tempest.send_pray(gift, target_name, favors)
  local cmd = &quot;pray &quot; .. gift
  local target_text = tostring(target_name or &quot;&quot;):match(&quot;^%s*(.-)%s*$&quot;)
  if target_text ~= &quot;&quot; then
    cmd = cmd .. &quot; &quot; .. target_text
  end

  local favors_text = Tempest.normalize_favors(favors)
  if favors_text then
    cmd = cmd .. &quot; &quot; .. favors_text
  end

  send(cmd)
end

local function resolve_attack_target(explicit_target)
  local text = tostring(explicit_target or &quot;&quot;):match(&quot;^%s*(.-)%s*$&quot;)
  if text ~= &quot;&quot; then
    return text
  end
  return Tempest.get_target()
end

function Tempest.send_basic_attack(attack, explicit_target)
  local target_name = resolve_attack_target(explicit_target)
  if target_name and target_name ~= &quot;&quot; then
    send(attack .. &quot; &quot; .. target_name)
  else
    send(attack)
  end
end

function Tempest.set_risk_band(value)
  local band = tostring(value or &quot;&quot;):lower():match(&quot;^%s*(.-)%s*$&quot;)
  local valid = {
    good = true,
    neutral = true,
    bad = true,
    critical = true,
  }

  if not valid[band] then
    cecho(&quot;&lt;red&gt;[Tempest] Invalid risk band. Use: good, neutral, bad, critical.\n&quot;)
    return false
  end

  Tempest.risk_band = band
  cecho(&quot;&lt;cyan&gt;[Tempest] Risk band set: &lt;white&gt;&quot; .. band .. &quot;\n&quot;)
  return true
end

function Tempest.send_melee(kind, explicit_target)
  local key = tostring(kind or &quot;&quot;):lower()
  local attack = nil

  if key == &quot;safe&quot; then
    attack = &quot;pound&quot;
  elseif key == &quot;mid&quot; then
    attack = &quot;crush&quot;
  elseif key == &quot;heavy&quot; then
    attack = &quot;smash&quot;
  elseif key == &quot;auto&quot; then
    if Tempest.risk_band == &quot;good&quot; then
      attack = &quot;smash&quot;
    elseif Tempest.risk_band == &quot;bad&quot; or Tempest.risk_band == &quot;critical&quot; then
      attack = &quot;pound&quot;
    else
      attack = &quot;crush&quot;
    end
  else
    cecho(&quot;&lt;red&gt;[Tempest] Unknown melee mode. Use: safe, mid, heavy, auto.\n&quot;)
    return false
  end

  Tempest.send_basic_attack(attack, explicit_target)
  return true
end</script>
        <eventHandlerList />quot;):lower()
end

local function is_target_self(name)
  local self_name = Tempest.get_self_name()
  if self_name == &quot;&quot; or name == &quot;&quot; then
    return false
  end
  return norm_lower(name) == norm_lower(self_name)
end

function Tempest.set_weapon_line(line)
  local key = norm_lower(line)
  if key ~= &quot;sharp&quot; and key ~= &quot;blunt&quot; then
    cecho(&quot;&lt;red&gt;[Tempest] weapon_line must be sharp or blunt.\n&quot;)
    return false
  end
  Tempest.weapon_line = key
  cecho(&quot;&lt;cyan&gt;[Tempest] Weapon line: &lt;white&gt;&quot; .. key .. &quot;\n&quot;)
  return true
end

function Tempest.melee_verb_for_tier(tier)
  local t = tostring(tier or &quot;&quot;):lower()
  local wl = Tempest.weapon_line or &quot;blunt&quot;
  if t == &quot;safe&quot; then
    return wl == &quot;sharp&quot; and &quot;slash&quot; or &quot;pound&quot;
  end
  if t == &quot;mid&quot; then
    return wl == &quot;sharp&quot; and &quot;slice&quot; or &quot;crush&quot;
  end
  if t == &quot;heavy&quot; then
    return wl == &quot;sharp&quot; and &quot;cleave&quot; or &quot;smash&quot;
  end
  return nil
end

function Tempest.set_target(name)
  name = tostring(name or &quot;&quot;):match(&quot;^%s*(.-)%s*<name>target helper</name>
        <packageName></packageName>
        <script>Tempest = Tempest or {}

Tempest.target = Tempest.target or &quot;&quot;
Tempest.risk_band = Tempest.risk_band or &quot;neutral&quot;
Tempest.weapon_line = Tempest.weapon_line or &quot;blunt&quot;
Tempest.character_name = Tempest.character_name or &quot;&quot;

function Tempest.set_character_name(name)
  Tempest.character_name = tostring(name or &quot;&quot;):match(&quot;^%s*(.-)%s*<name>target helper</name>
        <packageName></packageName>
        <script>Tempest = Tempest or {}

Tempest.target = Tempest.target or &quot;&quot;
Tempest.risk_band = Tempest.risk_band or &quot;neutral&quot;

function Tempest.set_target(name)
  name = tostring(name or &quot;&quot;):match(&quot;^%s*(.-)%s*$&quot;)

  if name == &quot;&quot; then
    cecho(&quot;&lt;red&gt;[Tempest] No target supplied.\n&quot;)
    return false
  end

  Tempest.target = name

  -- Compatibility with older/simple aliases that may expect a global target.
  target = name

  cecho(&quot;&lt;cyan&gt;[Tempest] Target set: &lt;white&gt;&quot; .. name .. &quot;\n&quot;)
  return true
end

function Tempest.get_target()
  if Tempest.target and Tempest.target ~= &quot;&quot; then
    return Tempest.target
  end

  if target and target ~= &quot;&quot; then
    return target
  end

  return nil
end

function Tempest.require_target()
  local tgt = Tempest.get_target()

  if not tgt or tgt == &quot;&quot; then
    cecho(&quot;&lt;red&gt;[Tempest] No target set. Use: tt &lt;name&gt;\n&quot;)
    return nil
  end

  return tgt
end

function Tempest.normalize_favors(value, silent)
  local text = tostring(value or &quot;&quot;):match(&quot;^%s*(.-)%s*$&quot;):lower()
  if text == &quot;&quot; then
    return nil
  end

  if text == &quot;min&quot; or text == &quot;max&quot; or text:match(&quot;^%d+$&quot;) then
    return text
  end

  if not silent then
    cecho(&quot;&lt;red&gt;[Tempest] Invalid favors value. Use: min, max, or a number.\n&quot;)
  end

  return nil
end

function Tempest.send_pray(gift, target_name, favors)
  local cmd = &quot;pray &quot; .. gift
  local target_text = tostring(target_name or &quot;&quot;):match(&quot;^%s*(.-)%s*$&quot;)
  if target_text ~= &quot;&quot; then
    cmd = cmd .. &quot; &quot; .. target_text
  end

  local favors_text = Tempest.normalize_favors(favors)
  if favors_text then
    cmd = cmd .. &quot; &quot; .. favors_text
  end

  send(cmd)
end

local function resolve_attack_target(explicit_target)
  local text = tostring(explicit_target or &quot;&quot;):match(&quot;^%s*(.-)%s*$&quot;)
  if text ~= &quot;&quot; then
    return text
  end
  return Tempest.get_target()
end

function Tempest.send_basic_attack(attack, explicit_target)
  local target_name = resolve_attack_target(explicit_target)
  if target_name and target_name ~= &quot;&quot; then
    send(attack .. &quot; &quot; .. target_name)
  else
    send(attack)
  end
end

function Tempest.set_risk_band(value)
  local band = tostring(value or &quot;&quot;):lower():match(&quot;^%s*(.-)%s*$&quot;)
  local valid = {
    good = true,
    neutral = true,
    bad = true,
    critical = true,
  }

  if not valid[band] then
    cecho(&quot;&lt;red&gt;[Tempest] Invalid risk band. Use: good, neutral, bad, critical.\n&quot;)
    return false
  end

  Tempest.risk_band = band
  cecho(&quot;&lt;cyan&gt;[Tempest] Risk band set: &lt;white&gt;&quot; .. band .. &quot;\n&quot;)
  return true
end

function Tempest.send_melee(kind, explicit_target)
  local key = tostring(kind or &quot;&quot;):lower()
  local attack = nil

  if key == &quot;safe&quot; then
    attack = &quot;pound&quot;
  elseif key == &quot;mid&quot; then
    attack = &quot;crush&quot;
  elseif key == &quot;heavy&quot; then
    attack = &quot;smash&quot;
  elseif key == &quot;auto&quot; then
    if Tempest.risk_band == &quot;good&quot; then
      attack = &quot;smash&quot;
    elseif Tempest.risk_band == &quot;bad&quot; or Tempest.risk_band == &quot;critical&quot; then
      attack = &quot;pound&quot;
    else
      attack = &quot;crush&quot;
    end
  else
    cecho(&quot;&lt;red&gt;[Tempest] Unknown melee mode. Use: safe, mid, heavy, auto.\n&quot;)
    return false
  end

  Tempest.send_basic_attack(attack, explicit_target)
  return true
end</script>
        <eventHandlerList />quot;)

  if name == &quot;&quot; then
    cecho(&quot;&lt;red&gt;[Tempest] No target supplied.\n&quot;)
    return false
  end

  Tempest.target = name

  -- Compatibility with older/simple aliases that may expect a global target.
  target = name

  cecho(&quot;&lt;cyan&gt;[Tempest] Target set: &lt;white&gt;&quot; .. name .. &quot;\n&quot;)
  return true
end

function Tempest.get_target()
  if Tempest.target and Tempest.target ~= &quot;&quot; then
    return Tempest.target
  end

  if target and target ~= &quot;&quot; then
    return target
  end

  return nil
end

function Tempest.require_target()
  local tgt = Tempest.get_target()

  if not tgt or tgt == &quot;&quot; then
    cecho(&quot;&lt;red&gt;[Tempest] No target set. Use: tt &lt;name&gt;\n&quot;)
    return nil
  end

  if is_target_self(tgt) then
    cecho(&quot;&lt;red&gt;[Tempest] Target is your character name; clear or change target with tt.\n&quot;)
    return nil
  end

  return tgt
end

function Tempest.normalize_favors(value, silent)
  local text = tostring(value or &quot;&quot;):match(&quot;^%s*(.-)%s*<name>target helper</name>
        <packageName></packageName>
        <script>Tempest = Tempest or {}

Tempest.target = Tempest.target or &quot;&quot;
Tempest.risk_band = Tempest.risk_band or &quot;neutral&quot;
Tempest.weapon_line = Tempest.weapon_line or &quot;blunt&quot;
Tempest.character_name = Tempest.character_name or &quot;&quot;

function Tempest.set_character_name(name)
  Tempest.character_name = tostring(name or &quot;&quot;):match(&quot;^%s*(.-)%s*<name>target helper</name>
        <packageName></packageName>
        <script>Tempest = Tempest or {}

Tempest.target = Tempest.target or &quot;&quot;
Tempest.risk_band = Tempest.risk_band or &quot;neutral&quot;

function Tempest.set_target(name)
  name = tostring(name or &quot;&quot;):match(&quot;^%s*(.-)%s*$&quot;)

  if name == &quot;&quot; then
    cecho(&quot;&lt;red&gt;[Tempest] No target supplied.\n&quot;)
    return false
  end

  Tempest.target = name

  -- Compatibility with older/simple aliases that may expect a global target.
  target = name

  cecho(&quot;&lt;cyan&gt;[Tempest] Target set: &lt;white&gt;&quot; .. name .. &quot;\n&quot;)
  return true
end

function Tempest.get_target()
  if Tempest.target and Tempest.target ~= &quot;&quot; then
    return Tempest.target
  end

  if target and target ~= &quot;&quot; then
    return target
  end

  return nil
end

function Tempest.require_target()
  local tgt = Tempest.get_target()

  if not tgt or tgt == &quot;&quot; then
    cecho(&quot;&lt;red&gt;[Tempest] No target set. Use: tt &lt;name&gt;\n&quot;)
    return nil
  end

  return tgt
end

function Tempest.normalize_favors(value, silent)
  local text = tostring(value or &quot;&quot;):match(&quot;^%s*(.-)%s*$&quot;):lower()
  if text == &quot;&quot; then
    return nil
  end

  if text == &quot;min&quot; or text == &quot;max&quot; or text:match(&quot;^%d+$&quot;) then
    return text
  end

  if not silent then
    cecho(&quot;&lt;red&gt;[Tempest] Invalid favors value. Use: min, max, or a number.\n&quot;)
  end

  return nil
end

function Tempest.send_pray(gift, target_name, favors)
  local cmd = &quot;pray &quot; .. gift
  local target_text = tostring(target_name or &quot;&quot;):match(&quot;^%s*(.-)%s*$&quot;)
  if target_text ~= &quot;&quot; then
    cmd = cmd .. &quot; &quot; .. target_text
  end

  local favors_text = Tempest.normalize_favors(favors)
  if favors_text then
    cmd = cmd .. &quot; &quot; .. favors_text
  end

  send(cmd)
end

local function resolve_attack_target(explicit_target)
  local text = tostring(explicit_target or &quot;&quot;):match(&quot;^%s*(.-)%s*$&quot;)
  if text ~= &quot;&quot; then
    return text
  end
  return Tempest.get_target()
end

function Tempest.send_basic_attack(attack, explicit_target)
  local target_name = resolve_attack_target(explicit_target)
  if target_name and target_name ~= &quot;&quot; then
    send(attack .. &quot; &quot; .. target_name)
  else
    send(attack)
  end
end

function Tempest.set_risk_band(value)
  local band = tostring(value or &quot;&quot;):lower():match(&quot;^%s*(.-)%s*$&quot;)
  local valid = {
    good = true,
    neutral = true,
    bad = true,
    critical = true,
  }

  if not valid[band] then
    cecho(&quot;&lt;red&gt;[Tempest] Invalid risk band. Use: good, neutral, bad, critical.\n&quot;)
    return false
  end

  Tempest.risk_band = band
  cecho(&quot;&lt;cyan&gt;[Tempest] Risk band set: &lt;white&gt;&quot; .. band .. &quot;\n&quot;)
  return true
end

function Tempest.send_melee(kind, explicit_target)
  local key = tostring(kind or &quot;&quot;):lower()
  local attack = nil

  if key == &quot;safe&quot; then
    attack = &quot;pound&quot;
  elseif key == &quot;mid&quot; then
    attack = &quot;crush&quot;
  elseif key == &quot;heavy&quot; then
    attack = &quot;smash&quot;
  elseif key == &quot;auto&quot; then
    if Tempest.risk_band == &quot;good&quot; then
      attack = &quot;smash&quot;
    elseif Tempest.risk_band == &quot;bad&quot; or Tempest.risk_band == &quot;critical&quot; then
      attack = &quot;pound&quot;
    else
      attack = &quot;crush&quot;
    end
  else
    cecho(&quot;&lt;red&gt;[Tempest] Unknown melee mode. Use: safe, mid, heavy, auto.\n&quot;)
    return false
  end

  Tempest.send_basic_attack(attack, explicit_target)
  return true
end</script>
        <eventHandlerList />quot;):lower()
  if text == &quot;&quot; then
    return nil
  end

  if text == &quot;min&quot; or text == &quot;max&quot; or text:match(&quot;^%d+<name>target helper</name>
        <packageName></packageName>
        <script>Tempest = Tempest or {}

Tempest.target = Tempest.target or &quot;&quot;
Tempest.risk_band = Tempest.risk_band or &quot;neutral&quot;
Tempest.weapon_line = Tempest.weapon_line or &quot;blunt&quot;
Tempest.character_name = Tempest.character_name or &quot;&quot;

function Tempest.set_character_name(name)
  Tempest.character_name = tostring(name or &quot;&quot;):match(&quot;^%s*(.-)%s*<name>target helper</name>
        <packageName></packageName>
        <script>Tempest = Tempest or {}

Tempest.target = Tempest.target or &quot;&quot;
Tempest.risk_band = Tempest.risk_band or &quot;neutral&quot;

function Tempest.set_target(name)
  name = tostring(name or &quot;&quot;):match(&quot;^%s*(.-)%s*$&quot;)

  if name == &quot;&quot; then
    cecho(&quot;&lt;red&gt;[Tempest] No target supplied.\n&quot;)
    return false
  end

  Tempest.target = name

  -- Compatibility with older/simple aliases that may expect a global target.
  target = name

  cecho(&quot;&lt;cyan&gt;[Tempest] Target set: &lt;white&gt;&quot; .. name .. &quot;\n&quot;)
  return true
end

function Tempest.get_target()
  if Tempest.target and Tempest.target ~= &quot;&quot; then
    return Tempest.target
  end

  if target and target ~= &quot;&quot; then
    return target
  end

  return nil
end

function Tempest.require_target()
  local tgt = Tempest.get_target()

  if not tgt or tgt == &quot;&quot; then
    cecho(&quot;&lt;red&gt;[Tempest] No target set. Use: tt &lt;name&gt;\n&quot;)
    return nil
  end

  return tgt
end

function Tempest.normalize_favors(value, silent)
  local text = tostring(value or &quot;&quot;):match(&quot;^%s*(.-)%s*$&quot;):lower()
  if text == &quot;&quot; then
    return nil
  end

  if text == &quot;min&quot; or text == &quot;max&quot; or text:match(&quot;^%d+$&quot;) then
    return text
  end

  if not silent then
    cecho(&quot;&lt;red&gt;[Tempest] Invalid favors value. Use: min, max, or a number.\n&quot;)
  end

  return nil
end

function Tempest.send_pray(gift, target_name, favors)
  local cmd = &quot;pray &quot; .. gift
  local target_text = tostring(target_name or &quot;&quot;):match(&quot;^%s*(.-)%s*$&quot;)
  if target_text ~= &quot;&quot; then
    cmd = cmd .. &quot; &quot; .. target_text
  end

  local favors_text = Tempest.normalize_favors(favors)
  if favors_text then
    cmd = cmd .. &quot; &quot; .. favors_text
  end

  send(cmd)
end

local function resolve_attack_target(explicit_target)
  local text = tostring(explicit_target or &quot;&quot;):match(&quot;^%s*(.-)%s*$&quot;)
  if text ~= &quot;&quot; then
    return text
  end
  return Tempest.get_target()
end

function Tempest.send_basic_attack(attack, explicit_target)
  local target_name = resolve_attack_target(explicit_target)
  if target_name and target_name ~= &quot;&quot; then
    send(attack .. &quot; &quot; .. target_name)
  else
    send(attack)
  end
end

function Tempest.set_risk_band(value)
  local band = tostring(value or &quot;&quot;):lower():match(&quot;^%s*(.-)%s*$&quot;)
  local valid = {
    good = true,
    neutral = true,
    bad = true,
    critical = true,
  }

  if not valid[band] then
    cecho(&quot;&lt;red&gt;[Tempest] Invalid risk band. Use: good, neutral, bad, critical.\n&quot;)
    return false
  end

  Tempest.risk_band = band
  cecho(&quot;&lt;cyan&gt;[Tempest] Risk band set: &lt;white&gt;&quot; .. band .. &quot;\n&quot;)
  return true
end

function Tempest.send_melee(kind, explicit_target)
  local key = tostring(kind or &quot;&quot;):lower()
  local attack = nil

  if key == &quot;safe&quot; then
    attack = &quot;pound&quot;
  elseif key == &quot;mid&quot; then
    attack = &quot;crush&quot;
  elseif key == &quot;heavy&quot; then
    attack = &quot;smash&quot;
  elseif key == &quot;auto&quot; then
    if Tempest.risk_band == &quot;good&quot; then
      attack = &quot;smash&quot;
    elseif Tempest.risk_band == &quot;bad&quot; or Tempest.risk_band == &quot;critical&quot; then
      attack = &quot;pound&quot;
    else
      attack = &quot;crush&quot;
    end
  else
    cecho(&quot;&lt;red&gt;[Tempest] Unknown melee mode. Use: safe, mid, heavy, auto.\n&quot;)
    return false
  end

  Tempest.send_basic_attack(attack, explicit_target)
  return true
end</script>
        <eventHandlerList />quot;) then
    return text
  end

  if not silent then
    cecho(&quot;&lt;red&gt;[Tempest] Invalid favors value. Use: min, max, or a number.\n&quot;)
  end
  return nil
end

function Tempest.send_pray(gift, target_name, favors)
  local cmd = &quot;pray &quot; .. gift
  local target_text = tostring(target_name or &quot;&quot;):match(&quot;^%s*(.-)%s*<name>target helper</name>
        <packageName></packageName>
        <script>Tempest = Tempest or {}

Tempest.target = Tempest.target or &quot;&quot;
Tempest.risk_band = Tempest.risk_band or &quot;neutral&quot;
Tempest.weapon_line = Tempest.weapon_line or &quot;blunt&quot;
Tempest.character_name = Tempest.character_name or &quot;&quot;

function Tempest.set_character_name(name)
  Tempest.character_name = tostring(name or &quot;&quot;):match(&quot;^%s*(.-)%s*<name>target helper</name>
        <packageName></packageName>
        <script>Tempest = Tempest or {}

Tempest.target = Tempest.target or &quot;&quot;
Tempest.risk_band = Tempest.risk_band or &quot;neutral&quot;

function Tempest.set_target(name)
  name = tostring(name or &quot;&quot;):match(&quot;^%s*(.-)%s*$&quot;)

  if name == &quot;&quot; then
    cecho(&quot;&lt;red&gt;[Tempest] No target supplied.\n&quot;)
    return false
  end

  Tempest.target = name

  -- Compatibility with older/simple aliases that may expect a global target.
  target = name

  cecho(&quot;&lt;cyan&gt;[Tempest] Target set: &lt;white&gt;&quot; .. name .. &quot;\n&quot;)
  return true
end

function Tempest.get_target()
  if Tempest.target and Tempest.target ~= &quot;&quot; then
    return Tempest.target
  end

  if target and target ~= &quot;&quot; then
    return target
  end

  return nil
end

function Tempest.require_target()
  local tgt = Tempest.get_target()

  if not tgt or tgt == &quot;&quot; then
    cecho(&quot;&lt;red&gt;[Tempest] No target set. Use: tt &lt;name&gt;\n&quot;)
    return nil
  end

  return tgt
end

function Tempest.normalize_favors(value, silent)
  local text = tostring(value or &quot;&quot;):match(&quot;^%s*(.-)%s*$&quot;):lower()
  if text == &quot;&quot; then
    return nil
  end

  if text == &quot;min&quot; or text == &quot;max&quot; or text:match(&quot;^%d+$&quot;) then
    return text
  end

  if not silent then
    cecho(&quot;&lt;red&gt;[Tempest] Invalid favors value. Use: min, max, or a number.\n&quot;)
  end

  return nil
end

function Tempest.send_pray(gift, target_name, favors)
  local cmd = &quot;pray &quot; .. gift
  local target_text = tostring(target_name or &quot;&quot;):match(&quot;^%s*(.-)%s*$&quot;)
  if target_text ~= &quot;&quot; then
    cmd = cmd .. &quot; &quot; .. target_text
  end

  local favors_text = Tempest.normalize_favors(favors)
  if favors_text then
    cmd = cmd .. &quot; &quot; .. favors_text
  end

  send(cmd)
end

local function resolve_attack_target(explicit_target)
  local text = tostring(explicit_target or &quot;&quot;):match(&quot;^%s*(.-)%s*$&quot;)
  if text ~= &quot;&quot; then
    return text
  end
  return Tempest.get_target()
end

function Tempest.send_basic_attack(attack, explicit_target)
  local target_name = resolve_attack_target(explicit_target)
  if target_name and target_name ~= &quot;&quot; then
    send(attack .. &quot; &quot; .. target_name)
  else
    send(attack)
  end
end

function Tempest.set_risk_band(value)
  local band = tostring(value or &quot;&quot;):lower():match(&quot;^%s*(.-)%s*$&quot;)
  local valid = {
    good = true,
    neutral = true,
    bad = true,
    critical = true,
  }

  if not valid[band] then
    cecho(&quot;&lt;red&gt;[Tempest] Invalid risk band. Use: good, neutral, bad, critical.\n&quot;)
    return false
  end

  Tempest.risk_band = band
  cecho(&quot;&lt;cyan&gt;[Tempest] Risk band set: &lt;white&gt;&quot; .. band .. &quot;\n&quot;)
  return true
end

function Tempest.send_melee(kind, explicit_target)
  local key = tostring(kind or &quot;&quot;):lower()
  local attack = nil

  if key == &quot;safe&quot; then
    attack = &quot;pound&quot;
  elseif key == &quot;mid&quot; then
    attack = &quot;crush&quot;
  elseif key == &quot;heavy&quot; then
    attack = &quot;smash&quot;
  elseif key == &quot;auto&quot; then
    if Tempest.risk_band == &quot;good&quot; then
      attack = &quot;smash&quot;
    elseif Tempest.risk_band == &quot;bad&quot; or Tempest.risk_band == &quot;critical&quot; then
      attack = &quot;pound&quot;
    else
      attack = &quot;crush&quot;
    end
  else
    cecho(&quot;&lt;red&gt;[Tempest] Unknown melee mode. Use: safe, mid, heavy, auto.\n&quot;)
    return false
  end

  Tempest.send_basic_attack(attack, explicit_target)
  return true
end</script>
        <eventHandlerList />quot;)
  if target_text ~= &quot;&quot; then
    cmd = cmd .. &quot; &quot; .. target_text
  end

  local favors_text = Tempest.normalize_favors(favors)
  if favors_text then
    cmd = cmd .. &quot; &quot; .. favors_text
  end

  send(cmd)
end

local function resolve_attack_target(explicit_target)
  local text = tostring(explicit_target or &quot;&quot;):match(&quot;^%s*(.-)%s*<name>target helper</name>
        <packageName></packageName>
        <script>Tempest = Tempest or {}

Tempest.target = Tempest.target or &quot;&quot;
Tempest.risk_band = Tempest.risk_band or &quot;neutral&quot;
Tempest.weapon_line = Tempest.weapon_line or &quot;blunt&quot;
Tempest.character_name = Tempest.character_name or &quot;&quot;

function Tempest.set_character_name(name)
  Tempest.character_name = tostring(name or &quot;&quot;):match(&quot;^%s*(.-)%s*<name>target helper</name>
        <packageName></packageName>
        <script>Tempest = Tempest or {}

Tempest.target = Tempest.target or &quot;&quot;
Tempest.risk_band = Tempest.risk_band or &quot;neutral&quot;

function Tempest.set_target(name)
  name = tostring(name or &quot;&quot;):match(&quot;^%s*(.-)%s*$&quot;)

  if name == &quot;&quot; then
    cecho(&quot;&lt;red&gt;[Tempest] No target supplied.\n&quot;)
    return false
  end

  Tempest.target = name

  -- Compatibility with older/simple aliases that may expect a global target.
  target = name

  cecho(&quot;&lt;cyan&gt;[Tempest] Target set: &lt;white&gt;&quot; .. name .. &quot;\n&quot;)
  return true
end

function Tempest.get_target()
  if Tempest.target and Tempest.target ~= &quot;&quot; then
    return Tempest.target
  end

  if target and target ~= &quot;&quot; then
    return target
  end

  return nil
end

function Tempest.require_target()
  local tgt = Tempest.get_target()

  if not tgt or tgt == &quot;&quot; then
    cecho(&quot;&lt;red&gt;[Tempest] No target set. Use: tt &lt;name&gt;\n&quot;)
    return nil
  end

  return tgt
end

function Tempest.normalize_favors(value, silent)
  local text = tostring(value or &quot;&quot;):match(&quot;^%s*(.-)%s*$&quot;):lower()
  if text == &quot;&quot; then
    return nil
  end

  if text == &quot;min&quot; or text == &quot;max&quot; or text:match(&quot;^%d+$&quot;) then
    return text
  end

  if not silent then
    cecho(&quot;&lt;red&gt;[Tempest] Invalid favors value. Use: min, max, or a number.\n&quot;)
  end

  return nil
end

function Tempest.send_pray(gift, target_name, favors)
  local cmd = &quot;pray &quot; .. gift
  local target_text = tostring(target_name or &quot;&quot;):match(&quot;^%s*(.-)%s*$&quot;)
  if target_text ~= &quot;&quot; then
    cmd = cmd .. &quot; &quot; .. target_text
  end

  local favors_text = Tempest.normalize_favors(favors)
  if favors_text then
    cmd = cmd .. &quot; &quot; .. favors_text
  end

  send(cmd)
end

local function resolve_attack_target(explicit_target)
  local text = tostring(explicit_target or &quot;&quot;):match(&quot;^%s*(.-)%s*$&quot;)
  if text ~= &quot;&quot; then
    return text
  end
  return Tempest.get_target()
end

function Tempest.send_basic_attack(attack, explicit_target)
  local target_name = resolve_attack_target(explicit_target)
  if target_name and target_name ~= &quot;&quot; then
    send(attack .. &quot; &quot; .. target_name)
  else
    send(attack)
  end
end

function Tempest.set_risk_band(value)
  local band = tostring(value or &quot;&quot;):lower():match(&quot;^%s*(.-)%s*$&quot;)
  local valid = {
    good = true,
    neutral = true,
    bad = true,
    critical = true,
  }

  if not valid[band] then
    cecho(&quot;&lt;red&gt;[Tempest] Invalid risk band. Use: good, neutral, bad, critical.\n&quot;)
    return false
  end

  Tempest.risk_band = band
  cecho(&quot;&lt;cyan&gt;[Tempest] Risk band set: &lt;white&gt;&quot; .. band .. &quot;\n&quot;)
  return true
end

function Tempest.send_melee(kind, explicit_target)
  local key = tostring(kind or &quot;&quot;):lower()
  local attack = nil

  if key == &quot;safe&quot; then
    attack = &quot;pound&quot;
  elseif key == &quot;mid&quot; then
    attack = &quot;crush&quot;
  elseif key == &quot;heavy&quot; then
    attack = &quot;smash&quot;
  elseif key == &quot;auto&quot; then
    if Tempest.risk_band == &quot;good&quot; then
      attack = &quot;smash&quot;
    elseif Tempest.risk_band == &quot;bad&quot; or Tempest.risk_band == &quot;critical&quot; then
      attack = &quot;pound&quot;
    else
      attack = &quot;crush&quot;
    end
  else
    cecho(&quot;&lt;red&gt;[Tempest] Unknown melee mode. Use: safe, mid, heavy, auto.\n&quot;)
    return false
  end

  Tempest.send_basic_attack(attack, explicit_target)
  return true
end</script>
        <eventHandlerList />quot;)
  if text ~= &quot;&quot; then
    return text
  end
  return Tempest.get_target()
end

function Tempest.send_basic_attack(attack, explicit_target)
  local target_name = resolve_attack_target(explicit_target)
  if target_name and target_name ~= &quot;&quot; then
    if is_target_self(target_name) then
      cecho(&quot;&lt;red&gt;[Tempest] Refusing to attack yourself; fix target (tt) or character name (Tempest.set_character_name).\n&quot;)
      return false
    end
    send(attack .. &quot; &quot; .. target_name)
  else
    send(attack)
  end
  return true
end

function Tempest.set_risk_band(value)
  local band = tostring(value or &quot;&quot;):lower():match(&quot;^%s*(.-)%s*<name>target helper</name>
        <packageName></packageName>
        <script>Tempest = Tempest or {}

Tempest.target = Tempest.target or &quot;&quot;
Tempest.risk_band = Tempest.risk_band or &quot;neutral&quot;
Tempest.weapon_line = Tempest.weapon_line or &quot;blunt&quot;
Tempest.character_name = Tempest.character_name or &quot;&quot;

function Tempest.set_character_name(name)
  Tempest.character_name = tostring(name or &quot;&quot;):match(&quot;^%s*(.-)%s*<name>target helper</name>
        <packageName></packageName>
        <script>Tempest = Tempest or {}

Tempest.target = Tempest.target or &quot;&quot;
Tempest.risk_band = Tempest.risk_band or &quot;neutral&quot;

function Tempest.set_target(name)
  name = tostring(name or &quot;&quot;):match(&quot;^%s*(.-)%s*$&quot;)

  if name == &quot;&quot; then
    cecho(&quot;&lt;red&gt;[Tempest] No target supplied.\n&quot;)
    return false
  end

  Tempest.target = name

  -- Compatibility with older/simple aliases that may expect a global target.
  target = name

  cecho(&quot;&lt;cyan&gt;[Tempest] Target set: &lt;white&gt;&quot; .. name .. &quot;\n&quot;)
  return true
end

function Tempest.get_target()
  if Tempest.target and Tempest.target ~= &quot;&quot; then
    return Tempest.target
  end

  if target and target ~= &quot;&quot; then
    return target
  end

  return nil
end

function Tempest.require_target()
  local tgt = Tempest.get_target()

  if not tgt or tgt == &quot;&quot; then
    cecho(&quot;&lt;red&gt;[Tempest] No target set. Use: tt &lt;name&gt;\n&quot;)
    return nil
  end

  return tgt
end

function Tempest.normalize_favors(value, silent)
  local text = tostring(value or &quot;&quot;):match(&quot;^%s*(.-)%s*$&quot;):lower()
  if text == &quot;&quot; then
    return nil
  end

  if text == &quot;min&quot; or text == &quot;max&quot; or text:match(&quot;^%d+$&quot;) then
    return text
  end

  if not silent then
    cecho(&quot;&lt;red&gt;[Tempest] Invalid favors value. Use: min, max, or a number.\n&quot;)
  end

  return nil
end

function Tempest.send_pray(gift, target_name, favors)
  local cmd = &quot;pray &quot; .. gift
  local target_text = tostring(target_name or &quot;&quot;):match(&quot;^%s*(.-)%s*$&quot;)
  if target_text ~= &quot;&quot; then
    cmd = cmd .. &quot; &quot; .. target_text
  end

  local favors_text = Tempest.normalize_favors(favors)
  if favors_text then
    cmd = cmd .. &quot; &quot; .. favors_text
  end

  send(cmd)
end

local function resolve_attack_target(explicit_target)
  local text = tostring(explicit_target or &quot;&quot;):match(&quot;^%s*(.-)%s*$&quot;)
  if text ~= &quot;&quot; then
    return text
  end
  return Tempest.get_target()
end

function Tempest.send_basic_attack(attack, explicit_target)
  local target_name = resolve_attack_target(explicit_target)
  if target_name and target_name ~= &quot;&quot; then
    send(attack .. &quot; &quot; .. target_name)
  else
    send(attack)
  end
end

function Tempest.set_risk_band(value)
  local band = tostring(value or &quot;&quot;):lower():match(&quot;^%s*(.-)%s*$&quot;)
  local valid = {
    good = true,
    neutral = true,
    bad = true,
    critical = true,
  }

  if not valid[band] then
    cecho(&quot;&lt;red&gt;[Tempest] Invalid risk band. Use: good, neutral, bad, critical.\n&quot;)
    return false
  end

  Tempest.risk_band = band
  cecho(&quot;&lt;cyan&gt;[Tempest] Risk band set: &lt;white&gt;&quot; .. band .. &quot;\n&quot;)
  return true
end

function Tempest.send_melee(kind, explicit_target)
  local key = tostring(kind or &quot;&quot;):lower()
  local attack = nil

  if key == &quot;safe&quot; then
    attack = &quot;pound&quot;
  elseif key == &quot;mid&quot; then
    attack = &quot;crush&quot;
  elseif key == &quot;heavy&quot; then
    attack = &quot;smash&quot;
  elseif key == &quot;auto&quot; then
    if Tempest.risk_band == &quot;good&quot; then
      attack = &quot;smash&quot;
    elseif Tempest.risk_band == &quot;bad&quot; or Tempest.risk_band == &quot;critical&quot; then
      attack = &quot;pound&quot;
    else
      attack = &quot;crush&quot;
    end
  else
    cecho(&quot;&lt;red&gt;[Tempest] Unknown melee mode. Use: safe, mid, heavy, auto.\n&quot;)
    return false
  end

  Tempest.send_basic_attack(attack, explicit_target)
  return true
end</script>
        <eventHandlerList />quot;)
  local valid = {
    good = true,
    neutral = true,
    bad = true,
    critical = true,
  }

  if not valid[band] then
    cecho(&quot;&lt;red&gt;[Tempest] Invalid risk band. Use: good, neutral, bad, critical.\n&quot;)
    return false
  end

  Tempest.risk_band = band
  cecho(&quot;&lt;cyan&gt;[Tempest] Risk band set: &lt;white&gt;&quot; .. band .. &quot;\n&quot;)
  return true
end

function Tempest.send_melee(kind, explicit_target)
  local key = tostring(kind or &quot;&quot;):lower()
  local tier = nil

  if key == &quot;safe&quot; then
    tier = &quot;safe&quot;
  elseif key == &quot;mid&quot; then
    tier = &quot;mid&quot;
  elseif key == &quot;heavy&quot; then
    tier = &quot;heavy&quot;
  elseif key == &quot;auto&quot; then
    if Tempest.risk_band == &quot;good&quot; then
      tier = &quot;heavy&quot;
    elseif Tempest.risk_band == &quot;bad&quot; or Tempest.risk_band == &quot;critical&quot; then
      tier = &quot;safe&quot;
    else
      tier = &quot;mid&quot;
    end
  else
    cecho(&quot;&lt;red&gt;[Tempest] Unknown melee mode. Use: safe, mid, heavy, auto.\n&quot;)
    return false
  end

  local attack = Tempest.melee_verb_for_tier(tier)
  if not attack then
    return false
  end

  Tempest.send_basic_attack(attack, explicit_target)
  return true
end
</script>
        <eventHandlerList />quot;) or &quot;&quot;
  if Tempest.character_name ~= &quot;&quot; then
    cecho(&quot;&lt;cyan&gt;[Tempest] Character name set for self-checks: &lt;white&gt;&quot; .. Tempest.character_name .. &quot;\n&quot;)
  end
end

function Tempest.get_self_name()
  local n = Tempest.character_name or &quot;&quot;
  if n ~= &quot;&quot; then
    return n
  end
  if type(gmcp) == &quot;table&quot; and gmcp.Char and gmcp.Char.Name then
    return tostring(gmcp.Char.Name)
  end
  return &quot;&quot;
end

local function norm_lower(s)
  return tostring(s or &quot;&quot;):match(&quot;^%s*(.-)%s*<name>target helper</name>
        <packageName></packageName>
        <script>Tempest = Tempest or {}

Tempest.target = Tempest.target or &quot;&quot;
Tempest.risk_band = Tempest.risk_band or &quot;neutral&quot;

function Tempest.set_target(name)
  name = tostring(name or &quot;&quot;):match(&quot;^%s*(.-)%s*$&quot;)

  if name == &quot;&quot; then
    cecho(&quot;&lt;red&gt;[Tempest] No target supplied.\n&quot;)
    return false
  end

  Tempest.target = name

  -- Compatibility with older/simple aliases that may expect a global target.
  target = name

  cecho(&quot;&lt;cyan&gt;[Tempest] Target set: &lt;white&gt;&quot; .. name .. &quot;\n&quot;)
  return true
end

function Tempest.get_target()
  if Tempest.target and Tempest.target ~= &quot;&quot; then
    return Tempest.target
  end

  if target and target ~= &quot;&quot; then
    return target
  end

  return nil
end

function Tempest.require_target()
  local tgt = Tempest.get_target()

  if not tgt or tgt == &quot;&quot; then
    cecho(&quot;&lt;red&gt;[Tempest] No target set. Use: tt &lt;name&gt;\n&quot;)
    return nil
  end

  return tgt
end

function Tempest.normalize_favors(value, silent)
  local text = tostring(value or &quot;&quot;):match(&quot;^%s*(.-)%s*$&quot;):lower()
  if text == &quot;&quot; then
    return nil
  end

  if text == &quot;min&quot; or text == &quot;max&quot; or text:match(&quot;^%d+$&quot;) then
    return text
  end

  if not silent then
    cecho(&quot;&lt;red&gt;[Tempest] Invalid favors value. Use: min, max, or a number.\n&quot;)
  end

  return nil
end

function Tempest.send_pray(gift, target_name, favors)
  local cmd = &quot;pray &quot; .. gift
  local target_text = tostring(target_name or &quot;&quot;):match(&quot;^%s*(.-)%s*$&quot;)
  if target_text ~= &quot;&quot; then
    cmd = cmd .. &quot; &quot; .. target_text
  end

  local favors_text = Tempest.normalize_favors(favors)
  if favors_text then
    cmd = cmd .. &quot; &quot; .. favors_text
  end

  send(cmd)
end

local function resolve_attack_target(explicit_target)
  local text = tostring(explicit_target or &quot;&quot;):match(&quot;^%s*(.-)%s*$&quot;)
  if text ~= &quot;&quot; then
    return text
  end
  return Tempest.get_target()
end

function Tempest.send_basic_attack(attack, explicit_target)
  local target_name = resolve_attack_target(explicit_target)
  if target_name and target_name ~= &quot;&quot; then
    send(attack .. &quot; &quot; .. target_name)
  else
    send(attack)
  end
end

function Tempest.set_risk_band(value)
  local band = tostring(value or &quot;&quot;):lower():match(&quot;^%s*(.-)%s*$&quot;)
  local valid = {
    good = true,
    neutral = true,
    bad = true,
    critical = true,
  }

  if not valid[band] then
    cecho(&quot;&lt;red&gt;[Tempest] Invalid risk band. Use: good, neutral, bad, critical.\n&quot;)
    return false
  end

  Tempest.risk_band = band
  cecho(&quot;&lt;cyan&gt;[Tempest] Risk band set: &lt;white&gt;&quot; .. band .. &quot;\n&quot;)
  return true
end

function Tempest.send_melee(kind, explicit_target)
  local key = tostring(kind or &quot;&quot;):lower()
  local attack = nil

  if key == &quot;safe&quot; then
    attack = &quot;pound&quot;
  elseif key == &quot;mid&quot; then
    attack = &quot;crush&quot;
  elseif key == &quot;heavy&quot; then
    attack = &quot;smash&quot;
  elseif key == &quot;auto&quot; then
    if Tempest.risk_band == &quot;good&quot; then
      attack = &quot;smash&quot;
    elseif Tempest.risk_band == &quot;bad&quot; or Tempest.risk_band == &quot;critical&quot; then
      attack = &quot;pound&quot;
    else
      attack = &quot;crush&quot;
    end
  else
    cecho(&quot;&lt;red&gt;[Tempest] Unknown melee mode. Use: safe, mid, heavy, auto.\n&quot;)
    return false
  end

  Tempest.send_basic_attack(attack, explicit_target)
  return true
end</script>
        <eventHandlerList />quot;):lower()
end

local function is_target_self(name)
  local self_name = Tempest.get_self_name()
  if self_name == &quot;&quot; or name == &quot;&quot; then
    return false
  end
  return norm_lower(name) == norm_lower(self_name)
end

function Tempest.set_weapon_line(line)
  local key = norm_lower(line)
  if key ~= &quot;sharp&quot; and key ~= &quot;blunt&quot; then
    cecho(&quot;&lt;red&gt;[Tempest] weapon_line must be sharp or blunt.\n&quot;)
    return false
  end
  Tempest.weapon_line = key
  cecho(&quot;&lt;cyan&gt;[Tempest] Weapon line: &lt;white&gt;&quot; .. key .. &quot;\n&quot;)
  return true
end

function Tempest.melee_verb_for_tier(tier)
  local t = tostring(tier or &quot;&quot;):lower()
  local wl = Tempest.weapon_line or &quot;blunt&quot;
  if t == &quot;safe&quot; then
    return wl == &quot;sharp&quot; and &quot;slash&quot; or &quot;pound&quot;
  end
  if t == &quot;mid&quot; then
    return wl == &quot;sharp&quot; and &quot;slice&quot; or &quot;crush&quot;
  end
  if t == &quot;heavy&quot; then
    return wl == &quot;sharp&quot; and &quot;cleave&quot; or &quot;smash&quot;
  end
  return nil
end

function Tempest.set_target(name)
  name = tostring(name or &quot;&quot;):match(&quot;^%s*(.-)%s*<name>target helper</name>
        <packageName></packageName>
        <script>Tempest = Tempest or {}

Tempest.target = Tempest.target or &quot;&quot;
Tempest.risk_band = Tempest.risk_band or &quot;neutral&quot;

function Tempest.set_target(name)
  name = tostring(name or &quot;&quot;):match(&quot;^%s*(.-)%s*$&quot;)

  if name == &quot;&quot; then
    cecho(&quot;&lt;red&gt;[Tempest] No target supplied.\n&quot;)
    return false
  end

  Tempest.target = name

  -- Compatibility with older/simple aliases that may expect a global target.
  target = name

  cecho(&quot;&lt;cyan&gt;[Tempest] Target set: &lt;white&gt;&quot; .. name .. &quot;\n&quot;)
  return true
end

function Tempest.get_target()
  if Tempest.target and Tempest.target ~= &quot;&quot; then
    return Tempest.target
  end

  if target and target ~= &quot;&quot; then
    return target
  end

  return nil
end

function Tempest.require_target()
  local tgt = Tempest.get_target()

  if not tgt or tgt == &quot;&quot; then
    cecho(&quot;&lt;red&gt;[Tempest] No target set. Use: tt &lt;name&gt;\n&quot;)
    return nil
  end

  return tgt
end

function Tempest.normalize_favors(value, silent)
  local text = tostring(value or &quot;&quot;):match(&quot;^%s*(.-)%s*$&quot;):lower()
  if text == &quot;&quot; then
    return nil
  end

  if text == &quot;min&quot; or text == &quot;max&quot; or text:match(&quot;^%d+$&quot;) then
    return text
  end

  if not silent then
    cecho(&quot;&lt;red&gt;[Tempest] Invalid favors value. Use: min, max, or a number.\n&quot;)
  end

  return nil
end

function Tempest.send_pray(gift, target_name, favors)
  local cmd = &quot;pray &quot; .. gift
  local target_text = tostring(target_name or &quot;&quot;):match(&quot;^%s*(.-)%s*$&quot;)
  if target_text ~= &quot;&quot; then
    cmd = cmd .. &quot; &quot; .. target_text
  end

  local favors_text = Tempest.normalize_favors(favors)
  if favors_text then
    cmd = cmd .. &quot; &quot; .. favors_text
  end

  send(cmd)
end

local function resolve_attack_target(explicit_target)
  local text = tostring(explicit_target or &quot;&quot;):match(&quot;^%s*(.-)%s*$&quot;)
  if text ~= &quot;&quot; then
    return text
  end
  return Tempest.get_target()
end

function Tempest.send_basic_attack(attack, explicit_target)
  local target_name = resolve_attack_target(explicit_target)
  if target_name and target_name ~= &quot;&quot; then
    send(attack .. &quot; &quot; .. target_name)
  else
    send(attack)
  end
end

function Tempest.set_risk_band(value)
  local band = tostring(value or &quot;&quot;):lower():match(&quot;^%s*(.-)%s*$&quot;)
  local valid = {
    good = true,
    neutral = true,
    bad = true,
    critical = true,
  }

  if not valid[band] then
    cecho(&quot;&lt;red&gt;[Tempest] Invalid risk band. Use: good, neutral, bad, critical.\n&quot;)
    return false
  end

  Tempest.risk_band = band
  cecho(&quot;&lt;cyan&gt;[Tempest] Risk band set: &lt;white&gt;&quot; .. band .. &quot;\n&quot;)
  return true
end

function Tempest.send_melee(kind, explicit_target)
  local key = tostring(kind or &quot;&quot;):lower()
  local attack = nil

  if key == &quot;safe&quot; then
    attack = &quot;pound&quot;
  elseif key == &quot;mid&quot; then
    attack = &quot;crush&quot;
  elseif key == &quot;heavy&quot; then
    attack = &quot;smash&quot;
  elseif key == &quot;auto&quot; then
    if Tempest.risk_band == &quot;good&quot; then
      attack = &quot;smash&quot;
    elseif Tempest.risk_band == &quot;bad&quot; or Tempest.risk_band == &quot;critical&quot; then
      attack = &quot;pound&quot;
    else
      attack = &quot;crush&quot;
    end
  else
    cecho(&quot;&lt;red&gt;[Tempest] Unknown melee mode. Use: safe, mid, heavy, auto.\n&quot;)
    return false
  end

  Tempest.send_basic_attack(attack, explicit_target)
  return true
end</script>
        <eventHandlerList />quot;)

  if name == &quot;&quot; then
    cecho(&quot;&lt;red&gt;[Tempest] No target supplied.\n&quot;)
    return false
  end

  Tempest.target = name

  -- Compatibility with older/simple aliases that may expect a global target.
  target = name

  cecho(&quot;&lt;cyan&gt;[Tempest] Target set: &lt;white&gt;&quot; .. name .. &quot;\n&quot;)
  return true
end

function Tempest.get_target()
  if Tempest.target and Tempest.target ~= &quot;&quot; then
    return Tempest.target
  end

  if target and target ~= &quot;&quot; then
    return target
  end

  return nil
end

function Tempest.require_target()
  local tgt = Tempest.get_target()

  if not tgt or tgt == &quot;&quot; then
    cecho(&quot;&lt;red&gt;[Tempest] No target set. Use: tt &lt;name&gt;\n&quot;)
    return nil
  end

  if is_target_self(tgt) then
    cecho(&quot;&lt;red&gt;[Tempest] Target is your character name; clear or change target with tt.\n&quot;)
    return nil
  end

  return tgt
end

function Tempest.normalize_favors(value, silent)
  local text = tostring(value or &quot;&quot;):match(&quot;^%s*(.-)%s*<name>target helper</name>
        <packageName></packageName>
        <script>Tempest = Tempest or {}

Tempest.target = Tempest.target or &quot;&quot;
Tempest.risk_band = Tempest.risk_band or &quot;neutral&quot;

function Tempest.set_target(name)
  name = tostring(name or &quot;&quot;):match(&quot;^%s*(.-)%s*$&quot;)

  if name == &quot;&quot; then
    cecho(&quot;&lt;red&gt;[Tempest] No target supplied.\n&quot;)
    return false
  end

  Tempest.target = name

  -- Compatibility with older/simple aliases that may expect a global target.
  target = name

  cecho(&quot;&lt;cyan&gt;[Tempest] Target set: &lt;white&gt;&quot; .. name .. &quot;\n&quot;)
  return true
end

function Tempest.get_target()
  if Tempest.target and Tempest.target ~= &quot;&quot; then
    return Tempest.target
  end

  if target and target ~= &quot;&quot; then
    return target
  end

  return nil
end

function Tempest.require_target()
  local tgt = Tempest.get_target()

  if not tgt or tgt == &quot;&quot; then
    cecho(&quot;&lt;red&gt;[Tempest] No target set. Use: tt &lt;name&gt;\n&quot;)
    return nil
  end

  return tgt
end

function Tempest.normalize_favors(value, silent)
  local text = tostring(value or &quot;&quot;):match(&quot;^%s*(.-)%s*$&quot;):lower()
  if text == &quot;&quot; then
    return nil
  end

  if text == &quot;min&quot; or text == &quot;max&quot; or text:match(&quot;^%d+$&quot;) then
    return text
  end

  if not silent then
    cecho(&quot;&lt;red&gt;[Tempest] Invalid favors value. Use: min, max, or a number.\n&quot;)
  end

  return nil
end

function Tempest.send_pray(gift, target_name, favors)
  local cmd = &quot;pray &quot; .. gift
  local target_text = tostring(target_name or &quot;&quot;):match(&quot;^%s*(.-)%s*$&quot;)
  if target_text ~= &quot;&quot; then
    cmd = cmd .. &quot; &quot; .. target_text
  end

  local favors_text = Tempest.normalize_favors(favors)
  if favors_text then
    cmd = cmd .. &quot; &quot; .. favors_text
  end

  send(cmd)
end

local function resolve_attack_target(explicit_target)
  local text = tostring(explicit_target or &quot;&quot;):match(&quot;^%s*(.-)%s*$&quot;)
  if text ~= &quot;&quot; then
    return text
  end
  return Tempest.get_target()
end

function Tempest.send_basic_attack(attack, explicit_target)
  local target_name = resolve_attack_target(explicit_target)
  if target_name and target_name ~= &quot;&quot; then
    send(attack .. &quot; &quot; .. target_name)
  else
    send(attack)
  end
end

function Tempest.set_risk_band(value)
  local band = tostring(value or &quot;&quot;):lower():match(&quot;^%s*(.-)%s*$&quot;)
  local valid = {
    good = true,
    neutral = true,
    bad = true,
    critical = true,
  }

  if not valid[band] then
    cecho(&quot;&lt;red&gt;[Tempest] Invalid risk band. Use: good, neutral, bad, critical.\n&quot;)
    return false
  end

  Tempest.risk_band = band
  cecho(&quot;&lt;cyan&gt;[Tempest] Risk band set: &lt;white&gt;&quot; .. band .. &quot;\n&quot;)
  return true
end

function Tempest.send_melee(kind, explicit_target)
  local key = tostring(kind or &quot;&quot;):lower()
  local attack = nil

  if key == &quot;safe&quot; then
    attack = &quot;pound&quot;
  elseif key == &quot;mid&quot; then
    attack = &quot;crush&quot;
  elseif key == &quot;heavy&quot; then
    attack = &quot;smash&quot;
  elseif key == &quot;auto&quot; then
    if Tempest.risk_band == &quot;good&quot; then
      attack = &quot;smash&quot;
    elseif Tempest.risk_band == &quot;bad&quot; or Tempest.risk_band == &quot;critical&quot; then
      attack = &quot;pound&quot;
    else
      attack = &quot;crush&quot;
    end
  else
    cecho(&quot;&lt;red&gt;[Tempest] Unknown melee mode. Use: safe, mid, heavy, auto.\n&quot;)
    return false
  end

  Tempest.send_basic_attack(attack, explicit_target)
  return true
end</script>
        <eventHandlerList />quot;):lower()
  if text == &quot;&quot; then
    return nil
  end

  if text == &quot;min&quot; or text == &quot;max&quot; or text:match(&quot;^%d+<name>target helper</name>
        <packageName></packageName>
        <script>Tempest = Tempest or {}

Tempest.target = Tempest.target or &quot;&quot;
Tempest.risk_band = Tempest.risk_band or &quot;neutral&quot;

function Tempest.set_target(name)
  name = tostring(name or &quot;&quot;):match(&quot;^%s*(.-)%s*$&quot;)

  if name == &quot;&quot; then
    cecho(&quot;&lt;red&gt;[Tempest] No target supplied.\n&quot;)
    return false
  end

  Tempest.target = name

  -- Compatibility with older/simple aliases that may expect a global target.
  target = name

  cecho(&quot;&lt;cyan&gt;[Tempest] Target set: &lt;white&gt;&quot; .. name .. &quot;\n&quot;)
  return true
end

function Tempest.get_target()
  if Tempest.target and Tempest.target ~= &quot;&quot; then
    return Tempest.target
  end

  if target and target ~= &quot;&quot; then
    return target
  end

  return nil
end

function Tempest.require_target()
  local tgt = Tempest.get_target()

  if not tgt or tgt == &quot;&quot; then
    cecho(&quot;&lt;red&gt;[Tempest] No target set. Use: tt &lt;name&gt;\n&quot;)
    return nil
  end

  return tgt
end

function Tempest.normalize_favors(value, silent)
  local text = tostring(value or &quot;&quot;):match(&quot;^%s*(.-)%s*$&quot;):lower()
  if text == &quot;&quot; then
    return nil
  end

  if text == &quot;min&quot; or text == &quot;max&quot; or text:match(&quot;^%d+$&quot;) then
    return text
  end

  if not silent then
    cecho(&quot;&lt;red&gt;[Tempest] Invalid favors value. Use: min, max, or a number.\n&quot;)
  end

  return nil
end

function Tempest.send_pray(gift, target_name, favors)
  local cmd = &quot;pray &quot; .. gift
  local target_text = tostring(target_name or &quot;&quot;):match(&quot;^%s*(.-)%s*$&quot;)
  if target_text ~= &quot;&quot; then
    cmd = cmd .. &quot; &quot; .. target_text
  end

  local favors_text = Tempest.normalize_favors(favors)
  if favors_text then
    cmd = cmd .. &quot; &quot; .. favors_text
  end

  send(cmd)
end

local function resolve_attack_target(explicit_target)
  local text = tostring(explicit_target or &quot;&quot;):match(&quot;^%s*(.-)%s*$&quot;)
  if text ~= &quot;&quot; then
    return text
  end
  return Tempest.get_target()
end

function Tempest.send_basic_attack(attack, explicit_target)
  local target_name = resolve_attack_target(explicit_target)
  if target_name and target_name ~= &quot;&quot; then
    send(attack .. &quot; &quot; .. target_name)
  else
    send(attack)
  end
end

function Tempest.set_risk_band(value)
  local band = tostring(value or &quot;&quot;):lower():match(&quot;^%s*(.-)%s*$&quot;)
  local valid = {
    good = true,
    neutral = true,
    bad = true,
    critical = true,
  }

  if not valid[band] then
    cecho(&quot;&lt;red&gt;[Tempest] Invalid risk band. Use: good, neutral, bad, critical.\n&quot;)
    return false
  end

  Tempest.risk_band = band
  cecho(&quot;&lt;cyan&gt;[Tempest] Risk band set: &lt;white&gt;&quot; .. band .. &quot;\n&quot;)
  return true
end

function Tempest.send_melee(kind, explicit_target)
  local key = tostring(kind or &quot;&quot;):lower()
  local attack = nil

  if key == &quot;safe&quot; then
    attack = &quot;pound&quot;
  elseif key == &quot;mid&quot; then
    attack = &quot;crush&quot;
  elseif key == &quot;heavy&quot; then
    attack = &quot;smash&quot;
  elseif key == &quot;auto&quot; then
    if Tempest.risk_band == &quot;good&quot; then
      attack = &quot;smash&quot;
    elseif Tempest.risk_band == &quot;bad&quot; or Tempest.risk_band == &quot;critical&quot; then
      attack = &quot;pound&quot;
    else
      attack = &quot;crush&quot;
    end
  else
    cecho(&quot;&lt;red&gt;[Tempest] Unknown melee mode. Use: safe, mid, heavy, auto.\n&quot;)
    return false
  end

  Tempest.send_basic_attack(attack, explicit_target)
  return true
end</script>
        <eventHandlerList />quot;) then
    return text
  end

  if not silent then
    cecho(&quot;&lt;red&gt;[Tempest] Invalid favors value. Use: min, max, or a number.\n&quot;)
  end
  return nil
end

function Tempest.send_pray(gift, target_name, favors)
  local cmd = &quot;pray &quot; .. gift
  local target_text = tostring(target_name or &quot;&quot;):match(&quot;^%s*(.-)%s*<name>target helper</name>
        <packageName></packageName>
        <script>Tempest = Tempest or {}

Tempest.target = Tempest.target or &quot;&quot;
Tempest.risk_band = Tempest.risk_band or &quot;neutral&quot;

function Tempest.set_target(name)
  name = tostring(name or &quot;&quot;):match(&quot;^%s*(.-)%s*$&quot;)

  if name == &quot;&quot; then
    cecho(&quot;&lt;red&gt;[Tempest] No target supplied.\n&quot;)
    return false
  end

  Tempest.target = name

  -- Compatibility with older/simple aliases that may expect a global target.
  target = name

  cecho(&quot;&lt;cyan&gt;[Tempest] Target set: &lt;white&gt;&quot; .. name .. &quot;\n&quot;)
  return true
end

function Tempest.get_target()
  if Tempest.target and Tempest.target ~= &quot;&quot; then
    return Tempest.target
  end

  if target and target ~= &quot;&quot; then
    return target
  end

  return nil
end

function Tempest.require_target()
  local tgt = Tempest.get_target()

  if not tgt or tgt == &quot;&quot; then
    cecho(&quot;&lt;red&gt;[Tempest] No target set. Use: tt &lt;name&gt;\n&quot;)
    return nil
  end

  return tgt
end

function Tempest.normalize_favors(value, silent)
  local text = tostring(value or &quot;&quot;):match(&quot;^%s*(.-)%s*$&quot;):lower()
  if text == &quot;&quot; then
    return nil
  end

  if text == &quot;min&quot; or text == &quot;max&quot; or text:match(&quot;^%d+$&quot;) then
    return text
  end

  if not silent then
    cecho(&quot;&lt;red&gt;[Tempest] Invalid favors value. Use: min, max, or a number.\n&quot;)
  end

  return nil
end

function Tempest.send_pray(gift, target_name, favors)
  local cmd = &quot;pray &quot; .. gift
  local target_text = tostring(target_name or &quot;&quot;):match(&quot;^%s*(.-)%s*$&quot;)
  if target_text ~= &quot;&quot; then
    cmd = cmd .. &quot; &quot; .. target_text
  end

  local favors_text = Tempest.normalize_favors(favors)
  if favors_text then
    cmd = cmd .. &quot; &quot; .. favors_text
  end

  send(cmd)
end

local function resolve_attack_target(explicit_target)
  local text = tostring(explicit_target or &quot;&quot;):match(&quot;^%s*(.-)%s*$&quot;)
  if text ~= &quot;&quot; then
    return text
  end
  return Tempest.get_target()
end

function Tempest.send_basic_attack(attack, explicit_target)
  local target_name = resolve_attack_target(explicit_target)
  if target_name and target_name ~= &quot;&quot; then
    send(attack .. &quot; &quot; .. target_name)
  else
    send(attack)
  end
end

function Tempest.set_risk_band(value)
  local band = tostring(value or &quot;&quot;):lower():match(&quot;^%s*(.-)%s*$&quot;)
  local valid = {
    good = true,
    neutral = true,
    bad = true,
    critical = true,
  }

  if not valid[band] then
    cecho(&quot;&lt;red&gt;[Tempest] Invalid risk band. Use: good, neutral, bad, critical.\n&quot;)
    return false
  end

  Tempest.risk_band = band
  cecho(&quot;&lt;cyan&gt;[Tempest] Risk band set: &lt;white&gt;&quot; .. band .. &quot;\n&quot;)
  return true
end

function Tempest.send_melee(kind, explicit_target)
  local key = tostring(kind or &quot;&quot;):lower()
  local attack = nil

  if key == &quot;safe&quot; then
    attack = &quot;pound&quot;
  elseif key == &quot;mid&quot; then
    attack = &quot;crush&quot;
  elseif key == &quot;heavy&quot; then
    attack = &quot;smash&quot;
  elseif key == &quot;auto&quot; then
    if Tempest.risk_band == &quot;good&quot; then
      attack = &quot;smash&quot;
    elseif Tempest.risk_band == &quot;bad&quot; or Tempest.risk_band == &quot;critical&quot; then
      attack = &quot;pound&quot;
    else
      attack = &quot;crush&quot;
    end
  else
    cecho(&quot;&lt;red&gt;[Tempest] Unknown melee mode. Use: safe, mid, heavy, auto.\n&quot;)
    return false
  end

  Tempest.send_basic_attack(attack, explicit_target)
  return true
end</script>
        <eventHandlerList />quot;)
  if target_text ~= &quot;&quot; then
    cmd = cmd .. &quot; &quot; .. target_text
  end

  local favors_text = Tempest.normalize_favors(favors)
  if favors_text then
    cmd = cmd .. &quot; &quot; .. favors_text
  end

  send(cmd)
end

local function resolve_attack_target(explicit_target)
  local text = tostring(explicit_target or &quot;&quot;):match(&quot;^%s*(.-)%s*<name>target helper</name>
        <packageName></packageName>
        <script>Tempest = Tempest or {}

Tempest.target = Tempest.target or &quot;&quot;
Tempest.risk_band = Tempest.risk_band or &quot;neutral&quot;

function Tempest.set_target(name)
  name = tostring(name or &quot;&quot;):match(&quot;^%s*(.-)%s*$&quot;)

  if name == &quot;&quot; then
    cecho(&quot;&lt;red&gt;[Tempest] No target supplied.\n&quot;)
    return false
  end

  Tempest.target = name

  -- Compatibility with older/simple aliases that may expect a global target.
  target = name

  cecho(&quot;&lt;cyan&gt;[Tempest] Target set: &lt;white&gt;&quot; .. name .. &quot;\n&quot;)
  return true
end

function Tempest.get_target()
  if Tempest.target and Tempest.target ~= &quot;&quot; then
    return Tempest.target
  end

  if target and target ~= &quot;&quot; then
    return target
  end

  return nil
end

function Tempest.require_target()
  local tgt = Tempest.get_target()

  if not tgt or tgt == &quot;&quot; then
    cecho(&quot;&lt;red&gt;[Tempest] No target set. Use: tt &lt;name&gt;\n&quot;)
    return nil
  end

  return tgt
end

function Tempest.normalize_favors(value, silent)
  local text = tostring(value or &quot;&quot;):match(&quot;^%s*(.-)%s*$&quot;):lower()
  if text == &quot;&quot; then
    return nil
  end

  if text == &quot;min&quot; or text == &quot;max&quot; or text:match(&quot;^%d+$&quot;) then
    return text
  end

  if not silent then
    cecho(&quot;&lt;red&gt;[Tempest] Invalid favors value. Use: min, max, or a number.\n&quot;)
  end

  return nil
end

function Tempest.send_pray(gift, target_name, favors)
  local cmd = &quot;pray &quot; .. gift
  local target_text = tostring(target_name or &quot;&quot;):match(&quot;^%s*(.-)%s*$&quot;)
  if target_text ~= &quot;&quot; then
    cmd = cmd .. &quot; &quot; .. target_text
  end

  local favors_text = Tempest.normalize_favors(favors)
  if favors_text then
    cmd = cmd .. &quot; &quot; .. favors_text
  end

  send(cmd)
end

local function resolve_attack_target(explicit_target)
  local text = tostring(explicit_target or &quot;&quot;):match(&quot;^%s*(.-)%s*$&quot;)
  if text ~= &quot;&quot; then
    return text
  end
  return Tempest.get_target()
end

function Tempest.send_basic_attack(attack, explicit_target)
  local target_name = resolve_attack_target(explicit_target)
  if target_name and target_name ~= &quot;&quot; then
    send(attack .. &quot; &quot; .. target_name)
  else
    send(attack)
  end
end

function Tempest.set_risk_band(value)
  local band = tostring(value or &quot;&quot;):lower():match(&quot;^%s*(.-)%s*$&quot;)
  local valid = {
    good = true,
    neutral = true,
    bad = true,
    critical = true,
  }

  if not valid[band] then
    cecho(&quot;&lt;red&gt;[Tempest] Invalid risk band. Use: good, neutral, bad, critical.\n&quot;)
    return false
  end

  Tempest.risk_band = band
  cecho(&quot;&lt;cyan&gt;[Tempest] Risk band set: &lt;white&gt;&quot; .. band .. &quot;\n&quot;)
  return true
end

function Tempest.send_melee(kind, explicit_target)
  local key = tostring(kind or &quot;&quot;):lower()
  local attack = nil

  if key == &quot;safe&quot; then
    attack = &quot;pound&quot;
  elseif key == &quot;mid&quot; then
    attack = &quot;crush&quot;
  elseif key == &quot;heavy&quot; then
    attack = &quot;smash&quot;
  elseif key == &quot;auto&quot; then
    if Tempest.risk_band == &quot;good&quot; then
      attack = &quot;smash&quot;
    elseif Tempest.risk_band == &quot;bad&quot; or Tempest.risk_band == &quot;critical&quot; then
      attack = &quot;pound&quot;
    else
      attack = &quot;crush&quot;
    end
  else
    cecho(&quot;&lt;red&gt;[Tempest] Unknown melee mode. Use: safe, mid, heavy, auto.\n&quot;)
    return false
  end

  Tempest.send_basic_attack(attack, explicit_target)
  return true
end</script>
        <eventHandlerList />quot;)
  if text ~= &quot;&quot; then
    return text
  end
  return Tempest.get_target()
end

function Tempest.send_basic_attack(attack, explicit_target)
  local target_name = resolve_attack_target(explicit_target)
  if target_name and target_name ~= &quot;&quot; then
    if is_target_self(target_name) then
      cecho(&quot;&lt;red&gt;[Tempest] Refusing to attack yourself; fix target (tt) or character name (Tempest.set_character_name).\n&quot;)
      return false
    end
    send(attack .. &quot; &quot; .. target_name)
  else
    send(attack)
  end
  return true
end

function Tempest.set_risk_band(value)
  local band = tostring(value or &quot;&quot;):lower():match(&quot;^%s*(.-)%s*<name>target helper</name>
        <packageName></packageName>
        <script>Tempest = Tempest or {}

Tempest.target = Tempest.target or &quot;&quot;
Tempest.risk_band = Tempest.risk_band or &quot;neutral&quot;

function Tempest.set_target(name)
  name = tostring(name or &quot;&quot;):match(&quot;^%s*(.-)%s*$&quot;)

  if name == &quot;&quot; then
    cecho(&quot;&lt;red&gt;[Tempest] No target supplied.\n&quot;)
    return false
  end

  Tempest.target = name

  -- Compatibility with older/simple aliases that may expect a global target.
  target = name

  cecho(&quot;&lt;cyan&gt;[Tempest] Target set: &lt;white&gt;&quot; .. name .. &quot;\n&quot;)
  return true
end

function Tempest.get_target()
  if Tempest.target and Tempest.target ~= &quot;&quot; then
    return Tempest.target
  end

  if target and target ~= &quot;&quot; then
    return target
  end

  return nil
end

function Tempest.require_target()
  local tgt = Tempest.get_target()

  if not tgt or tgt == &quot;&quot; then
    cecho(&quot;&lt;red&gt;[Tempest] No target set. Use: tt &lt;name&gt;\n&quot;)
    return nil
  end

  return tgt
end

function Tempest.normalize_favors(value, silent)
  local text = tostring(value or &quot;&quot;):match(&quot;^%s*(.-)%s*$&quot;):lower()
  if text == &quot;&quot; then
    return nil
  end

  if text == &quot;min&quot; or text == &quot;max&quot; or text:match(&quot;^%d+$&quot;) then
    return text
  end

  if not silent then
    cecho(&quot;&lt;red&gt;[Tempest] Invalid favors value. Use: min, max, or a number.\n&quot;)
  end

  return nil
end

function Tempest.send_pray(gift, target_name, favors)
  local cmd = &quot;pray &quot; .. gift
  local target_text = tostring(target_name or &quot;&quot;):match(&quot;^%s*(.-)%s*$&quot;)
  if target_text ~= &quot;&quot; then
    cmd = cmd .. &quot; &quot; .. target_text
  end

  local favors_text = Tempest.normalize_favors(favors)
  if favors_text then
    cmd = cmd .. &quot; &quot; .. favors_text
  end

  send(cmd)
end

local function resolve_attack_target(explicit_target)
  local text = tostring(explicit_target or &quot;&quot;):match(&quot;^%s*(.-)%s*$&quot;)
  if text ~= &quot;&quot; then
    return text
  end
  return Tempest.get_target()
end

function Tempest.send_basic_attack(attack, explicit_target)
  local target_name = resolve_attack_target(explicit_target)
  if target_name and target_name ~= &quot;&quot; then
    send(attack .. &quot; &quot; .. target_name)
  else
    send(attack)
  end
end

function Tempest.set_risk_band(value)
  local band = tostring(value or &quot;&quot;):lower():match(&quot;^%s*(.-)%s*$&quot;)
  local valid = {
    good = true,
    neutral = true,
    bad = true,
    critical = true,
  }

  if not valid[band] then
    cecho(&quot;&lt;red&gt;[Tempest] Invalid risk band. Use: good, neutral, bad, critical.\n&quot;)
    return false
  end

  Tempest.risk_band = band
  cecho(&quot;&lt;cyan&gt;[Tempest] Risk band set: &lt;white&gt;&quot; .. band .. &quot;\n&quot;)
  return true
end

function Tempest.send_melee(kind, explicit_target)
  local key = tostring(kind or &quot;&quot;):lower()
  local attack = nil

  if key == &quot;safe&quot; then
    attack = &quot;pound&quot;
  elseif key == &quot;mid&quot; then
    attack = &quot;crush&quot;
  elseif key == &quot;heavy&quot; then
    attack = &quot;smash&quot;
  elseif key == &quot;auto&quot; then
    if Tempest.risk_band == &quot;good&quot; then
      attack = &quot;smash&quot;
    elseif Tempest.risk_band == &quot;bad&quot; or Tempest.risk_band == &quot;critical&quot; then
      attack = &quot;pound&quot;
    else
      attack = &quot;crush&quot;
    end
  else
    cecho(&quot;&lt;red&gt;[Tempest] Unknown melee mode. Use: safe, mid, heavy, auto.\n&quot;)
    return false
  end

  Tempest.send_basic_attack(attack, explicit_target)
  return true
end</script>
        <eventHandlerList />quot;)
  local valid = {
    good = true,
    neutral = true,
    bad = true,
    critical = true,
  }

  if not valid[band] then
    cecho(&quot;&lt;red&gt;[Tempest] Invalid risk band. Use: good, neutral, bad, critical.\n&quot;)
    return false
  end

  Tempest.risk_band = band
  cecho(&quot;&lt;cyan&gt;[Tempest] Risk band set: &lt;white&gt;&quot; .. band .. &quot;\n&quot;)
  return true
end

function Tempest.send_melee(kind, explicit_target)
  local key = tostring(kind or &quot;&quot;):lower()
  local tier = nil

  if key == &quot;safe&quot; then
    tier = &quot;safe&quot;
  elseif key == &quot;mid&quot; then
    tier = &quot;mid&quot;
  elseif key == &quot;heavy&quot; then
    tier = &quot;heavy&quot;
  elseif key == &quot;auto&quot; then
    if Tempest.risk_band == &quot;good&quot; then
      tier = &quot;heavy&quot;
    elseif Tempest.risk_band == &quot;bad&quot; or Tempest.risk_band == &quot;critical&quot; then
      tier = &quot;safe&quot;
    else
      tier = &quot;mid&quot;
    end
  else
    cecho(&quot;&lt;red&gt;[Tempest] Unknown melee mode. Use: safe, mid, heavy, auto.\n&quot;)
    return false
  end

  local attack = Tempest.melee_verb_for_tier(tier)
  if not attack then
    return false
  end

  Tempest.send_basic_attack(attack, explicit_target)
  return true
end
</script>
        <eventHandlerList />
      </Script>
    </ScriptGroup>
  </ScriptPackage>
  <KeyPackage />
</MudletPackage>

